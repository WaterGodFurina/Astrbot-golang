# Part 01：Astrbot-golang/internal/dashboard（Go WebUI 后端）

统计：发现 3 个问题（高 0 / 中 0 / 低 3）。

### [internal/dashboard/handlers.go:5428] 备份分片大小校验过宽 [严重程度：低]
- 严重程度：低（systemOnly 端点，仅已认证管理员可触发）
- 问题描述：`writeChunkFile` 对每个分片只限制 `maxMultipartBodySize`（256MB），且 `complete` 合并时未核对合并后总大小与 init 时声明的 `TotalSize` 一致：
```go
_, err = io.Copy(dst, io.LimitReader(src, maxMultipartBodySize))
```
  声明 1 字节的 TotalSize 也可以上传 total_chunks × 256MB 的数据，磁盘可被已认证用户大量占用。
- 修复建议：
```go
func writeChunkFile(chunkPath string, src io.Reader, limit int64) error {
    dst, err := os.Create(chunkPath)
    if err != nil { return err }
    n, err := io.Copy(dst, io.LimitReader(src, limit+1))
    _ = dst.Close()
    if err != nil { return err }
    if n > limit { return fmt.Errorf("分片超过声明大小") }
    return nil
}
// complete 合并后校验：
if fileSize != session.TotalSize { _ = os.Remove(outputPath); /* 拒绝 */ }
```

### [internal/dashboard/auth.go:378] 配置文件损坏时后续写回会丢失其余配置段 [严重程度：低]
- 严重程度：低（触发条件苛刻：JSON 已损坏；且已有 .corrupt.bak 备份）
- 问题描述：`loadConfigOrNew` 在 JSON 损坏时备份后返回空 map，随后 `writeDashboardDirect`（auth 持久化路径）以"仅含 dashboard 段"的 map 覆盖写回整个配置文件，其余配置段（provider/platform 等）静默丢失。
```go
if err := json.Unmarshal(data, &cfg); err != nil {
    backup := path + ".corrupt.bak"
    ...
}
return cfg // 空 map
```
- 修复建议：损坏时给 PasswordManager 打"只读"标记，`writeDashboardDirect` 检测到该标记时拒绝覆盖写并提示用户从备份恢复：
```go
if pm.configCorrupt {
    authLogger.Error("配置文件 JSON 损坏，跳过写回以免丢失其余配置段，请从 %s 恢复", pm.configPath+".corrupt.bak")
    return
}
```

### [internal/dashboard/handlers.go:6611] go install 包名未做格式校验 [严重程度：低]
- 严重程度：低（systemOnly 端点；实际风险取决于 internal/plugin 的 GoInstall 实现，见 Part 05）
- 问题描述：`installGoPackage` 把 `body.Package` 原样传给 `GoInstall`，若以 `-` 开头可能被 go 子命令解析为 flag。
```go
out, err := s.subPluginMgr.GoInstall(ctx, body.Package, body.Mirror)
```
- 修复建议：入口处白名单校验：
```go
if !regexp.MustCompile(`^[A-Za-z0-9._~+/:@-]+$`).MatchString(body.Package) || strings.HasPrefix(body.Package, "-") {
    writeJSON(w, http.StatusBadRequest, apiError("非法的包名")); return
}
```

# Part 04：Astrbot-golang/internal/pipeline（消息处理管线）

统计：发现 16 个问题（高 1 / 中 5 / 低 10）。

### [internal/pipeline/stages.go:1577] 用户图片/语音从未传入 LLM 请求，多模态输入整体断链 [严重程度：高]
- 严重程度：高（用户发送任何图片/语音消息必然触发：模型只能看到文字，视觉输入被静默丢弃；属必然触发的功能性缺陷，非安全漏洞）
- 问题描述：`prepareAgentRequest` 组装主对话请求时，`ImageURLs`/`AudioURLs` 被硬编码为空切片，事件消息链中的 Image/Record 组件从未被收集进请求：

```go
// stages.go:1573-1581
req := &provider.ProviderRequest{
    Prompt:       prompt,
    SessionID:    event.UnifiedMsgOrigin(),
    SystemPrompt: systemPrompt,
    ImageURLs:    []string{},
    AudioURLs:    []string{},
    Conversation: s.convMgr,
    Contexts:     s.conversationHistory(event.UnifiedMsgOrigin()),
}
```

全仓库检索确认 `req.ImageURLs` 在管线内没有任何赋值来源（唯一给 `ImageURLs` 传值的是插件 RPC 路径 plugin/host_service.go:417 与 group_context.go 的图片描述器）；而 provider 侧 `AssembleContext()`（internal/provider/entities.go:214）正是从 `r.ImageURLs` 构造 `image_url` 内容块的。因此：
1. 用户发图片 + "这是什么？"，LLM 只收到文字 prompt；纯图片消息则 `prompt == ""` 在 resolveAgentContext 直接返回不调用 LLM（stages.go:1413-1415）。
2. stages.go:1592-1614 的图片压缩逻辑（`if len(req.ImageURLs) > 0`）成为永不可达的死代码。
3. `sanitize_context_by_modalities`、`providerModalities` 对 image 的处理同样只对空切片生效。

与上游 Python（astr_main_agent 将消息链中的 Image 组件编入 user content）行为不一致。
- 修复建议：

```go
// prepareAgentRequest 中，构建 req 前从事件链收集媒体（对齐 Python）：
var imageURLs, audioURLs []string
if event.Message != nil {
    for _, comp := range event.Message.Chain {
        switch c := comp.(type) {
        case *message.Image:
            if u := c.URL; u != "" {
                imageURLs = append(imageURLs, u)
            } else if c.Path != "" {
                imageURLs = append(imageURLs, "file://"+c.Path)
            } else if c.Base64 != "" {
                imageURLs = append(imageURLs, "data:image/png;base64,"+c.Base64)
            }
        case *message.Record:
            if u := c.URL; u != "" {
                audioURLs = append(audioURLs, u)
            } else if c.Path != "" {
                audioURLs = append(audioURLs, "file://"+c.Path)
            }
        }
    }
}
req := &provider.ProviderRequest{
    ...
    ImageURLs: imageURLs,
    AudioURLs: audioURLs,
    ...
}
```

### [internal/pipeline/computer_tools.go:986] executeFileRead 拒绝大于 1MB 的文件，却提示用 offset/limit 分段读取——该补救路径不存在 [严重程度：中]
- 严重程度：中（模型/用户读取任何 >1MB 文件必然触发，工具自我矛盾，长日志、数据文件永远无法读取）
- 问题描述：大小检查在 `os.ReadFile` 之前，且不区分是否传了 offset/limit：

```go
// computer_tools.go:985-990
// 先 stat 判断大小：超限文件拒绝整体读入内存，提示用 offset/limit 分段读取。
if info.Size() > maxFileReadBytes {
    return fmt.Sprintf("Error reading file: %s is %d bytes, exceeds the %d-byte read limit. "+
        "Use offset/limit to read a portion of the file.", resolved, info.Size(), maxFileReadBytes)
}
data, err := os.ReadFile(resolved)
```

即使模型按提示带上 `offset/limit`，请求仍会先命中同一 `info.Size() > maxFileReadBytes` 分支被拒绝——分页读取根本无法绕过整文件大小检查（且即使放行，当前实现也是先 `os.ReadFile` 全量读入再切行，"分段读取省内存"的设计意图未实现）。
- 修复建议：

```go
if info.Size() > maxFileReadBytes {
    if offset <= 0 && limit <= 0 {
        return fmt.Sprintf("Error reading file: %s is %d bytes, exceeds the %d-byte read limit. "+
            "Use offset/limit to read a portion of the file.", resolved, info.Size(), maxFileReadBytes)
    }
    // 分页路径：按行流式读取，不整体载入内存
    f, err := os.Open(resolved)
    if err != nil {
        return "Error reading file: " + err.Error()
    }
    defer f.Close()
    var lines []string
    sc := bufio.NewScanner(f)
    sc.Buffer(make([]byte, 0, 64*1024), 1024*1024)
    for i := 0; sc.Scan() && (limit <= 0 || i < offset+limit); i++ {
        if i >= offset {
            lines = append(lines, sc.Text())
        }
    }
    return fmt.Sprintf("Read %d lines from %s:\n%s", len(lines), resolved, strings.Join(lines, "\n"))
}
```

### [internal/pipeline/doom_loop.go:29] astrbot_shell_session 的 poll/list 轮询被 doom-loop 检测误判，默认 5 轮内必然暂停工具 [严重程度：中]
- 严重程度：中（长任务轮询场景常见触发：模型每轮 poll 同一会话，第 5 次调用即被判定为死循环并暂停工具、要求用户回复"继续"）
- 问题描述：doom-loop 的重复键由工具名+参数派生，shell 会话的 poll 动作没有 command 参数，因此每次轮询的键完全相同：

```go
// doom_loop.go:29-37
func doomLoopKey(toolName string, args map[string]interface{}) string {
    switch toolName {
    case "astrbot_execute_shell":
        return toolName + "\x00" + argString(args, "command")
    case "astrbot_shell_session":
        return toolName + "\x00" + argString(args, "action") + "\x00" + argString(args, "command")
    }
    return toolName
}
```

`runAgentToolLoop` 中每个工具调用都经过 `s.checkDoomLoop(event, doomLoopKey(name, args))`（stages.go:1764），而 `checkDoomLoop` 在同一请求内累计计数（`resetDoomLoopCount` 仅在每次 LLM 请求入口调用一次，stages.go:1420）。于是：模型在默认 `maxSteps=5` 的工具循环里逐轮 `poll` 同一会话（完全正常的长任务等待模式），第 5 次 poll 使 `count >= doomLoopThreshold(5)`，触发暂停并群发确认请求——合法轮询被当成死循环。`list` 动作同理。
- 修复建议：

```go
func doomLoopKey(toolName string, args map[string]interface{}) string {
    switch toolName {
    case "astrbot_execute_shell":
        return toolName + "\x00" + argString(args, "command")
    case "astrbot_shell_session":
        action := argString(args, "action")
        // 只读动作（list/poll/get/status）是合法的重复轮询，不参与死循环检测；
        // 会改变状态的 write/write_line/interrupt/terminate 按内容计键。
        switch action {
        case "list", "poll", "get", "status":
            return "" // 空键 = 豁免
        }
        return toolName + "\x00" + action + "\x00" + argString(args, "command")
    }
    return toolName
}
// checkDoomLoop 开头：
if key := doomLoopKey(toolName, args); key == "" { return true }
```

### [internal/pipeline/computer_tools.go:698] 本地 shell/python 工具的 timeout 参数取自 LLM 无上限（沙箱路径有 600s 封顶，本地路径没有） [严重程度：中]
- 严重程度：中（被提示注入的模型可传超大 timeout；结合工具超时后"放任 goroutine 排水"的设计，宿主进程可长期驻留）
- 问题描述：本地运行时仅做下限修正，无上限：

```go
// computer_tools.go:698-700（executeLocalShell）
if timeout <= 0 {
    timeout = 300
}
// computer_tools.go:935-937（executeLocalPython）
if timeout <= 0 {
    timeout = 30
}
```

而沙箱执行器对同一参数做了封顶（stages.go:3456-3458 `if timeout > 600 { timeout = 600 }`），两个路径不对称。同时本地 shell/python 的 ctx 基于 `context.Background()`（computer_tools.go:776、939），不继承 `executeToolWithTimeout` 的取消：`toolCallTimeout`（默认 120s）到期后管线放弃等待，但宿主上的 `sh`/`python3` 进程与阻塞在 `cmd.Wait()` 的 goroutine 会一直存活到 LLM 给出的 timeout——传入 `timeout=86400` 即可让一个 `sleep` 类命令驻留一天（输出虽被 cappedWriter 限幅，进程与 goroutine 仍在）。
- 修复建议：

```go
// executeLocalShell / executeLocalPython 统一钳制：
if timeout <= 0 {
    timeout = 300
}
if timeout > 600 {
    timeout = 600 // 与沙箱路径一致
}
// 更彻底：把外层 ctx 传入并组合，使工具调用超时能真正终止进程组
ctx, cancel := context.WithTimeout(outerCtx, time.Duration(timeout)*time.Second)
```

### [internal/pipeline/stages.go:2429] collectPluginTools 在 LLM 热路径同步调用 RefreshTools（阻塞 RPC，30s 超时） [严重程度：中]
- 严重程度：中（任一插件进程卡顿时，TTL 过期后的每次 LLM 调用最长阻塞 30 秒/插件；N 个插件同时过期则 N×30s）
- 问题描述：工具收集在每次 LLM 请求的同步路径上逐插件刷新：

```go
// stages.go:2429-2437
for _, inst := range s.subPlugins.List() {
    if inst.Meta == nil {
        continue
    }
    if inst.ToolsFreshWithin(toolsRefreshTTL) {
        continue
    }
    inst.RefreshTools(context.Background())
}
```

`RefreshTools` 是同步 RPC（internal/plugin/runtime.go:244-250，`ListTools` 带 30s 超时），且注释声称"避免每次 LLM 调用都同步发起 ListTools RPC"，但 TTL 过期时仍会阻塞当前请求。插件假死/被调试器暂停/网络抖动时，用户消息在 ProcessStage 内长时间无响应；`executePluginTool` 唤醒休眠插件后同样同步调用（stages.go:2500）。
- 修复建议：

```go
if inst.ToolsFreshWithin(toolsRefreshTTL) {
    continue
}
// TTL 过期：先用旧快照（若有），后台异步刷新，下次请求生效
go inst.RefreshTools(context.Background())
```

### [internal/pipeline/group_context.go:170] RemoveSession 是死代码：群上下文记录与锁表只增不减，会话重置后旧记录仍被注入 [严重程度：中]
- 严重程度：中（长期运行内存缓慢增长；/reset 或删除会话后，历史群聊记录在下一次请求仍注入 prompt，数据一致性缺陷）
- 问题描述：`RemoveSession` 实现完整且加锁正确（group_context.go:170-188），但全仓库无任何调用方（grep 仅剩定义处）；`locks`、`rawRecords`、`recordIDs` 三个 map 的条目只增不删。`OnReqLLM` 只有在 LLM 请求发生时才丢弃已注入记录（group_context.go:276-306），因此：
1. 每个出现过群聊的 UMO 永久保留一个 mutex 与至多 `group_message_max_cnt`（默认 1000）条字符串记录，无 TTL、无淘汰；
2. 用户执行 /reset（清空对话）后，`group_icl_enable` 场景下一次请求仍会注入 reset 之前积累的群聊上下文，"新会话"带着旧记忆。
- 修复建议：在会话删除/重置入口（builtin commands 的 reset、dashboard 会话删除）调用 `groupCtx.RemoveSession(umo)`；并在 `pruneDoomTrackers` 风格上给 `prune` 补充空闲淘汰：

```go
// ProcessStage 暴露方法，供 reset/delete 流程调用：
func (s *ProcessStage) ResetGroupContext(umo string) int {
    if s.groupCtx == nil {
        return 0
    }
    return s.groupCtx.RemoveSession(umo)
}
// 或在 GroupChatContext 增加按 lastSeen 的 TTL 淘汰（记录每个 umo 最后活动时间）。
```

### [internal/pipeline/stages.go:1125] 任一 handler 权限过滤失败即终止整个事件，其他已匹配 handler 与 LLM 兜底均被跳过 [严重程度：低]
- 严重程度：低（需要同一消息同时命中一个高权限 handler 与另一个放行 handler/唤醒 LLM 时触发；方向是过度限制而非提权）
- 问题描述：

```go
// stages.go:1121-1136
activated, permissionDenied := s.findMatchingHandlers(event)
activated = s.filterHandlersBySession(event, activated)
if permissionDenied {
    if s.noPermissionReply && !event.IsStopped() {
        s.replyText(event, ...)
    }
    event.Stop()
    return &StageResult{Continue: false}, nil
}
```

`findMatchingHandlers`（stages.go:1240-1257）中 PermissionFilter 失败只把 `filterDenied` 置位、该 handler 被跳过，但只要任何一个 handler 权限失败，`permissionDenied=true` 就使整个事件停止——`activated` 中其他完全放行的 handler 不再执行，唤醒了的消息也不再走 LLM。上游 Python（waking_check/stage.py）对非 `raise_error` 的权限失败仅跳过该 handler，其余 handler 照常激活。
- 修复建议：

```go
if permissionDenied && len(activated) == 0 {
    if s.noPermissionReply && !event.IsStopped() { s.replyText(...) }
    event.Stop()
    return &StageResult{Continue: false}, nil
}
// permissionDenied 且仍有放行 handler 时照常执行 activated；
// 事件处理完毕后无 Result 且 permissionDenied，再决定是否提示权限不足。
```

### [internal/pipeline/stages.go:1848] 工具循环被 doom 暂停或 maxSteps 用尽时，空 CompletionText 仍写入会话历史 [严重程度：低]
- 严重程度：低（doom 暂停或达到最大步数且模型仍在请求工具时触发；历史被空 assistant 消息污染，后续请求携带无效上下文）
- 问题描述：`runAgentToolLoop` 在 `doomed` 中断（stages.go:1811-1813）或 `round == maxSteps` 退出循环时，`resp` 是最后一带工具调用的响应，`CompletionText` 通常为空，但仍返回 `ok=true`：

```go
// stages.go:1846-1850
if s.convMgr != nil {
    s.convMgr.AppendHistory(event.UnifiedMsgOrigin(), "user", ar.prompt)
    s.convMgr.AppendHistory(event.UnifiedMsgOrigin(), "assistant", resp.CompletionText)
}
```

结果：会话历史出现 `{"role":"assistant","content":""}` 条目（无对应工具结果，破坏 user/assistant 配对），且 `event.Result` 为空文本链。用户只看到 doom 提示或干脆没有回复，无"已达最大工具步数"之类的说明。
- 修复建议：

```go
if strings.TrimSpace(resp.CompletionText) == "" && len(resp.ToolsCallName) > 0 {
    resp.CompletionText = "（已达最大工具调用步数/工具被暂停，未能生成最终回复。）"
    if s.convMgr != nil {
        s.convMgr.AppendHistory(event.UnifiedMsgOrigin(), "user", ar.prompt)
        s.convMgr.AppendHistory(event.UnifiedMsgOrigin(), "assistant", resp.CompletionText)
    }
    ... // 正常设置 event.Result，给用户可见的提示
}
```

### [internal/pipeline/stages.go:1447] 单事件内 GetSessionRules 重复查询 5+ 次，每次 2 条 SQL（N+1 式重复查询、无缓存） [严重程度：低]
- 严重程度：低（每条消息/每次 LLM 调用必发多次冗余 SQL；本地 SQLite 影响有限但属热路径重复 IO）
- 问题描述：`GetSessionRules`（internal/conversation/manager.go:728-753）每次调用执行 `ListPreferencesByScopeID("umo", ...)` 与 `ListPreferencesByScopeID("config_route", ...)` 两条查询并做 JSON 反序列化，而管线在同一个事件里重复调用它：
- stages.go:1281（filterHandlersBySession）、1447（provider 覆盖）、1485（persona 覆盖）、4180 与 4197（applyTTS 两次）、888（sttRecord）。

```go
// stages.go:1446-1448（同文件内多处同型调用）
if s.convMgr != nil {
    if rules := s.convMgr.GetSessionRules(event.UnifiedMsgOrigin()); rules != nil {
```
- 修复建议：在一次 Process 内提取/缓存 rules（事件级 memo），或在 conversation.Manager 内加短 TTL 缓存：

```go
// core.Event 增加 rules 缓存（或 ProcessStage 传递 *agentRequest 时顺带携带）：
rules := event.GetExtra("_session_rules")
if rules == nil {
    r := s.convMgr.GetSessionRules(umo)
    event.SetExtra("_session_rules", r)
    rules = r
}
```

### [internal/pipeline/provider_options.go:135] sanitizeContextByModalities 删除的 tool_calls 会被后续"从原 msg 重拷贝"恢复 [严重程度：低]
- 严重程度：低（仅当 provider 不支持 tool_use 且 assistant 消息 content 为数组且含 image/audio part 时触发；窄条件数据变形）
- 问题描述：第一个分支从 `msg` 拷贝并删除 `tool_calls`，但第二个分支在 `removed` 时再次"从原 msg 拷贝"重建 `m`，把刚删除的 `tool_calls` 原样带回：

```go
// provider_options.go:135-144
if role == "assistant" {
    if _, hasToolCalls := msg["tool_calls"]; hasToolCalls {
        m = make(map[string]interface{}, len(msg))
        for k, v := range msg { m[k] = v }
        delete(m, "tool_calls")
        delete(m, "tool_call_id")
    }
}
// provider_options.go:170-175
if removed {
    m = make(map[string]interface{}, len(msg))
    for k, v := range msg { m[k] = v }   // ← 从原始 msg 重拷贝，tool_calls 被恢复
    m["content"] = filtered
}
```
- 修复建议：第二处拷贝基于 `m`（已加工的副本）而非 `msg`：

```go
if removed {
    nm := make(map[string]interface{}, len(m))
    for k, v := range m { nm[k] = v }
    nm["content"] = filtered
    m = nm
}
```

### [internal/pipeline/stages.go:1507] persona 解析失败时回退读取 providerSettings["persona"]，把 persona ID 当系统提示词 [严重程度：低]
- 严重程度：低（persona 被删除/改名后触发：模型收到形如 "persona_1712345" 的垃圾系统提示词）
- 问题描述：

```go
// stages.go:1466-1471：providerSettings["persona"] 被写成 persona ID
conv := s.convMgr.GetOrCreateConversation(...)
if conv != nil {
    personaID = conv.Persona
    providerSettings["persona"] = conv.Persona
}
// stages.go:1502-1509：resolver 查不到该 ID 时回退读 settings["persona"] —— 得到的是 ID 本身
systemPrompt := ""
if s.personaPrompt != nil {
    systemPrompt = s.personaPrompt(event.UnifiedMsgOrigin(), personaID)
}
if systemPrompt == "" {
    systemPrompt = personaPrompt(providerSettings) // 返回 providerSettings["persona"].(string) = persona ID
}
```

personaResolver（lifecycle.go:786-798）对已删除的 personaID 返回 ""，随后 `personaPrompt(providerSettings)` 返回的正是刚写入的 ID 字符串，被当作 system prompt 发给模型。
- 修复建议：回退分支跳过 ID 语义的值，或在写入时区分两个键：

```go
if systemPrompt == "" {
    // 仅当 persona 配置是历史遗留的"提示词文本"形态时才回退；
    // conv.Persona 已在上方写入 settings["persona"]（ID 语义），不能再当提示词用。
    if p, ok := providerSettings["persona_prompt_text"].(string); ok {
        systemPrompt = p
    }
}
```

### [internal/pipeline/git_snapshot.go:108] 快照 patch 按字节截断 800，可能切断 UTF-8 多字节字符产生非法 UTF-8 [严重程度：低]
- 严重程度：低（含中文的 diff 长度超 800 字节时必然有概率触发；非法 UTF-8 进入日志、LLM 工具结果与平台消息）
- 问题描述：

```go
// git_snapshot.go:106-110
truncated := patch
const maxPatch = 800
if len(truncated) > maxPatch {
    truncated = truncated[:maxPatch] + "\n...(截断)"
}
```

`truncated[:maxPatch]` 是字节切分，中文（3 字节）被拦腰切断后产生非法 UTF-8 序列；该字符串随后进入 `logger.I18nInfo` 和返回给模型的工具结果（经 json.Marshal 会变 U+FFFD 乱码）。
- 修复建议：复用包内已有的 rune 安全截断（tool_result.go 的 `truncateRunes`）：

```go
truncated := truncateRunes(patch, maxPatch)
if len(patch) > len(truncated) {
    truncated += "\n...(截断)"
}
```

### [internal/pipeline/group_context.go:344] At 组件 Name 为空时群上下文记录渲染为 " @ @123456"（双 @） [严重程度：低]
- 严重程度：低（所有无昵称 At 的群消息触发；注入 LLM 的群聊上下文记录格式失真）
- 问题描述：

```go
// group_context.go:343-347
case *message.At:
    fmt.Fprintf(&b, " @%s", v.Name)
    if v.Name == "" {
        fmt.Fprintf(&b, " @%s", v.TargetID)
    }
```

`v.Name == ""` 时第一次 Fprintf 已写入 " @"，第二次再写 " @123" —— 生成 `" @ @123"`。与 extractPlainText（stages.go:4623-4628）的 if/else 写法不一致。
- 修复建议：

```go
case *message.At:
    if v.Name != "" {
        fmt.Fprintf(&b, " @%s", v.Name)
    } else {
        fmt.Fprintf(&b, " @%s", v.TargetID)
    }
```

### [internal/pipeline/message_tools.go:208] record/video/file 在 path/url 均为空时仍构造空组件并发送（image 分支则静默跳过，行为不一致） [严重程度：低]
- 严重程度：低（模型传了空 path/url 的 record/video/file 时触发：发送空媒体组件，平台侧报错或发出空消息）
- 问题描述：

```go
// message_tools.go:200-211（record 分支，video/file 同型）
case "record":
    if err := checkURL(); err != nil { return nil, err }
    p, err := resolvePath()
    if err != nil { return nil, err }
    if p == "" {
        p = path // path 为空时 p 仍为 ""
    }
    return &message.Record{Path: p, File: p, URL: url, Text: text}, nil
```

`path` 与 `url` 同时为空时返回 `&message.Record{}`（非 nil），被 `executeSendMessage` 追加进链并发送；而 image 分支在三者皆空时落到函数末尾返回 `nil, nil` 被跳过。
- 修复建议：

```go
case "record":
    ...
    if p == "" && url == "" {
        return nil, fmt.Errorf("record 组件需要 path 或 url 之一")
    }
```
（video/file 分支同样补齐；或统一在 executeSendMessage 里过滤 Path/URL 均空的媒体组件。）

### [internal/pipeline/computer_tools.go:899] shell 会话 write（原始文本）动作也强制追加换行，与 write_line 无差别 [严重程度：低]
- 严重程度：低（模型按 schema 语义用 write 写不含换行的原始文本时触发：多送一个 \n，破坏行式程序的输入协议）
- 问题描述：schema 描述明确区分两者（"write_line ... so the session receives a real line feed"，computer_tools.go:419-437），但实现里两者共用同一函数且无条件补 "\n"：

```go
// computer_tools.go:853-854
case "write", "write_line":
    return shellSessionWrite(sessionID, data, umo, senderID)
// computer_tools.go:899
if _, err := io.WriteString(s.Stdin, data+"\n"); err != nil {
```
- 修复建议：

```go
case "write":
    return shellSessionWrite(sessionID, data, umo, senderID, false)
case "write_line":
    return shellSessionWrite(sessionID, data, umo, senderID, true)

func shellSessionWrite(sessionID, data, umo, senderID string, addNewline bool) string {
    ...
    if addNewline {
        data += "\n"
    }
    if _, err := io.WriteString(s.Stdin, data); err != nil { ... }
```

### [internal/pipeline/websearch.go:267] web_search 工具的结果数量参数无上限钳制（tavily max_results / firecrawl limit / exa numResults） [严重程度：低]
- 严重程度：低（被提示注入或模型滥用时放大 API 账单/响应体；brave count 与 baidu top_k 已有上限，其余三家没有，标准不一致）
- 问题描述：

```go
// websearch.go:265-271（tavily：max_results 无钳制，schema 声称 "range 5-20" 但未强制）
payload := map[string]interface{}{
    "query":           query,
    "max_results":     argIntDefault(args, "max_results", 7),
    ...
}
// websearch.go:482-486（firecrawl limit）、websearch.go:661-665（exa numResults：仅有 >=1 下限）同样无上限。
```
- 修复建议：对齐 brave/baidu 的做法统一钳制：

```go
maxResults := argIntDefault(args, "max_results", 7)
if maxResults < 1 { maxResults = 1 }
if maxResults > 20 { maxResults = 20 } // firecrawl/exa 同理各设上限
```

---

# Part 02：Astrbot-golang/internal/platform 根 + sources 组A（aiocqhttp/qqofficial/dingtalk/satori/kook/qqofficial_webhook/discord/slack）

统计：发现 16 个问题（高 1 / 中 7 / 低 8）。

### [internal/platform/sources/qqofficial/adapter.go:970] 本地路径字符串直接作为 file_data 上传，语音/视频/文件发送必然失败 [严重程度：高]

- 严重程度：高（必然触发的功能 bug：只要通过 QQ 官方机器人发送语音/视频/文件——包括 TTS 生成的本地文件和经 materialize 物化的远程 URL——上传必然失败；同时把宿主机本地路径泄露给腾讯 API。qqofficial_webhook/adapter.go:761-763 是同一份拷贝，同样受影响）
- 问题描述：`extractSendParts` 对 File/Video/Record 组件优先取本地路径（`comp.Path`），而 `uploadFile` 只识别 http(s) 前缀，其余字符串原样塞进 `payload["file_data"]`：

```go
// qqofficial/adapter.go:864-873（File 分支，Video 874-882、Record 883-891 同理）
case *message.File:
    if fileRef == "" {
        if comp.Path != "" {
            fileRef = comp.Path     // ← 本地路径，例如 /tmp/astrbot-media-123.mp3
        } else {
            fileRef = comp.URL
        }
        fileName = comp.Name
        fileType = fileTypeFile
    }

// qqofficial/adapter.go:967-971
if strings.HasPrefix(fileData, "http://") || strings.HasPrefix(fileData, "https://") {
    payload["url"] = fileData
} else {
    payload["file_data"] = fileData   // ← 把 "/tmp/xxx.mp3" 路径字符串当 base64 数据发出
}
```

而发送入口 `PlatformManager.Send`（platform.go:680-682）会先调用 `materializeRemoteMedia` 把所有远程 URL 下载为临时文件并写入 `Path`，所以"带 Path 的本地媒体"正是最常见的发送形态。Python 原版 `upload_group_and_c2c_media`（qqofficial_message_event.py）会 `os.path.exists(file_source)` 后读文件内容 base64 编码（大文件走分片上传），Go 移植漏掉了这一步；同文件中 Image 分支用的 `readFileBase64`（897-903 行）是正确写法，但 File/Video/Record 没有复用。qqofficial_webhook/adapter.go:617-644（extractSendParts）与 759-763（uploadFile）是整段复制，问题相同。
- 修复建议：在 uploadFile 中识别本地文件并读取编码（或直接在 extractSendParts 中统一 base64 化）：

```go
// qqofficial/adapter.go uploadFile 内，替换 967-971 的分支
if strings.HasPrefix(fileData, "http://") || strings.HasPrefix(fileData, "https://") {
    payload["url"] = fileData
} else if data, err := os.ReadFile(fileData); err == nil {
    payload["file_data"] = base64.StdEncoding.EncodeToString(data)
} else {
    payload["file_data"] = fileData // 兼容调用方直接传 base64 的旧路径
}
```

qqofficial_webhook/adapter.go:759-763 做同样修改。

---

### [internal/platform/sources/satori/message.go:783] 发送链路二次下载媒体无任何 SSRF 主机校验，可完全旁路 materialize 的防线 [严重程度：中]

- 严重程度：中（条件触发：插件/转发/LLM 输出等把攻击者可控 URL 带入出站消息链时，satori 的 `fetchMedia` 与 discord 的 `fetchFile` 会对任意地址发起 GET，包括被 `mediaHostAllowed` 明确拒绝的 169.254.169.254 云元数据地址——materialize 拒绝下载时 URL 原样保留，恰好落到这条无防护路径上）
- 问题描述：internal/platform/media_materialize.go:93-95 对链内 URL 做了链路本地/组播地址拒绝（防 SSRF 打云元数据），下载失败或被拒时"组件原样保留"。但 satori 发送时对带 URL 的图片/语音再次自行下载，且没有任何主机校验：

```go
// satori/message.go:783-791
func fetchMedia(rawURL string) ([]byte, string, error) {
    resp, err := mediaHTTPClient.Get(rawURL)   // ← 无 mediaHostAllowed 检查、无重定向校验
    ...
}

// satori/message.go:832-838（imageToDataURL 的 URL 分支）
case img.URL != "":
    raw, path, err := fetchMedia(img.URL)
```

discord/adapter.go:617-635 的 `fetchFile` 同样直接 `http.DefaultClient.Do(req)` 下载出站链中的任意 URL（仅限了大小和超时）。这两条路径使 media_materialize.go 精心构建的地址黑名单在 satori/discord 上形同虚设：materialize 因地址被拒而保留 URL → 组件仍有 URL 且无 Path → 适配器自己再下载同一个被拒地址，结果以 base64 形式发往外部平台（可回传给攻击者）。
- 修复建议：复用 platform 包的主机校验逻辑（将 `mediaHostAllowed` 导出或在各适配器内实现同样的检查），并禁止跨主机重定向：

```go
var mediaHTTPClient = &http.Client{
    Timeout: 30 * time.Second,
    CheckRedirect: func(req *http.Request, via []*http.Request) error {
        if len(via) >= 3 {
            return fmt.Errorf("too many redirects")
        }
        return nil // 每一跳仍需通过下面的主机校验
    },
}

func fetchMedia(rawURL string) ([]byte, string, error) {
    if !platform.MediaHostAllowed(rawURL) { // 导出 media_materialize.go 的校验
        return nil, "", fmt.Errorf("媒体地址被拒绝: %s", rawURL)
    }
    ...
}
```

discord/adapter.go:617 的 fetchFile 同样接入该校验。

---

### [internal/platform/sources/aiocqhttp/quoted_parser.go:285] max_forward_fetch=0 语义与 Python 相反：配置 0 变成"无限制抓取" [严重程度：中]

- 严重程度：中（条件触发：用户按 Python 语义把 `quoted_message_parser.max_forward_fetch` 配成 0（意为关闭转发抓取）时，Go 版反而完全不设上限；合并转发消息内的 forward id 由消息发送方控制，可驱动无限 BFS，每次 get_forward_msg 最多阻塞 10 秒且运行在事件处理 goroutine 上）
- 问题描述：Python 原版 extractor.py:36 的循环条件是 `while pending and fetch_count < max_fetch:`（0 = 一次也不抓），而 Go 版把 `maxFetch <= 0` 当成"无限制"：

```go
// quoted_parser.go:41-46 —— 0 是合法配置值
if v, ok := raw["max_forward_fetch"].(int); ok && v >= 0 {
    s.maxForwardFetch = v
}

// quoted_parser.go:285
for len(pending) > 0 && (maxFetch <= 0 || fetchCount < maxFetch) {
```

由于 resolveQuotedParserSettings 只接受 `v >= 0`，`maxFetch <= 0` 实际只可能是用户显式配置的 0——本想"关闭"却得到"不限量"。
- 修复建议：与 Python 对齐，0 表示不抓取：

```go
for len(pending) > 0 && fetchCount < maxFetch {
    ...
}
if len(pending) > 0 {
    logger.Warn("quoted_message_parser: 在 %d 跳后停止获取嵌套转发消息", maxFetch)
}
```

---

### [internal/platform/sources/kook/roles_record.go:202] 重试间隔比较单位错误：60 纳秒被当作 60 秒，失败退避完全失效 [严重程度：中]

- 严重程度：中（条件触发：/user/view 接口连续失败超过 3 次后，本应 60 秒内放弃重试，实际窗口是 60ns 恒为过期 → 每条带角色 @ 的消息都会重新发起一次最长 3 秒的 HTTP 请求；该调用位于 WS 读循环的同步路径（convertTextToComponents），既阻塞消息接收又持续打 KOOK API 触发限流）
- 问题描述：常量是纯数字，在与 `time.Duration` 比较时被隐式转换为 60 纳秒。Python 原版（kook_roles_record.py:129-131）是 `time.time() - latest_update_time < self._retry_interval`，单位为秒：

```go
// roles_record.go:17
retryIntervalSecond    = 60               // 失败后的重试间隔(秒)

// roles_record.go:201-205
if cache, ok := r.cache[guildID]; ok {
    // 失败次数过多且在重试间隔内, 直接放弃本次查询
    if cache.FailedCount > maxRetryTimes && time.Since(cache.LatestUpdateTime) < retryIntervalSecond {
        //                                                                    ↑ 60ns，永远为 false
```
- 修复建议：

```go
const retryInterval = 60 * time.Second

if cache.FailedCount > maxRetryTimes && time.Since(cache.LatestUpdateTime) < retryInterval {
    r.mu.Unlock()
    return nil
}
```

---

### [internal/platform/sources/kook/client.go:279] 重连未取消旧心跳 goroutine：可出现双心跳并发写同一 WebSocket 连接 [严重程度：中]

- 严重程度：中（条件触发：旧心跳 goroutine 的定时器在新连接置 `running=true` 之后才醒来时，新旧两个心跳循环同时存活并都向新连接 `WriteMessage`——gorilla/websocket 要求单写者，并发写会破坏帧甚至 panic；且旧循环的 ctx 永远不会被取消。断线重连频繁的部署长期运行下命中概率可观）
- 问题描述：每次 `Connect` 直接覆盖 `c.heartbeatCancel`，既不取消上一个心跳，新 ctx 又基于 `context.Background()`（Stop 只能取消最后注册的那一个）：

```go
// kook/client.go:278-281
// 启动心跳任务
hbCtx, cancel := context.WithCancel(context.Background())
c.heartbeatCancel = cancel       // ← 直接覆盖，旧 cancel 丢失、旧 goroutine 未终止
go c.heartbeatLoop(hbCtx)

// kook/client.go:427-443（heartbeatLoop 的存活条件是 IsRunning()，重连成功后旧循环也会"复活"）
func (c *KookClient) heartbeatLoop(ctx context.Context) {
    for c.IsRunning() {
        ...
        c.sendPing()  // 两个循环都会走到 conn.WriteMessage
```

时序：连接 1 断开 → listen 返回并 `setRunning(false)` → mainLoop 立即 Connect 连接 2 并 `setRunning(true)`；此刻旧心跳若正阻塞在 `time.After` 上，醒来后 `IsRunning()` 已重新为 true，继续向连接 2 发心跳，与新启动的心跳循环形成并发写。
- 修复建议：在锁内先取消旧心跳，并让 heartbeat 的生命周期跟随单次连接：

```go
c.heartbeatMu.Lock()
if c.heartbeatCancel != nil {
    c.heartbeatCancel()
}
hbCtx, cancel := context.WithCancel(context.Background())
c.heartbeatCancel = cancel
c.heartbeatMu.Unlock()
go c.heartbeatLoop(hbCtx)
```

同时在 listen 退出（defer）时也调用一次取消，保证心跳与连接同生共死。

---

### [internal/platform/sources/dingtalk/adapter.go:500] 收到的图片/语音/文件下载到 /tmp 后永不清理，磁盘无限累积 [严重程度：中]

- 严重程度：中（长期运行必然触发：钉钉每收到一条图片/语音/文件消息就往 os.TempDir() 写一个最多 50MB 的 `dingtalk_<ns>.<ext>` 文件（utils.DownloadFile 的 maxDownloadBytes 上限），全文没有任何删除路径；kook/adapter.go:596-601 的音频下载 `utils.TempFilePath(".wav")` 同样无人清理。platform.go:340-360 专门定义了 `TrackTemporaryFile/CleanupTemporaryFiles` 临时文件机制，但全仓库无一处调用，属于机制建好未接线）
- 问题描述：

```go
// dingtalk/adapter.go:500-505 —— 文件写入 /tmp 后作为组件字段发布，之后再无人负责删除
fPath := filepath.Join(os.TempDir(), fmt.Sprintf("dingtalk_%d.%s", time.Now().UnixNano(), ext))
if err := utils.DownloadFile(a.dingtalkCtx(), downloadURL, fPath); err != nil {
    logger.I18nError("下载钉钉文件失败: %v", err)
    return ""
}
return fPath
```

对照发送侧（adapter.go:774-789、810-835）对转码产物都有 `safeRemoveFile` 清理，接收侧完全缺失。kook/adapter.go:596-601 同病：

```go
path := utils.TempFilePath(".wav")
if err := utils.DownloadFile(a.kookCtx(), f.src, path); err != nil { ... }
msgComps = append(msgComps, &message.Record{File: path, URL: path})
```
- 修复建议：在消息发布后（或事件处理完成的钩子处）按组件回收临时文件，例如在 handleMsg 发布事件前登记、随后交由统一清理：

```go
// dingtalk: convertMsg 返回前收集本条消息产生的临时文件，事件发布后统一删除
func (a *Adapter) handleMsg(abm *platform.AstrBotMessage) {
    ... // 现有发布逻辑
    for _, comp := range abm.Message {
        switch c := comp.(type) {
        case *message.Image:
            removeDingtalkTemp(c.Path)
        case *message.Record:
            removeDingtalkTemp(c.File)
        case *message.File:
            removeDingtalkTemp(c.Path)
        }
    }
}

func removeDingtalkTemp(p string) {
    if p != "" && strings.HasPrefix(filepath.Base(p), "dingtalk_") {
        _ = os.Remove(p)
    }
}
```

（若下游插件需要这些文件，则应接入事件级 TrackTemporaryFile 机制并在管线末端清理。）

---

### [internal/platform/sources/satori/message.go:488] skipElement 不按深度跳过子树，非空 at/quote 元素会截断后续消息内容 [严重程度：中]

- 严重程度：中（条件触发：被跳过的元素（at/img/quote 等）带有子元素时——典型如引用消息中嵌有 `<img/>` 的 `<quote>`（message.go:193-195 的注释明确记录了 quote 会保留在内容里"被解析时跳过"的场景）——skipElement 在第一个嵌套 EndElement 就返回，外层 EndElement 被当作根结束，该元素之后的全部消息内容静默丢失）
- 问题描述：

```go
// satori/message.go:487-498
func skipElement(d *xml.Decoder) error {
    for {
        tok, err := d.Token()
        if err != nil {
            return err
        }
        if _, ok := tok.(xml.EndElement); ok {
            return nil   // ← 遇到的是【子元素】的 EndElement 就提前返回了
        }
    }
}
```

对 `<quote id="x">看这张图<img src="a"/>哈哈</quote>后面的内容`：skipElement 在 `</img>` 处返回，`</quote>` 随后被 walkSatoriChildren 当作根元素结束，"后面的内容"全部丢弃。自闭合标签（`<at/>`）恰好不受影响，所以常规消息掩盖了该缺陷。
- 修复建议：带深度计数地跳过完整子树：

```go
func skipElement(d *xml.Decoder) error {
    depth := 0
    for {
        tok, err := d.Token()
        if err != nil {
            return err
        }
        switch tok.(type) {
        case xml.StartElement:
            depth++
        case xml.EndElement:
            if depth == 0 {
                return nil
            }
            depth--
        }
    }
}
```

---

### [internal/platform/media_materialize.go:122] SSRF 主机校验存在 TOCTOU 与重定向绕过 [严重程度：中]

- 严重程度：中（条件触发：攻击者可控 DNS（重绑定：校验时解析到公网 IP、真正连接时解析到 169.254.169.254）或让被允许的主机 302 跳转到链路本地地址——`client.Get` 默认跟随最多 10 次重定向且每一跳不重新校验。校验与取数用两次独立解析/连接，防线可被完全绕过）
- 问题描述：

```go
// media_materialize.go:122-141 —— 校验时自行 LookupIP
func mediaHostAllowed(rawURL string) bool {
    ...
    addrs, err := net.LookupIP(host)   // 第一次解析
    ...

// media_materialize.go:154-156 —— 下载时 http.Client 再次解析，且跟随重定向不校验
func downloadMediaToTemp(rawURL string) (string, error) {
    client := &http.Client{Timeout: mediaDownloadTimeout}
    resp, err := client.Get(rawURL)    // 第二次解析 + 302 跟随
```
- 修复建议：把校验下沉到拨号时刻（Control 回调拿到的是实际即将连接的 IP），并在 CheckRedirect 里逐跳复检：

```go
func mediaDownloadClient() *http.Client {
    dialer := &net.Dialer{
        Control: func(network, address string, _ syscall.RawConn) error {
            host, _, _ := net.SplitHostPort(address)
            if ip := net.ParseIP(host); ip != nil && !ipAllowed(ip) {
                return fmt.Errorf("blocked address: %s", host)
            }
            return nil
        },
    }
    return &http.Client{
        Timeout: mediaDownloadTimeout,
        Transport: &http.Transport{DialContext: dialer.DialContext},
        CheckRedirect: func(req *http.Request, via []*http.Request) error {
            if !mediaHostAllowed(req.URL.String()) {
                return http.ErrUseLastResponse
            }
            if len(via) >= 3 {
                return fmt.Errorf("too many redirects")
            }
            return nil
        },
    }
}
```

---

### [internal/platform/sources/kook/client.go:341] KOOK/DingTalk WebSocket 未设读上限，KOOK zlib 解压无上限（解压炸弹） [严重程度：低]

- 严重程度：低（对端为官方网关（带 token 鉴权、TLS），攻击前提是对端被控制或 MITM；但 aiocqhttp/adapter.go:587、qqofficial/adapter.go:312、satori/adapter.go:253 都设置了 `SetReadLimit`，kook 与 dingtalk 漏配，属于一致性缺口，异常大帧可直接放大内存占用）
- 问题描述：

```go
// kook/client.go:266-274 —— Dial 后无 SetReadLimit
conn, _, err := websocket.DefaultDialer.DialContext(ctx, gatewayURL, nil)
...

// kook/client.go:341-348 —— 解压无上限
func zlibUncompress(data []byte) ([]byte, error) {
    r, err := zlib.NewReader(bytes.NewReader(data))
    ...
    return io.ReadAll(r)   // ← 几 KB 压缩帧可解出任意大小
}
```

dingtalk/stream.go:132-153 的连接同样未调用 SetReadLimit。
- 修复建议：

```go
conn.SetReadLimit(1 << 20) // 与其他适配器对齐

func zlibUncompress(data []byte) ([]byte, error) {
    r, err := zlib.NewReader(bytes.NewReader(data))
    if err != nil {
        return nil, err
    }
    defer r.Close()
    return io.ReadAll(io.LimitReader(r, 10<<20)) // 与 maxWSMessageSize 对齐
}
```

---

### [internal/platform/sources/kook/adapter.go:744] Send 聚合所有发送错误后只打日志，恒返回 nil [严重程度：低]

- 严重程度：低（错误吞掉：消息全部发送失败时上游（管线重试、WebUI 状态、插件判断）仍认为成功；且 731 行把 `err.Error()`（含内部 URL/路径）当作聊天消息发回给用户）
- 问题描述：

```go
// kook/adapter.go:720-744
var errors []error
for _, item := range orders {
    ...
    errors = append(errors, err)
    ...
}
if len(errors) > 0 {
    ...
    logger.I18nError("[KOOK] 发送消息时出现 %d 个错误: %s", len(errors), strings.Join(msgs, "\n"))
}
return nil   // ← 永远成功
```
- 修复建议：

```go
if len(errors) > 0 {
    return fmt.Errorf("kook: %d/%d 条消息发送失败: %w", len(errors), len(orders), errors[0])
}
return nil
```

（发送 err.Error() 到聊天的行为建议同步去掉或脱敏。）

---

### [internal/platform/sources/aiocqhttp/adapter.go:625] events 通道（容量 64）写满后阻塞 WS 读循环，echo 应答停摆引发全连接 RPC 超时 [严重程度：低]

- 严重程度：低（条件触发：handleEvent 单线程消费（get_msg/get_forward_msg 每个最多 10s，enrichFileURLs 每个文件再加 10s），事件积压超过 64 条后读循环阻塞在 `events <- msg`，无法再读取 send_msg 的 echo 帧 → 期间所有 CallAction/sendAction 观察者集体超时；10s 超时后可自行恢复）
- 问题描述：

```go
// aiocqhttp/adapter.go:598-604
events := make(chan map[string]interface{}, 64)
defer close(events)
go func() {
    for ev := range events {
        a.handleEvent(ev)   // 串行，单条可达数十秒
    }
}()

// aiocqhttp/adapter.go:624-626
if _, hasPost := msg["post_type"]; hasPost {
    events <- msg          // ← 无 default 分支，满时阻塞读循环
    continue
}
```

注释声称该设计是"避免慢 handleEvent 阻塞读循环"，但容量有限 + 阻塞发送并未完全达成该目标。对照 dingtalk/stream.go:271-275 用 `select/default` 丢弃并告警的做法。
- 修复建议：

```go
select {
case events <- msg:
default:
    logger.I18nWarn("aiocqhttp: 事件处理队列已满，丢弃一条事件（echo 通道不受影响）")
}
```

或改为无界队列/多 worker，并保留 echo 帧的独立读取路径。

---

### [internal/platform/sources/discord/adapter.go:605] openFile 打开的 *os.File 从不关闭（discordgo 不会代为关闭） [严重程度：低]

- 严重程度：低（资源泄漏：每次以本地路径发送图片/语音/文件泄漏一个 fd，仅靠 runtime finalizer 兜底；已核实 discordgo v0.25.1 util.go 的 MultipartBodyWithJSON 只对 Reader 做 io.Copy，不 Close）
- 问题描述：

```go
// discord/adapter.go:605-612
func openFile(path, name string) *discordgo.File {
    f, err := os.Open(path)
    if err != nil {
        logger.I18nWarn("打开文件失败: %v", err)
        return nil
    }
    return &discordgo.File{Name: mediaFilename(name, filepath.Ext(path)), Reader: f} // ← f 无人关闭
}
```
- 修复建议：发送完成后统一关闭（Send 中发送路径结束后遍历 files 关闭实现了 io.Closer 的 Reader），或改用 ReadCloser 包装：

```go
// discord/adapter.go Send 末尾
defer func() {
    for _, f := range files {
        if rc, ok := f.Reader.(io.Closer); ok {
            _ = rc.Close()
        }
    }
}()
_, err := a.session.ChannelMessageSendComplex(channelID, data)
return err
```

---

### [internal/platform/sources/slack/client.go:205] 事件处理 goroutine 逃出 recover 保护，处理链 panic 将击穿整个进程 [严重程度：低]

- 严重程度：低（webhook 入口专门写了 recover 兜底，但事件在 `go s.eventHandler(eventData)` 中异步执行，panic 发生在 recover 覆盖范围之外，任何转换/下载代码的 nil 解引用都会使宿主进程整体崩溃）
- 问题描述：

```go
// slack/client.go:136-144 —— recover 只保护当前 goroutine
defer func() {
    if rec := recover(); rec != nil {
        webhookServerLogger.I18nError("处理 Slack 事件时出错: %v", rec)
        ...
    }
}()

// slack/client.go:203-206 —— 异步 goroutine 中的 panic 不在上述 recover 范围内
w.WriteHeader(http.StatusOK)
go s.eventHandler(eventData)
```
- 修复建议：

```go
go func() {
    defer func() {
        if rec := recover(); rec != nil {
            webhookServerLogger.I18nError("处理 Slack 事件时出错: %v", rec)
        }
    }()
    s.eventHandler(eventData)
}()
```

---

### [internal/platform/sources/satori/adapter.go:299] sendIdentify 无锁读取 sequence，与 updateSequence 构成数据竞态 [严重程度：低]

- 严重程度：低（int64 在 64 位平台上的撕裂风险低，但属未同步读写，race detector 必报；sequence 由读循环 goroutine 经 handleSignal/updateSequence 在锁内写入）
- 问题描述：

```go
// satori/adapter.go:298-301（connectWebsocket 的 goroutine 中，无锁）
    // 只有在有序列号时才添加 sn 字段
    if a.sequence > 0 {
        body["sn"] = a.sequence
    }

// satori/adapter.go:397-403（另一 goroutine，持锁写）
func (a *Adapter) updateSequence(body map[string]interface{}) {
    if sn, ok := body["sn"].(float64); ok && sn != 0 {
        a.mu.Lock()
        a.sequence = int64(sn)
        a.mu.Unlock()
    }
}
```
- 修复建议：

```go
a.mu.Lock()
sn := a.sequence
a.mu.Unlock()
if sn > 0 {
    body["sn"] = sn
}
```

---

### [internal/platform/media_materialize.go:49] 就地变异共享消息链组件：二次发送会命中已删除的临时路径，且对 URL 优先适配器做无效下载 [严重程度：低]

- 严重程度：低（条件触发：其一，cleanup 删除临时文件但 `c.Path/c.File` 仍指向它，若同一 MessageChain 实例被再次 Send（插件缓存重发、重试、广播），第二次 `isLocalMediaPath(path)` 命中死路径，qqofficial 的 readFileBase64 读不到文件且不再回退 URL；其二，aiocqhttp/satori(video) 等优先使用 URL 的适配器照样先付出一次最长 30 秒、最大 50MB 的本地下载，随后丢弃，徒增发送延迟）
- 问题描述：

```go
// media_materialize.go:48-63 —— 永久变异组件状态
case *message.Image:
    if p := materializeMediaURL(c.URL, c.Path, c.File, &temps); p != "" {
        c.Path, c.File = p, p   // ← Send 返回后被 cleanup 删除，但组件仍持有该路径
    }

// media_materialize.go:76-81 —— 清理只删文件不还原字段
return func() {
    for _, p := range temps {
        _ = os.Remove(p)
    }
}

// media_materialize.go:86-89 —— 二次发送时因 Path 非空而跳过物化，直接使用死路径
if isLocalMediaPath(path) || isLocalMediaPath(file) {
    return "" // 已有本地路径，无需下载
}
```
- 修复建议：cleanup 时还原组件原值（记录旧值），并考虑仅对"需要本地路径"的适配器物化：

```go
type mediaUndo struct {
    setPath func(string)
    old     string
}
// walk 时记录 (component setter, 原值)，cleanup：
return func() {
    for _, u := range undos {
        _ = os.Remove(u.newPath)
        u.set(u.old)
    }
}
```

---

### [internal/platform/sources/slack/message.go:114] File 分支优先远程 URL 导致 os.Stat(URL) 必然失败，文件发送恒返回"文件上传失败" [严重程度：低]

- 严重程度：低（与 Python 原版行为一致的缺陷，但 Go 侧 materialize 已把文件字节下载到 Path，却因优先级先取 URL 而未利用：Slack 会话中转发/重发任何带远程 URL 的 File 组件（包括收到的 url_private 附件）都会失败；仅纯本地路径（如 TTS 产物）可用）
- 问题描述：

```go
// slack/message.go:105-119
case *message.File:
    url := c.URL              // ← 远程 URL 优先；materialize 后 Path 里已有下载好的字节
    if url == "" {
        url = c.Path
    }
    ...
    permalink, ok := uploadAndGetPermalink(ctx, client, url, name)

// slack/message.go:169-173 —— uploadFile 对传入值做 os.Stat
info, err := os.Stat(path)    // os.Stat("https://...") 必然失败
if err != nil || info.IsDir() {
    logger.I18nWarn("Slack 上传文件不存在: %s", path)
    return "", false
}
```
- 修复建议：本地路径优先，URL 仅在没有本地文件时使用（或先经受控下载）：

```go
case *message.File:
    path := c.Path
    if path == "" {
        path = c.File
    }
    if path == "" || !fileExists(path) {
        return blockSection("文件上传失败（无本地文件）")
    }
    permalink, ok := uploadAndGetPermalink(ctx, client, path, name)
```

---

# Part 03：Astrbot-golang/internal/platform/sources 组B（wecom_ai_bot / wecom / weixin_official_account / weixin_oc / telegram / misskey / line / mattermost / lark / webchat + 根 init.go）

统计：发现 14 个问题（高 1 / 中 6 / 低 7）。

### [internal/platform/sources/telegram/adapter.go:372] 接收链路只处理 text/voice/audio，photo/sticker/document/video 消息（含 caption）被静默丢弃 [严重程度：高]

- 严重程度：高（必然触发的功能 bug：任何用户给机器人发图片、视频、文件、贴纸，消息内容全部丢失，机器人无响应；Python 原版完整支持这些类型的接收）
- 问题描述：`handleUpdate` 只从 update 里取 `msg["text"]`、`msg["voice"]`、`msg["audio"]` 三个字段构造消息链，其余媒体类型既不下载也不产生占位组件：

```go
// telegram/adapter.go:402-432（接收消息构造的全部逻辑）
// Extract text
text, _ := msg["text"].(string)
...
chain := &message.MessageChain{Chain: []message.Component{}}
if text != "" {
    chain.Chain = append(chain.Chain, &message.Plain{Text: text})
}

// 语音/音频消息：下载文件并按内容识别真实格式，转换为 Record 组件。
if _, ok := msg["voice"]; ok {
    ...
} else if _, ok := msg["audio"]; ok {
    ...
    if caption, ok := msg["caption"].(string); ok && caption != "" {
        chain.Chain = append(chain.Chain, &message.Plain{Text: caption})
    }
}
```

photo 消息进来时 `text == ""`、无 voice/audio，事件以空链发布，管线侧无从处理。而发送侧（`Send`，第 127/140/144 行）却实现了 sendPhoto/sendDocument/sendVideo，收发能力明显不对称。Python 原版 `tg_adapter.py` 明确处理了全部媒体：`elif update.message.photo:`（610-614 行，取最大尺寸下载）、`elif update.message.sticker:`（616-631 行，动图贴纸回退缩略图并附 emoji 文本）、`elif update.message.document:`（633-645 行）、`elif update.message.video:`（647 行起），且都调用 `_apply_caption()`。`grep` 确认 Go 文件中 `photo/document/video` 仅出现在发送路径。
- 修复建议：在 handleUpdate 中补齐媒体分支（复用已有的 `handleAudioMessage` 的 getFile+下载骨架）：

```go
// telegram/adapter.go handleUpdate 内，voice/audio 之后追加：
switch {
case msg["photo"] != nil:
    // photo 是数组，取最后一个（最大尺寸）
    if sizes, ok := msg["photo"].([]interface{}); ok && len(sizes) > 0 {
        if s, ok := sizes[len(sizes)-1].(map[string]interface{}); ok {
            if fileID, _ := s["file_id"].(string); fileID != "" {
                if img := a.handleMediaFile(ctx, fileID, "photo"); img != nil {
                    chain.Chain = append(chain.Chain, img)
                }
            }
        }
    }
    applyCaption(chain, msg) // caption 追加为 Plain
case msg["document"] != nil, msg["video"] != nil, msg["sticker"] != nil:
    // 同样经 getFile 下载到临时文件，构造 File/Video/Image 组件
    // （sticker 的 is_animated/is_video 贴纸按 Python 逻辑取 thumbnail）
}
```

其中 `handleMediaFile` 与现有 `handleAudioMessage`（461-531 行）一样走 `getFile` + `utils.DownloadFile(a.fileBase+"/"+filePath, tmp)` + `scheduleAudioCleanup` 延迟清理。

---

### [internal/platform/sources/wecom/adapter.go:300] kf/sync_msg 游标未推进时"终止同步"守卫失效，has_more=1 将导致无限循环重复拉取 [严重程度：中]

- 严重程度：中（条件触发：企业微信返回 `has_more=1` 但 `next_cursor` 缺失/为空/与上次相同（如服务端异常或被截断）时，`handleKFMsgOrEvent` 无限循环：每轮都用相同 cursor 重新拉取同一批消息并重复发布事件——文本消息有 15 秒去重兜底，图片/语音/文件消息会被反复下载、反复写入临时文件并反复触发机器人回复；同时无限调用 kf/sync_msg API）
- 问题描述：循环里先用"游标是否推进"设置 `hasMore=false` 打算终止，但紧接着又被响应里的 `has_more` 字段无条件覆盖，守卫形同虚设：

```go
// wecom/adapter.go:294-317
for hasMore {
    ret, err := a.client.KFSyncMsg(ctx, msg.Token, msg.OpenKfID, cursor, 1000)
    ...
    if nc, ok := ret["next_cursor"].(string); ok && nc != "" && nc != cursor {
        cursor = nc
    } else if hasMore {
        logger.I18nWarn("kf/sync_msg has_more=1 但游标未推进，终止同步")
        hasMore = false
    }
    if hm, ok := ret["has_more"].(float64); ok {
        hasMore = int(hm) != 0   // ← 把上面设置的 false 又改回 true，"终止同步"从未生效
    } else {
        hasMore = false
    }
    ...
}
```

日志文案声称"终止同步"但循环实际继续，属于典型的分支顺序错误。
- 修复建议：让 has_more 只能在"游标已推进"的前提下延续循环：

```go
cursorAdvanced := false
if nc, ok := ret["next_cursor"].(string); ok && nc != "" && nc != cursor {
    cursor = nc
    cursorAdvanced = true
}
if hm, ok := ret["has_more"].(float64); ok {
    hasMore = int(hm) != 0 && cursorAdvanced
} else {
    hasMore = false
}
if int(hm) == 1 && !cursorAdvanced { // hm 需提出到 if 外
    logger.I18nWarn("kf/sync_msg has_more=1 但游标未推进，终止同步")
}
```

---

### [internal/platform/sources/wecom_ai_bot/long_connection.go:93] 长连接重连退避永不复位，数次断线后重连延迟固定为 30 秒 [严重程度：中]

- 严重程度：中（条件触发的可用性 bug：每经历一次连接断开（哪怕上一次连接稳定运行数小时），`reconnectDelay` 就翻倍一次且永不回落；约 5 次断连后，之后每次掉线都要先等 30 秒才重连，掉线期间的智能机器人消息全部丢失）
- 问题描述：Go 版 `Start` 的退避在每一轮循环结束时无条件翻倍，包括 `runOnce` 正常运行很久后返回的情况；Python 原版在 `_run_once` 成功返回后立即把 `reconnect_delay` 复位为 1：

```go
// wecom_ai_bot/long_connection.go:74-97（Go，无复位）
reconnectDelay := time.Second
for {
    ...
    err := c.runOnce(ctx)          // 可能已稳定连接数小时后正常退出
    ...
    case <-time.After(reconnectDelay):
    }
    reconnectDelay *= 2            // ← 无论成败都翻倍，永不复位
    if reconnectDelay > 30*time.Second {
        reconnectDelay = 30 * time.Second
    }
}
```

```python
# wecomai_long_connection.py:44-56（Python 原版，成功即复位）
try:
    await self._run_once()
    reconnect_delay = 1
```
- 修复建议：对齐 Python，连接成功运行过即复位：

```go
err := c.runOnce(ctx)
if err != nil {
    logger.I18nError("[WecomAI][LongConn] 长连接异常: %v", err)
} else {
    reconnectDelay = time.Second // 正常退出（曾成功建立）后复位退避，对齐 Python
}
```

---

### [internal/platform/sources/weixin_oc/adapter.go:356] 发送媒体组件时把已累积的 pendingText 静默丢弃，文本+媒体混合消息丢失文本 [严重程度：中]

- 严重程度：中（必然触发的消息丢失：只要回复链是 Plain+媒体 的顺序（如 LLM 先答一句再发图，或插件拼接"说明文字+图片"），媒体之前的所有文本都不会被发送）
- 问题描述：`Send` 把 Plain 文本累积到 `pendingText`，但在发送 Image/File/Video/Record 时直接把 `pendingText` 清空而不发送：

```go
// weixin_oc/adapter.go:350-376
var pendingText string
for _, comp := range chain.Chain {
    switch c := comp.(type) {
    case *message.Plain:
        pendingText += c.Text
    case *message.Image:
        if err := a.sendImage(ctx, sessionID, c); err != nil {
            return err
        }
        pendingText = ""        // ← "hello" 没发出去就被丢了
    ...
```

Python 原版把待发文本作为参数随媒体段一起发送：`sent = await self._send_media_segment(target_user, segment, text=pending_text.strip() or None)`（weixin_oc_adapter.py:1656-1662）。
- 修复建议：发送媒体前先冲刷文本（或仿 Python 把 text 传给媒体发送函数）：

```go
case *message.Image:
    if strings.TrimSpace(pendingText) != "" {
        if err := a.bot.SendText(ctx, sessionID, pendingText); err != nil {
            return err
        }
    }
    if err := a.sendImage(ctx, sessionID, c); err != nil {
        return err
    }
    pendingText = ""
```

其余 File/Video/Record 分支同样处理。

---

### [internal/platform/sources/lark/message.go:318] sendMessageChain 吞掉全部发送错误（Send 永远返回成功），且文件/音频发送不支持仅 URL 的组件 [严重程度：中]

- 严重程度：中（条件触发：任一子消息（富文本/文件/音频/视频）上传或发送失败时只打 Warn 日志，`Send` 依然返回 nil，管线认为发送成功，用户收不到任何内容且无重试/降级机会；另外 File/Record 组件只取 Path/URL 做本地 `os.Open`，仅有 URL（如 materialize 被 SSRF 防线拒绝后 URL 原样保留的场景，见 Part 02）时必然 `os.Open("https://...")` 失败，Image 仅 URL 时被静默 continue 丢弃）
- 问题描述：四个发送阶段的错误全部只记日志，函数结尾无条件 `return nil`：

```go
// lark/message.go:318-338
if len(otherComps) > 0 {
    if err := sendRichText(ctx, client, otherComps, replyMessageID, receiveID, receiveIDType); err != nil {
        logger.I18nWarn("发送飞书富文本消息失败: %v", err)   // ← 吞掉
    }
}
for _, f := range fileComps {
    if err := sendFileMessage(...); err != nil {
        logger.I18nWarn("发送飞书文件失败: %v", err)          // ← 吞掉
    }
}
... // audio/video 同样
return nil                                                    // ← 永远成功
```

同时 `sendFileMessage`/`sendAudioMessage` 把 URL 当本地路径打开：

```go
// lark/message.go:500-507
path := comp.Path
if path == "" {
    path = comp.URL          // ← https://... 传给下面的 os.Open
}
...
key, err := uploadFile(ctx, client, path, "stream")  // uploadFile 内 os.Open(path) 必然失败
```

以及 `convertToLark` 对仅 URL 的图片直接跳过（376-387 行 `path == "" → continue`）。
- 修复建议：向上传播错误并对 URL 组件补下载：

```go
var errs []error
if err := sendRichText(...); err != nil {
    logger.I18nWarn("发送飞书富文本消息失败: %v", err)
    errs = append(errs, err)
}
... // 其余三段同样收集
return errors.Join(errs...)
```

`sendFileMessage`/`sendAudioMessage`/`sendMediaMessage` 在 path 为空且 URL 为 http(s) 时先下载到临时文件（复用 `writeTempBase64` 同款的 CreateTemp + http 下载 + 发送后 `os.Remove`），`convertToLark` 对 Image 的 URL 同样处理。

---

### [internal/platform/sources/wecom_ai_bot/webhook.go:355] 发送链路二次下载媒体无任何 SSRF 主机校验（Part 02 已知问题的本组实例，共 4 处） [严重程度：中]

- 严重程度：中（条件触发：插件/转发/LLM 输出把攻击者可控 URL 带入出站消息链，且该 URL 因 materialize 拒绝/失败而原样保留时，四个适配器都会对任意地址发起 GET，包括 169.254.169.254 云元数据、内网管理面板——完全旁路 internal/platform/media_materialize.go 的 mediaHostAllowed 防线。与 Part 02 报告的 satori fetchMedia / discord fetchFile 同构）
- 问题描述：本组四个适配器的发送侧各有一套"URL→下载"逻辑，全部无主机校验，其中三处还是无超时的 `http.DefaultClient`：

```go
// wecom_ai_bot/webhook.go:355-363（componentImageBase64）与 388-395（componentFilePath）
resp, err := http.DefaultClient.Do(req)          // 无 SSRF 校验、无大小限制外的并发/重定向约束

// wecom/send.go:188-206（downloadToFile，被 resolveComponentFile 的 URL 分支调用）
resp, err := http.DefaultClient.Do(req)

// weixin_oc/adapter.go:309-311（downloadMedia）
resp, err := mediaHTTPClient.Get(rawURL)

// mattermost/client.go:273-286（downloadBytes，且 io.ReadAll(resp.Body) 无大小上限）
resp, err := c.client.Do(req)
```

对照组：misskey/api.go:215-255 实现了完整的 `validateDownloadURL/validateDownloadHost`（私网/环回/链路本地/CGNAT 段封禁 + `CheckRedirect` 逐跳复验 + `maxDownloadRedirects`），说明代码库有能力也有规范，这四处属于漏网。
- 修复建议：抽公共受控下载器（或直接复用 misskey 的实现形态）替换四处裸下载：

```go
// internal/platform 或各适配器内：
func safeDownload(ctx context.Context, rawURL string, maxBytes int64) ([]byte, error) {
    u, err := url.Parse(rawURL)
    if err != nil || (u.Scheme != "http" && u.Scheme != "https") {
        return nil, fmt.Errorf("非法下载地址")
    }
    if err := validateDownloadHost(u.Hostname()); err != nil { // 封禁 127/8、10/8、172.16/12、192.168/16、169.254/16、fe80::/10、100.64/10
        return nil, err
    }
    client := &http.Client{Timeout: 30 * time.Second, CheckRedirect: 重定向逐跳复验}
    ...io.ReadAll(io.LimitReader(resp.Body, maxBytes))
}
```

mattermost 的 downloadBytes 额外补 `io.LimitReader`（如 64MB）。

---

### [internal/platform/sources/wecom_ai_bot/webhook.go:385] 发送侧物化的临时文件（astrbot_wecomai_* / astrbot_wecom_*）上传后永不清理 [严重程度：中]

- 严重程度：中（条件触发：wecom_ai_bot 每次经 URL 发送文件/语音/视频、wecom 每次经 base64/URL 发送任意媒体，都会在系统临时目录落一个文件且从不删除；长期运行的实例磁盘被持续蚕食，文件内容为已发送媒体，亦属敏感数据残留。与 Part 02"入站媒体临时文件永不清理"同族，但这是发送侧的新实例）
- 问题描述：两处下载/物化后把路径交给上传函数，无任何清理路径：

```go
// wecom_ai_bot/webhook.go:384-407（componentFilePath）
if url != "" {
    tmp := filepath.Join(os.TempDir(), fmt.Sprintf("astrbot_wecomai_%d", time.Now().UnixNano()))
    ...
    if err := os.WriteFile(tmp, data, 0600); err != nil {
        return "", err
    }
    return tmp, nil          // ← SendFile/SendVoice 上传完就丢，无 os.Remove
}

// wecom/send.go:164-183（resolveComponentFile 的 base64 与 url 分支）
tmp := fmt.Sprintf("%s/astrbot_wecom_%d%s", os.TempDir(), time.Now().UnixNano(), suffix)
...
return tmp, nil              // ← 同样无清理
```

对照组：misskey/adapter.go:511-515 在上传后按"位于 os.TempDir() 前缀"清理临时文件，telegram/lark/line/mattermost 均有 `schedule*Cleanup` 延迟删除，wecom 两兄弟是唯二漏网。
- 修复建议：上传后立即删除（仿 misskey 的守卫式清理，避免误删调用方自备文件）：

```go
// wecom_ai_bot/webhook.go SendMessageChain 的 File/Video/Record 分支
filePath, err := componentFilePath(cc.Path, cc.URL)
if err != nil { ... }
defer func() {
    if strings.HasPrefix(filePath, os.TempDir()+string(os.PathSeparator)) &&
        strings.Contains(filepath.Base(filePath), "astrbot_wecomai_") {
        _ = os.Remove(filePath)
    }
}()
```

wecom/send.go 的 sendChain 各媒体分支同样处理（仅当路径匹配 `astrbot_wecom_` 且位于 os.TempDir() 下时删除）。

---

### [internal/platform/sources/wecom_ai_bot/crypt.go:201] 三处回调签名比较未使用常量时间比较 [严重程度：低]

- 严重程度：低（弱安全实践：公网 webhook 的 SHA1/SHA256 签名用 `==`/`!=` 比较，理论上存在时序侧信道；实际经网络抖动利用难度极高，但同仓库 aiocqhttp/slack/weixin_official_account 均已使用 subtle.ConstantTimeCompare，标准不统一）
- 问题描述：

```go
// wecom_ai_bot/crypt.go:201（VerifyURL）、235（DecryptMsg）
if signature != msgSignature {
    return MsgCryptValidateSignatureError, ""
}

// wecom/wxcrypt.go:158（CheckSignature）、180（DecryptMessage）
if c.GetSignature(timestamp, nonce, echostr) != msgSignature {
    return "", ErrInvalidSignature
}

// lark/webhook.go:50（verifySignature）
return hex.EncodeToString(sum[:]) == signature
```
- 修复建议：统一改为常量时间比较：

```go
import "crypto/subtle"

if subtle.ConstantTimeCompare([]byte(signature), []byte(msgSignature)) != 1 {
    return MsgCryptValidateSignatureError, ""
}
```

lark/webhook.go 同理（先比长度再 ConstantTimeCompare，或直接对字节比较）。

---

### [internal/platform/sources/lark/webhook.go:107] webhook 时间戳无新鲜度校验，事件去重窗口（30 分钟）过后捕获的请求可重放 [严重程度：低]

- 严重程度：低（弱安全实践/条件触发：能截获一次合法请求（TLS + 猜中 webhook URL）的攻击者可在 30 分钟去重窗口过期后原样重放，签名仍然有效，旧消息会再次进入管线。Python 原版 lark/server.py 同样不校验时间戳新鲜度，属移植保真而非新引入，但本项目其它 webhook（wecom、weixin_official_account、qqofficial_webhook）均加了 ±300 秒窗口）
- 问题描述：签名只覆盖 timestamp 内容、不校验其新旧；去重缓存 30 分钟后删除（lark/adapter.go:221-235），此后同一 event_id 可再次通过全链路：

```go
// lark/webhook.go:106-118
if s.encryptKey != "" {
    timestamp := r.Header.Get("X-Lark-Request-Timestamp")
    ...
    if !s.verifySignature(timestamp, nonce, signature, body) { // 只验证签名匹配，不看 timestamp 距今多久
        ...
    }
}
```
- 修复建议：与 wecom 一致加新鲜度窗口（与去重窗口同量级即可）：

```go
if ts, err := strconv.ParseInt(timestamp, 10, 64); err != nil ||
    math.Abs(float64(time.Now().Unix()-ts)) > 300 {
    writeWebhookError(w, http.StatusBadRequest, "timestamp expired")
    return
}
```

---

### [internal/platform/sources/weixin_official_account/adapter.go:344] validateCiphertext 以密文首块作 IV，与协议 IV（key 前 16 字节）不一致 [严重程度：低]

- 严重程度：低（当前无功能影响的埋雷：该预校验函数用 `raw[:aes.BlockSize]`（密文前 16 字节）当 IV 解密，而微信协议及后续真正的解密 `msg.ShouldDecode` 用的是 `key[:16]`；CBC 特性使首块之外的字节不受 IV 影响，而此函数恰好只检查 pt[16:20]（长度字段，第二块）和末字节（最后一块），所以校验结果碰巧正确。日后若有人在此函数中检查首块 16 字节随机串或复用此解密结果，将得到错误数据）
- 问题描述：

```go
// weixin_official_account/adapter.go:343-344
pt := make([]byte, len(raw))
cipher.NewCBCDecrypter(block, raw[:aes.BlockSize]).CryptBlocks(pt, raw)  // IV 应为 key[:16]
```

对照同仓库 wecom/wxcrypt.go:107（`cipher.NewCBCEncrypter(block, c.key[:16])`）与 wecom_ai_bot/crypt.go:117，协议 IV 都是密钥前 16 字节。
- 修复建议：与协议对齐（纯一致性修正，不改行为）：

```go
cipher.NewCBCDecrypter(block, key[:aes.BlockSize]).CryptBlocks(pt, raw)
```

---

### [internal/platform/sources/telegram/adapter.go:345] workers 每个 chat 一个常驻 goroutine 永不回收；三处处理队列满时直接丢弃用户消息 [严重程度：低]

- 严重程度：低（条件触发：bot 被拉进大量群/被大量用户私聊时，`workers` map 与对应 goroutine 只增不减（仅随 ctx 结束），长生命周期进程缓慢积累；队列满（64/256/64）时静默丢消息仅有日志）
- 问题描述：

```go
// telegram/adapter.go:345-362（创建后无任何回收路径）
ch, ok := a.workers[chatID]
if !ok {
    ch = make(chan map[string]interface{}, 64)
    a.workers[chatID] = ch
    go func(c chan map[string]interface{}) { ... a.handleUpdate(ctx, u) ... }(ch)
}
// telegram/adapter.go:364-368
select {
case ch <- update:
default:
    logger.Warn("Telegram chat %s 的 update 队列已满，丢弃 update_id=%v", chatID, update["update_id"])
}
```

同型丢弃逻辑：wecom_ai_bot/long_connection.go:262-266（handlerCh 256 满"丢弃回调消息"）、mattermost/adapter.go:318-322（msgCh 64 满"丢弃一条消息"）。三者都是有意的背压取舍且留了日志，故仅列低。
- 修复建议：worker 加空闲超时自动退出并从 map 删除：

```go
go func(c chan map[string]interface{}) {
    idle := time.NewTicker(30 * time.Minute)
    defer idle.Stop()
    for {
        select {
        case <-idle.C:
            a.workerMu.Lock()
            if len(c) == 0 { delete(a.workers, chatID); a.workerMu.Unlock(); return }
            a.workerMu.Unlock()
        case u, ok := <-c:
            if !ok { return }
            idle.Reset(30 * time.Minute)
            a.handleUpdate(ctx, u)
        case <-ctx.Done():
            return
        }
    }
}(ch)
```

丢弃处至少可改为带短暂阻塞的 select（如 5 秒）再降级丢弃。

---

### [internal/platform/sources/wecom_ai_bot/server.go:145] 四个内置 HTTP 服务器只设 ReadHeaderTimeout、无 ReadTimeout，慢速请求体可长期占用连接 [严重程度：低]

- 严重程度：低（弱安全实践：攻击者建立连接后以极慢速率发送 body（每几分钟 1 字节），ReadHeaderTimeout 挡不住，连接与 goroutine 可被无限期占用；回调端点虽都有 1-4MB 的 body 上限，但那是读完之后的限制）
- 问题描述：

```go
// wecom_ai_bot/server.go:145-149（wecom/adapter.go:607-611、weixin_official_account/adapter.go:181-185、webchat/adapter.go:78-82 同型）
s.httpSrv = &http.Server{
    Addr:              fmt.Sprintf("%s:%d", s.host, s.port),
    Handler:           s.Handler(),
    ReadHeaderTimeout: 10 * time.Second,
    // 无 ReadTimeout / IdleTimeout
}
```
- 修复建议：补充读超时与空闲超时（合法回调 bodies 均为同步小报文，30 秒读超时无副作用）：

```go
s.httpSrv = &http.Server{
    Addr:              fmt.Sprintf("%s:%d", s.host, s.port),
    Handler:           s.Handler(),
    ReadHeaderTimeout: 10 * time.Second,
    ReadTimeout:       30 * time.Second,
    IdleTimeout:       120 * time.Second,
}
```

四处同样修改。

---

### [internal/platform/sources/wecom_ai_bot/api.go:156] processEncryptedImage 下载图片无大小上限 [严重程度：低]

- 严重程度：低（条件触发：`io.ReadAll(resp.Body)` 不限大小，仅有 15 秒整体超时；正常回调中的图片 URL 来自已验签的企微报文、指向企微 CDN，风险主要在异常/超大媒体时的一次性内存放大。同文件 webhook.go:367 有 32MB LimitReader、weixin_oc 有 20MB 上限，此处漏配）
- 问题描述：

```go
// wecom_ai_bot/api.go:156
encryptedData, err := io.ReadAll(resp.Body)
if err != nil {
    ...
}
```
- 修复建议：

```go
encryptedData, err := io.ReadAll(io.LimitReader(resp.Body, 32<<20))
if err != nil { ... }
if len(encryptedData) == 0 || len(encryptedData)%aes.BlockSize != 0 {
    return false, "参数错误: 无效的密文长度"  // 已有此校验，保留
}
```

---

### [internal/platform/sources/line/message.go:352] 媒体文件服务器的 sync.Once 失败被永久消费，启动失败后无法自愈 [严重程度：低]

- 严重程度：低（条件触发：6185-6200 端口全部被占时 `startMediaServer` 失败，`mediaServerOnce` 已被消耗，`mediaFileServer` 恒为 nil；即使之后端口空闲，所有 LINE 媒体发送（本地文件/缩略图注册）持续失败直至进程重启）
- 问题描述：

```go
// line/message.go:350-384
func startMediaServer() (string, error) {
    var err error
    mediaServerOnce.Do(func() {
        ...
        for {
            ln, err = net.Listen("tcp", fmt.Sprintf("127.0.0.1:%d", port))
            if err == nil { break }
            port++
            if port > 6200 {
                return            // ← 失败返回，但 Once 已消耗：此后永远走到下面的错误分支
            }
        }
        ...
    })
    if err != nil || mediaFileServer == nil {
        return "", fmt.Errorf("启动本地媒体文件服务器失败: %v", err)
    }
    ...
}
```
- 修复建议：用互斥锁 + "成功才落单例"替代 sync.Once，失败可重试：

```go
var mediaServerMu sync.Mutex

func startMediaServer() (string, error) {
    mediaServerMu.Lock()
    defer mediaServerMu.Unlock()
    if mediaFileServer != nil {
        return fmt.Sprintf("http://127.0.0.1:%d", mediaFileServer.port), nil
    }
    ms := &mediaServer{mux: http.NewServeMux(), tokens: map[string]mediaTokenEntry{}}
    ms.mux.HandleFunc("/api/file/", ms.handleFile)
    ln, err := net.Listen("tcp", "127.0.0.1:0") // 直接让内核分配端口，避免端口扫描与占用问题
    if err != nil {
        return "", err
    }
    ms.port = ln.Addr().(*net.TCPAddr).Port
    ms.srv = &http.Server{Handler: ms.mux}
    go func() { _ = ms.srv.Serve(ln) }()
    mediaFileServer = ms
    return fmt.Sprintf("http://127.0.0.1:%d", ms.port), nil
}
```

---

# Part 05：Astrbot-golang/internal/plugin（插件子系统：gRPC 子进程运行时 + 市场 + 安装/更新）

统计：发现 13 个问题（高 0 / 中 4 / 低 9）

### [internal/plugin/runtime.go:745] 插件更新失败/中途取消时，旧版本源码树被 defer 无条件删除（换名交换后无回滚） [严重程度：中]
- 严重程度：中（触发条件：对已安装插件做更新（dashboard handlers.go:2571 ReinstallSource → InstallFromSource），且交换目录后任一步失败——新版本 Prepare/Vet/Build 失败、Python 侧 pip/加载失败、或 dashboard 的 10 分钟 ctx 超时/用户取消（ctx 取消会杀掉 go build）。必然性：一旦更新失败，上一版本源码已丢失）
- 问题描述：Go 路径先做 staging→dest 的换名交换，随即注册 `defer os.RemoveAll(old)`：

```go
// runtime.go:740-745（Go 路径；Python 路径 832-837 同款）
	_ = os.Rename(srcDest, old)
	if err := os.Rename(staged, srcDest); err != nil {
		_ = os.Rename(old, srcDest)
		return nil, err
	}
	defer os.RemoveAll(old)
```

此后 Prepare（746）、Vet（749）、Build（770）、loadLocked（779）任一失败（或 ctx 取消）函数返回错误时，defer 都会把 `old`——上一版本的完整源码树——删除，而 `dest` 里已是新（坏）源码，manifest 仍指向 `dest`：
1. Python 插件：`dest`（data/plugins/<id>）就是运行载体，坏源码替换好源码后重启 `LoadInstalled` 必然加载失败，插件被"砖化"，只能人工重装；
2. Go 插件：旧二进制仍在 plugins-bin（go build 失败不覆盖产物）尚可启动，但源码目录与二进制版本错位，后续重编译会失败。

更新失败的原子性只剩"目录交换"半截，回滚在交换成功的那一刻就放弃了。
- 修复建议：交换成功后不立即注册删除，改为"提交制"——完全成功才删 old，失败回滚 dest→old：

```go
	_ = os.Rename(srcDest, old)
	if err := os.Rename(staged, srcDest); err != nil {
		_ = os.Rename(old, srcDest)
		return nil, err
	}
	commit := false
	defer func() {
		if commit {
			_ = os.RemoveAll(old)
			return
		}
		// 回滚：新源码挪回 staged 待清理，旧源码归位
		_ = os.Rename(srcDest, staged)
		_ = os.Rename(old, srcDest)
		_ = os.RemoveAll(staged)
	}()
	// ... Prepare / Vet / Build / pip / loadLocked / recordInstall 全部成功后：
	commit = true
```

（installPythonSource 的 832-837 做同样处理。）

### [internal/plugin/runtime.go:1598] 首次准备 Python 运行时在持有实例表写锁 m.mu 期间执行分钟级网络 IO，全部插件管理操作被阻塞 [严重程度：中]
- 严重程度：中（触发条件：主机首次安装/唤醒任意 Python 插件（宿主无 CPython/venv 未建）。CPython 下载数十 MB + venv 创建 + pip 安装宿主基础依赖（grpcio/cryptography 等）在网络一般时需数分钟，期间整个插件子系统卡死）
- 问题描述：

```go
// runtime.go:1597-1619
func (m *SubprocessManager) pythonRuntimeWithStage(stage func(string)) (*pysdk.RuntimeEnv, error) {
	m.mu.Lock()
	defer m.mu.Unlock()
	...
	env, err := pysdk.PrepareRuntimeWithStage(m.dataDir, stage)   // 下载 CPython + 建 venv + pip 安装
	if err != nil {
		return nil, err
	}
```

`m.mu` 是 instances/failures 表的读写锁，被跨整段运行时准备持有。期间所有 `m.Get/List/Clients/Failed`（RLock）、`loadLocked` 落表、`Unload/unloadLocked`、`sweepIdlePlugins`、`SetHostCapabilities`、崩溃重启的写回（handleExit → restart）全部阻塞：WebUI 插件列表打不开、闲置清扫停摆、Go 插件的崩溃重启排队。安装路径（installPythonSource → pythonRuntimeForInstall，runtime_prompt.go:273）与运行时懒唤醒路径（EnsureLoaded → startInstance → pythonRuntime）都会进入此函数。
- 修复建议：为 pythonEnv 单独设锁，把网络准备移出 m.mu：

```go
type SubprocessManager struct {
	...
	pythonEnvMu sync.Mutex   // 仅保护 pythonEnv 字段的读改写
}

func (m *SubprocessManager) pythonRuntimeWithStage(stage func(string)) (*pysdk.RuntimeEnv, error) {
	m.pythonEnvMu.Lock()
	defer m.pythonEnvMu.Unlock()
	if m.pythonEnv != nil && pythonEnvUsable(m.pythonEnv) {
		return m.pythonEnv, nil
	}
	m.pythonEnv = nil
	pysdk.SetPyPIIndex(m.pipIndex)
	env, err := pysdk.PrepareRuntimeWithStage(m.dataDir, stage)
	if err != nil {
		return nil, err
	}
	m.pythonEnv = env
	return env, nil
}
```

### [internal/plugin/runtime.go:1554] startInstance 全局互斥内同步执行 pip install，且 pip 子进程不受任何 ctx/超时约束 [严重程度：中]
- 严重程度：中（触发条件：启动/唤醒/崩溃重启任一含 requirements.txt 的 Python 插件时网络缓慢或镜像不可达。pip 自身重试可达数分钟且无法被取消，期间 startInstanceMu 串行化导致其它所有插件的加载/重载/重启全部排队）
- 问题描述：startInstance 在函数入口持有全局 `startInstanceMu`（runtime.go:1524-1525，为保护 SDK 的进程级 hostPluginID 身份绑定），Python 分支内每次启动只要源码目录有 requirements.txt 就同步跑 pip：

```go
// runtime.go:1554-1561
		if req := filepath.Join(abs, "requirements.txt"); func() bool {
			_, err := os.Stat(req)
			return err == nil
		}() {
			if err := m.pipInstall(env, abs, req); err != nil {
				logger.I18nWarn("插件 %s 依赖安装失败: %v（插件可能缺少依赖）", id, err)
			}
		}
```

而 pipInstall 用的是不带 ctx 的 `exec.Command`（runtime.go:900）：崩溃重启路径 restart 的 30s ctx（1927）、SetEnabled 的 60s ctx（runtime_admin.go:283）、dashboard 的 10min ctx 全都杀不掉 pip 进程——CommandContext 缺失使所有上层超时形同虚设；pip 阻塞多久，startInstanceMu 就被占多久，其余插件的装载（含 Go 插件的普通加载）整体停摆。
- 修复建议：pipInstall 接受 ctx 并改用 exec.CommandContext + 显式上限；requirements 未变化时跳过重复安装：

```go
func (m *SubprocessManager) pipInstall(ctx context.Context, env *pysdk.RuntimeEnv, pluginDir, req string) error {
	...
	ctx, cancel := context.WithTimeout(ctx, 5*time.Minute)
	defer cancel()
	cmd := exec.CommandContext(ctx, env.PythonBin, args...)
	...
}
// startInstance / installPythonSource 调用处传 ctx；
// 可另以 requirements.txt 的 mtime 哈希记录跳过无变化安装。
```

### [internal/plugin/host_service.go:69] 同名 Go/Python 插件并存时按 Name 解析实例不确定，GetConfig/SaveConfig 可能命中另一语言变体的配置目录 [严重程度：中]
- 严重程度：中（触发条件：同一插件名以 Go 与 Python 两个变体同时安装运行（id 为 name_go / name_python，Name 同为 name）——这正是本项目明确支持的隔离布局。此时配置读写与 SessionWait 喂入随机落到其中一个变体）
- 问题描述：插件反向 RPC 的身份是注册名，pluginConfigID 先按运行实例解析：

```go
// host_service.go:69-88
func (m *SubprocessManager) pluginConfigID(name string) string {
	...
	if inst := m.instanceByName(name); inst != nil {
		if inst.ID != "" {
			return inst.ID
		}
		return name
	}
	...
}

// runtime_admin.go:525-535
func (m *SubprocessManager) instanceByName(name string) *PluginInstance {
	if inst := m.Get(name); inst != nil {
		return inst
	}
	for _, inst := range m.List() {   // instances 按 id（name_language）分键
		if inst.Name == name {
			return inst
		}
	}
	return nil
}
```

SDK 侧 GetConfig/SetConfig 的身份校验只比对注册名（两个变体的连接身份同为 name），宿主无法区分调用方是哪一个；`m.List()` 按 map 遍历顺序返回"第一个" Name 匹配——Go/Python 变体并存时结果随机漂移：插件 A（Python）的 `GetConfig` 可能读到 `plugins_config/x_go/config.json`，`SetConfig` 可能写到另一个变体目录，破坏 configPath 注释宣称的"同名 Go/Python 插件的配置完全隔离"（FlatSchemaByID 特意按 id 精确读取，正说明 Name 维度存在此坑）。SessionWaitForUmo → InstanceByName（session_wait.go:119）同样受影响，事件可能喂给另一个变体的进程。
- 修复建议：让 SDK 把连接的 connKey（accept 时刻绑定的 manifest id，host.go 中已有该字段）随 GetConfig/SetConfig/RegisterSessionWait 一并传给宿主，宿主按 id 精确解析；宿主侧最小确定性修复：

```go
// SDK：GetConfig/SetConfig 校验 req.PluginName == s.identity() 通过后，
// 以 s.connKey（accept 时绑定的 manifest id）作为调用方实例 id 传入 hook。
// 宿主 host_service.go：
		GetConfig: func(pluginName, connID string) (map[string]any, error) {
			id := connID
			if id == "" {
				id = subMgr.pluginConfigID(pluginName) // 兼容旧 SDK
			}
			cfg := resolvePluginConfigID(subMgr, id)
			...
// 未升级 SDK 前至少消除随机性：
func (m *SubprocessManager) instanceByName(name string) *PluginInstance {
	if inst := m.Get(name); inst != nil {
		return inst
	}
	var hit *PluginInstance
	for _, inst := range m.List() {
		if inst.Name == name && (hit == nil || inst.ID < hit.ID) {
			hit = inst // 同名多变体时按 id 字典序取确定值，避免随机漂移
		}
	}
	return hit
}
```

### [internal/plugin/runtime.go:535] GoInstall 未校验 pkg 格式，"-" 前缀存在 go CLI 参数注入残余面（Part 01 交叉引用正式结论） [严重程度：低]
- 严重程度：低（暴露面：dashboard 管理员专用的 /update/pip-install 端点；pkg 以 "-" 开头会被 go 命令当作 flag 解析。无 shell、无命令注入——见问题描述中的链路结论）
- 问题描述：dashboard handlers.go:6611 把 body.Package 原样传入后：

```go
// runtime.go:543-556
	if pkg = strings.TrimSpace(pkg); pkg == "" {
		return "", fmt.Errorf("模块名不能为空")
	}
	if !strings.Contains(pkg, "@") {
		pkg += "@latest"
	}
	extra := map[string]string{}
	if strings.TrimSpace(goproxy) != "" {
		extra["GOPROXY"] = strings.TrimSpace(goproxy)
	}
	cmd := exec.CommandContext(ctx, bin, "install", pkg)
```

链路结论：pkg 是单一 argv 元素传给 `go install`，mirror 只进 GOPROXY 环境变量，二者均不经 shell 拼接——**不存在命令注入/路径穿越/SSRF**。残余面有二：① pkg 以 "-" 开头会被 go CLI 解析为 flag（参数注入；且由于未含 "@" 时追加 "@latest"，纯 flag 串会变成非法 flag，实际可利用性有限）；② `go install <module>` 本身即从模块代理拉取源码并编译执行（init 即运行）任意代码，这是该"Go 版 pip install"管理员功能的设计内语义。
- 修复建议：

```go
	if strings.HasPrefix(pkg, "-") {
		return "", fmt.Errorf("非法模块名 %q（不允许以 '-' 开头）", pkg)
	}
	if i := strings.Index(pkg, "@"); i > 0 {
		if err := module.CheckPath(pkg[:i]); err != nil {
			return "", fmt.Errorf("非法模块路径: %w", err)
		}
	}
```

### [internal/plugin/host_service.go:822] 插件管理控制面 RPC 无自我限定，任意插件可装卸/启停其他插件 [严重程度：低]
- 严重程度：低（暴露面：任意已加载插件（Go/Python 均可达）经 HostService 反向 RPC。属"可信插件"模型下的设计取舍而非漏洞，但与配置读写的纵深防御不对称）
- 问题描述：SDK 侧 GetConfig/SetConfig 强制"只能操作自己"（req.PluginName != identity 即 PermissionDenied），而插件管理类 RPC 只要求身份非空（requireIdentity），目标完全由调用方指定：

```go
// host_service.go:822-842
		SetPluginEnabled: func(pluginName string, enabled bool) error {
			...
			return subMgr.SetEnabled(id, enabled)
		},
		InstallPlugin: func(repo string) error {
			...
			_, err := subMgr.InstallFromSource(context.Background(), "", repo, InstallOptions{})
			return err
		},
		UninstallPlugin: func(pluginName string) error {
			...
			return subMgr.Uninstall(id, false, false)
		},
```

一个被供应链污染的插件可静默禁用/卸载安全类插件，再经 InstallPlugin 从任意 repo 拉起后门插件（InstallPlugin 还用 context.Background()，无超时约束）。与 source.go:36-40 自述的"隔离边界是同用户子进程、插件可信"模型一致，Python 原版亦然，故定低；但既然配置读写已做身份限定，管理面保持对称防御成本不高。
- 修复建议：宿主侧为管理类 hook 增加白名单（默认仅允许操作自身，或要求插件 metadata 声明 `admin: true` 能力并在安装时由用户确认）；InstallPlugin 至少改为带超时的 context。

### [internal/plugin/session_wait.go:51] waitID 可预测且 UnregisterSessionWait 不校验归属，插件间可互相注销会话等待 [严重程度：低]
- 严重程度：低（暴露面：任意插件可枚举 `<插件名>-<序号>` 形态的 waitID 并注销其他插件的等待，使其 SessionWaiter 永不触发——插件间 DoS，不跨信任边界）
- 问题描述：

```go
// session_wait.go:49-58
	n := sessionWaitSeq.Add(1)
	waitID := fmt.Sprintf("%s-%d", sanitizeID(pluginName), n)

	m.sessionWaitMu.Lock()
	...
	m.sessionWaitReg[waitID] = &sessionWaitEntry{pluginName: pluginName, umo: umo}
```

```go
// session_wait.go:72-79
func (m *SubprocessManager) UnregisterSessionWait(waitID string) {
	...
	m.sessionWaitMu.Lock()
	delete(m.sessionWaitReg, waitID)
	m.sessionWaitMu.Unlock()
}
```

waitID 格式完全可预测（sanitizeID 后的插件名 + 全局递增序号，SDK 侧 UnregisterSessionWait 也仅校验"身份非空"不校验归属），恶意插件可批量注销其他插件注册的等待。注册表本身有 maxSessionWaits=10000 上限与超时/卸载兜底，影响限于定向干扰。
- 修复建议：

```go
	// 注册时记录归属（已有 entry.pluginName），注销时校验调用方身份：
	func (m *SubprocessManager) UnregisterSessionWait(pluginName, waitID string) bool {
		m.sessionWaitMu.Lock()
		defer m.sessionWaitMu.Unlock()
		if e, ok := m.sessionWaitReg[waitID]; ok && e.pluginName == pluginName {
			delete(m.sessionWaitReg, waitID)
			return true
		}
		return false
	}
	// 或 waitID 追加 crypto/rand 8 字节熵，使其不可枚举。
```

### [internal/plugin/ccompiler.go:404] zig/Clang 归档下载无完整性校验、无大小上限、未用 pinned 拨号（与 source.go 下载路径不对称） [严重程度：低]
- 严重程度：低（暴露面：cgo 插件首次构建触发 zig 下载；镜像表默认 ziglang.org，覆盖镜像需本地环境变量 ASTRBOT_CLANG_MIRROR，攻击面主要为镜像被劫持的供应链场景）
- 问题描述：

```go
// ccompiler.go:404
	client := &http.Client{Timeout: 30 * time.Minute, CheckRedirect: safeRedirect}
```

downloadClangArchive / resumeDownload（425-503）三个与 source.go 下载路径不对称的点：① 无任何 checksum/签名校验（对比 toolchain 的 checksumURL 机制），解压即得会被 cgo 构建调用的编译器；② resumeDownload 写文件不限长（无 maxDownloadSize 对应物）；③ Transport 未挂 pinnedDialContext（source.go:502-511 用于封死 DNS-rebinding 的措施在此缺失，safeRedirect 只复检主机名）。提取侧反而是完备的（safeJoin + stripTopDir + 逐条目 ctx 检查 + 安装锁）。
- 修复建议：ziglang.org 为各版本发布 JSON（含 sha256）；下载后校验再解压，并复用 source.go 的限额与 pin：

```go
	const maxZigDownload int64 = 300 << 20
	// resumeDownload 内：limited := io.LimitReader(resp.Body, maxZigDownload+1)
	// downloadClangArchive 校验：
	if sum, ok := zigSHA256[info.archive]; ok {
		if err := verifySHA256(archivePath, sum); err != nil { _ = os.Remove(archivePath); return err }
	}
	// client 挂 pin：
	client := &http.Client{Timeout: 30 * time.Minute, CheckRedirect: safeRedirect,
		Transport: &http.Transport{Proxy: http.ProxyFromEnvironment, DialContext: pinnedDialContext()}}
```

### [internal/plugin/runtime_admin.go:1006] fetchRepoDoc 的 http.Client 无重定向 SSRF 复检，且先读满 body 再判断状态码 [严重程度：低]
- 严重程度：低（暴露面：URL 由 rawRepoDocURLs 从 manifest 的 repo 构造且限定 host==github.com → raw.githubusercontent.com，或加 github_proxy 前缀（config 信任源）；默认面小，属与 source.go 防护不对称的弱实践）
- 问题描述：

```go
// runtime_admin.go:1006-1023
	client := &http.Client{Timeout: 5 * time.Second}   // 无 CheckRedirect
	const maxDocSize = 2 << 20
	for _, rawURL := range rawURLs {
		resp, err := client.Get(rawURL)
		if err != nil {
			continue
		}
		content, err := io.ReadAll(io.LimitReader(resp.Body, maxDocSize+1))
		_ = resp.Body.Close()
		if err != nil || int64(len(content)) > maxDocSize {
			continue
		}
		if resp.StatusCode == http.StatusOK && len(content) > 0 {
```

两点：① 未挂 safeRedirect，默认客户端会跟随任意 30x 而不复检目标主机（source.go:310-315 的逐跳复检在此缺失；raw.githubusercontent.com 正常不重定向到内网，故仅弱实践）；② 404/500 时也先把 body 读满 2MB 才判状态码，浪费且对 proxy 前缀场景多读无谓流量。
- 修复建议：

```go
	client := &http.Client{Timeout: 5 * time.Second, CheckRedirect: safeRedirect}
	...
		resp, err := client.Get(rawURL)
		if err != nil { _ = resp.Body.Close(); continue }
		if resp.StatusCode != http.StatusOK { _ = resp.Body.Close(); continue }
		content, err := io.ReadAll(io.LimitReader(resp.Body, maxDocSize+1))
		_ = resp.Body.Close()
```

### [internal/plugin/runtime.go:2118] migratePluginLayout 第 4 步 id 归一化结果依赖第 5 步条件写回，边界条件下变更丢失、每次启动重复迁移 [严重程度：低]
- 严重程度：低（触发条件：manifest 条目的 Binary 不含旧 id 路径段且 ConfigDir/DocsDir/DataDir 恰已等于新 id 的路径——即上次迁移中途崩溃留下的残留态；影响为重复迁移与日志噪音，功能可自愈）
- 问题描述：第 4 步在循环变量副本上改 id/语言（目录已实际 movePluginDir 到新 id），但未写回切片：

```go
// runtime.go:2117-2122
				oldID := e.ID
				e.ID = stableID
				e.Language = lang
				sid = sanitizeID(stableID)
				e.Binary = rewritePluginIDPath(e.Binary, oldID, stableID)
				changed = true
```

写回只发生在第 5 步的条件块（2131-2141）：`if newBinary != e.Binary || e.ConfigDir != ... ` 才 `man.Plugins[i] = e`。当 rewritePluginIDPath 无命中（Binary 不含旧 id 段）且三个足迹字段已等于新路径时，条件不成立 → ID 变更丢失，而下一次启动 stableID 仍不等于 e.ID、man.Get(stableID) 仍为 nil → 无限重复执行第 4 步迁移并重复告警（2124 的冲突分支不会触发，因为写回从未发生）。
- 修复建议：第 4 步变更后立即写回：

```go
				oldID := e.ID
				e.ID = stableID
				e.Language = lang
				sid = sanitizeID(stableID)
				e.Binary = rewritePluginIDPath(e.Binary, oldID, stableID)
				man.Plugins[i] = e   // 立即落盘本次变更，不依赖第 5 步条件
				changed = true
```

### [internal/plugin/runtime.go:1970] plugins-manifest.json 损坏时全部已安装插件静默不加载，且一切管理操作失败，无恢复路径 [严重程度：低]
- 严重程度：低（触发条件：manifest JSON 被外部写坏/手工编辑出错。Save 本身是原子写（manifest.go:74-110），但一旦损坏无救援）
- 问题描述：

```go
// runtime.go:1970-1974
	man, err := LoadManifest(m.manifestPath())
	if err != nil {
		logger.I18nWarn("LoadInstalled: %v", err)
		return
	}
```

plugins-manifest.json 是安装态的唯一记录；LoadManifest 解析失败（manifest.go:66-68 返回 err）后：LoadInstalled 直接放弃全部插件加载（用户看到所有插件消失），SetEnabled/Uninstall/BindSource/ReinstallSource/SetIdleUnloadBlocked/EnsureLoaded 全部因 LoadManifest 失败而报错——损坏文件既不备份也不跳过，用户只能手删。ListInfo 倒是容错（err 时用空 manifest），行为不一致。
- 修复建议：损坏时备份原文件并以空 manifest 继续，保证系统可自愈（插件可重装）：

```go
	man, err := LoadManifest(m.manifestPath())
	if err != nil {
		logger.I18nWarn("LoadInstalled: manifest 损坏（%v），已备份至 %s.corrupt 并重置", err, m.manifestPath())
		_ = os.Rename(m.manifestPath(), m.manifestPath()+".corrupt")
		man = &Manifest{Version: 1}
	}
```

### [internal/plugin/runtime_admin.go:1034] manifest 状态读取无缓存，README/行为页形成逐插件 N+1 读盘 [严重程度：低]
- 严重程度：低（性能质量问题：插件几十个时，一次 WebUI 详情/行为页刷新触发几十次 plugins-manifest.json 的全量读盘与 JSON 解析）
- 问题描述：repoURLFor（1034-1047）在 Readme/Changelog 各调用一次；IdleUnloadBlocked（1241-1250）被 Components（617-625，每插件详情）与 dashboard 行为页逐插件调用；每次都 `LoadManifest` 全量读文件并解析，无任何缓存。对比同包 host_service.go:127-156 的 personaFileCache 已示范了 mtime 失效缓存的做法；sweepIdlePlugins（runtime.go:1290-1298）也特意做了"一次构建常驻表"的优化，说明读盘成本已被认知，只是这两处漏掉了。
- 修复建议：给 SubprocessManager 挂一个 mtime 失效的 manifest 只读缓存：

```go
func (m *SubprocessManager) cachedManifest() *Manifest {
	m.manifestCacheMu.Lock()
	defer m.manifestCacheMu.Unlock()
	if info, err := os.Stat(m.manifestPath()); err == nil {
		if m.manifestCache != nil && m.manifestCacheAt.Equal(info.ModTime()) {
			return m.manifestCache
		}
		if man, err := LoadManifest(m.manifestPath()); err == nil {
			m.manifestCache, m.manifestCacheAt = man, info.ModTime()
			return man
		}
	}
	return &Manifest{Version: 1}
}
// repoURLFor / IdleUnloadBlocked / readCachedDoc 回退路径改用 cachedManifest()
// （写路径 manifestMu 内 Save 后同步失效缓存）。
```

### [internal/plugin/host_service.go:139] 人格数据源用相对路径读 data/personas.json，与 dashboard 按 dataDir 写入不对称 [严重程度：低]
- 严重程度：低（触发条件：宿主 CWD 非项目根，或 dataDir 配置不为 "data"。此时插件侧人格 RPC 全部静默返回空）
- 问题描述：

```go
// host_service.go:139-155
func loadPersonas() []map[string]any {
	info, err := os.Stat("data/personas.json")
	if err != nil {
		return nil
	}
	...
	data, err := os.ReadFile("data/personas.json")
```

dashboard 的人格存储按 `filepath.Join(dataDir, "personas.json")` 绝对路径写入（dashboard/personas_store.go:28），而本文件（注释自述为复制 lifecycle 的本地副本）按 CWD 相对路径读取；两者仅在"CWD=项目根且 dataDir=data"的默认布局下恰好一致。不一致时 GetPersonas/GetDefaultPersona/ResolveSelectedPersona/GetPersonaTree 对插件静默失效（返回 nil/[%None]），无任何告警。lifecycle 侧同款代码存在同样问题（Part 06 范围）。
- 修复建议：SetHostService 增加 dataDir 参数（或复用 dashboard 的 personas store 实例），统一从 dataDir 绝对路径读取：

```go
func SetHostService(pm *platform.PlatformManager, subMgr *SubprocessManager, ..., dataDir string) {
	...
	personaDataPath = filepath.Join(dataDir, "personas.json")
}
// loadPersonas 内改为读 personaDataPath。
```

---

# Part 06：Astrbot-golang/internal/provider（LLM/向量/语音 provider 接入层）

统计：发现 14 个问题（高 1 / 中 4 / 低 9）。

### [internal/provider/sources/gemini_source.go:265] Gemini provider 完全未实现函数调用（tools），请求与响应双侧均丢弃 [严重程度：高]
- 严重程度：高（选择 Gemini 作为 chat provider 且启用工具（stages.go:1655/1657 必然填充 req.Tools）时必然触发：工具 schema 从不发给模型，模型返回的 functionCall 也从不解析，agent 工具循环整体失效；非安全漏洞，属核心功能移植缺失）
- 问题描述：`buildRequestBody` 组装的请求体只有 `contents`/`systemInstruction`，`req.Tools` 被完全忽略：

```go
// gemini_source.go:249-275
	// Add current user message with its inline media parts.
	parts := geminiPartsFromContent(req.Prompt)
	for _, imgURL := range req.ImageURLs {
		if part := imageToGeminiPart(imgURL); part != nil {
			parts = append(parts, part)
		}
	}
	...
	body := map[string]interface{}{
		"contents": contents,
	}
	if req.SystemPrompt != "" {
		body["systemInstruction"] = map[string]interface{}{...}
	}
	return body
```

响应侧同样只解析 `text`：非流式（gemini_source.go:93-115）与流式（gemini_source.go:156-191）的 candidates.parts 结构体只有 `Text string` 字段，Gemini 的 `functionCall` part（含 name/args）被静默丢弃。上游 Python gemini_source.py 是完整实现：请求侧 `tools.get_func_desc_google_genai_style()` 转 functionDeclarations（gemini_source.py:202-203、283），响应侧解析 `part.function_call` 填充 tools_call_name/args/ids（gemini_source.py:548-552）。结果：Go 版 Gemini 用户配置任何工具（网页搜索、插件工具等）后，模型永远收不到工具定义、也无法发起调用，且无任何日志提示。作为对照，同目录 anthropic_source.go:345-349 和 openai_source.go:354-356 都正确透传了 req.Tools。
- 修复建议：

```go
// buildRequestBody 末尾追加（对齐 Python get_func_desc_google_genai_style）：
	if len(req.Tools) > 0 {
		funcDecls := []map[string]interface{}{}
		for _, t := range req.Tools {
			fn, _ := t["function"].(map[string]interface{})
			if fn == nil {
				continue
			}
			name, _ := fn["name"].(string)
			if name == "" {
				continue
			}
			decl := map[string]interface{}{"name": name}
			if desc, ok := fn["description"].(string); ok && desc != "" {
				decl["description"] = desc
			}
			if params, ok := fn["parameters"].(map[string]interface{}); ok {
				decl["parameters"] = params
			}
			funcDecls = append(funcDecls, decl)
		}
		if len(funcDecls) > 0 {
			body["tools"] = []map[string]interface{}{
				{"functionDeclarations": funcDecls},
			}
		}
	}

// 响应解析（非流式与流式的 part 结构体）追加 functionCall 字段并收集：
	type partStruct struct {
		Text         string `json:"text"`
		FunctionCall *struct {
			Name string                 `json:"name"`
			Args map[string]interface{} `json:"args"`
		} `json:"functionCall"`
	}
	// 遍历时：
	if part.FunctionCall != nil {
		args := part.FunctionCall.Args
		if args == nil {
			args = map[string]interface{}{}
		}
		llmResp.ToolsCallName = append(llmResp.ToolsCallName, part.FunctionCall.Name)
		llmResp.ToolsCallArgs = append(llmResp.ToolsCallArgs, args)
		llmResp.ToolsCallIDs = append(llmResp.ToolsCallIDs, part.FunctionCall.Name) // Gemini 无 call id，用 name 占位
	}
```

### [internal/provider/sources/ollama_source.go:193] Ollama provider 丢弃 req.Tools 与 ImageURLs/AudioURLs，函数调用与多模态对 Python 版整体回退 [严重程度：中]
- 严重程度：中（配置 type=ollama（dashboard/handlers.go:1037 的 Ollama 模板默认注册该类型）且启用工具/发送图片时触发；Python 原版 Ollama 走 openai_chat_completion + `http://127.0.0.1:11434/v1`（default.py:1417-1427），天然具备 tools/image_url 支持，Go 版换成原生 /api/chat 后两者均丢失）
- 问题描述：`buildRequestBody` 只组装 model/messages/stream，`req.Tools`、`req.ImageURLs`、`req.AudioURLs` 全部被忽略：

```go
// ollama_source.go:193-211
func (s *OllamaSource) buildRequestBody(req *provider.ProviderRequest, stream bool) map[string]interface{} {
	messages := []map[string]interface{}{}
	if req.SystemPrompt != "" {
		messages = append(messages, map[string]interface{}{
			"role":    "system",
			"content": req.SystemPrompt,
		})
	}
	messages = append(messages, req.Contexts...)
	messages = append(messages, map[string]interface{}{
		"role":    "user",
		"content": req.Prompt,
	})
	return map[string]interface{}{
		"model":    s.GetModel(),
		"messages": messages,
		"stream":   stream,
	}
}
```

当前用户消息的 content 是纯字符串，图片/音频引用（Ollama /api chat 需要 messages[].images base64 数组）没有任何收集逻辑；工具 schema（Ollama /api/chat 的 tools 字段）也没有透传。Python 用户从 4.x 迁移过来后，llama3.1+ 等支持工具的本地模型在 Go 版中无法再发起工具调用。另注意历史里的 OpenAI 格式 image_url 内容块会原样塞进 /api/chat（Ollama 原生协议不认识该结构）。
- 修复建议：

```go
	// 图片：Ollama 原生协议要求 messages[].images 为 base64 数组（不带 data: 前缀）
	var images []string
	for _, img := range req.ImageURLs {
		if data, _, err := fetchMediaData(img); err == nil {
			images = append(images, base64.StdEncoding.EncodeToString(data))
		}
	}
	userMsg := map[string]interface{}{"role": "user", "content": req.Prompt}
	if len(images) > 0 {
		userMsg["images"] = images
	}
	messages = append(messages, userMsg)
	body := map[string]interface{}{"model": s.GetModel(), "messages": messages, "stream": stream}

	// 工具：透传 OpenAI function schema（Ollama 的 tools 字段与 OpenAI 结构一致）
	if len(req.Tools) > 0 {
		body["tools"] = req.Tools
	}
```

或者更省事：把 Go 的 Ollama 模板改回 Python 的做法——type 用 openai（OpenAISource），api_base 默认 `http://127.0.0.1:11434/v1`，复用 openai_source 已有的 tools/多模态/上下文处理。

### [internal/provider/sources/openai_source.go:348] 未移植 Python 的空 assistant 消息/孤儿 tool 消息清理，严格 API 将直接 400 [严重程度：中]
- 严重程度：中（条件触发：上下文经截断/压缩后出现"content 与 tool_calls 全空"的 assistant 消息、或 assistant(tool_calls) 被截断后残留孤儿 tool 消息时，Moonshot、DeepSeek Reasoner 等严格 API 返回 400，请求失败；Python 侧在 _query 与 _query_stream 发送前均调用 _sanitize_assistant_messages（openai_source.py:566、624）避免此问题）
- 问题描述：Go 版把 `req.Contexts` 原样拼进 messages，没有任何清洗：

```go
// openai_source.go:340-351
	// Build messages
	messages := []map[string]interface{}{}
	if req.SystemPrompt != "" {
		messages = append(messages, map[string]interface{}{
			"role":    "system",
			"content": req.SystemPrompt,
		})
	}
	// Add context messages
	messages = append(messages, req.Contexts...)
```

Python 原版 _sanitize_assistant_messages（openai_source.py:452-527）做了三件事：content/tool_calls/reasoning_content 三者全空的 assistant 消息直接丢弃；有 tool_calls 但 content 为空的把 content 置 None（符合 OpenAI 规范）；删除因上下文截断产生的孤儿/重复 tool 消息。Go 版全部缺失，推理模型（DeepSeek Reasoner 输出的历史含空 content assistant 消息）与工具循环长历史在严格端点上必然报错。
- 修复建议：

```go
func sanitizeContexts(msgs []map[string]interface{}) []map[string]interface{} {
	out := make([]map[string]interface{}, 0, len(msgs))
	pendingToolIDs := map[string]bool{}
	for _, msg := range msgs {
		role, _ := msg["role"].(string)
		if role == "assistant" {
			content, _ := msg["content"].(string)
			hasToolCalls := len(toolCallsSlice(msg["tool_calls"])) > 0
			if content == "" && !hasToolCalls {
				continue // 空 assistant 垃圾消息，丢弃
			}
			copied := cloneMap(msg)
			if content == "" && hasToolCalls {
				copied["content"] = nil
			}
			pendingToolIDs = map[string]bool{}
			for _, tc := range toolCallsSlice(copied["tool_calls"]) {
				if id, ok := tc["id"].(string); ok {
					pendingToolIDs[id] = true
				}
			}
			out = append(out, copied)
			continue
		}
		if role == "tool" {
			id, _ := msg["tool_call_id"].(string)
			if pendingToolIDs[id] {
				delete(pendingToolIDs, id)
				out = append(out, msg)
			}
			continue // 孤儿 tool 消息，丢弃
		}
		pendingToolIDs = map[string]bool{}
		out = append(out, msg)
	}
	return out
}
// buildRequestBody 中：messages = append(messages, sanitizeContexts(req.Contexts)...)
```

### [internal/provider/sources/openai_responses_source.go:280] 流式 response.failed / content_filter 终态错误被吞掉，以空响应冒充成功 [严重程度：中]
- 严重程度：中（条件触发：Responses API 流式请求服务端失败（response.failed）或输出被内容过滤截断（response.incomplete 且 reason=content_filter）时，parseResponse 返回的 error 被丢弃，最终回退到"聚合增量"分支发送一个不含任何错误信息的 final，用户/上层拿到部分或空文本却视为成功，服务端错误码与错误信息完全丢失）
- 问题描述：

```go
// openai_responses_source.go:278-284
			case "response.completed", "response.incomplete", "response.failed":
				if event.Response != nil {
					if parsed, err := s.parseResponse(event.Response, len(req.Tools) > 0); err == nil {
						final = parsed
					}
				}
				return true
```

parseResponse（openai_responses_source.go:371-384）对 `resp.Status == "failed"` 和 `IncompleteDetails.Reason == "content_filter"` 返回 error；这里 `err == nil` 才赋值 final，出错时 final 保持 nil，随后 scan 返回、走到：

```go
// openai_responses_source.go:294-304
		if final == nil {
			// Stream ended without a terminal event: consolidate accumulated state.
			final = &provider.LLMResponse{
				Role:             "assistant",
				ID:               responseID,
				CompletionText:   content.String(),
				...
```

失败信息（resp.Error.Code/Message）无人消费。对比非流式路径（TextChat:176-180）会把 parseResponse 的 error 返回给调用方，流式路径应保持一致。
- 修复建议：

```go
			case "response.completed", "response.incomplete", "response.failed":
				if event.Response != nil {
					parsed, err := s.parseResponse(event.Response, len(req.Tools) > 0)
					if err == nil {
						final = parsed
					} else {
						termErr = err // 在闭包外声明 var termErr error
					}
				}
				return true
	...
		if termErr != nil {
			ch <- &provider.LLMResponse{Role: "err", CompletionText: termErr.Error()}
			return
		}
```

### [internal/provider/sources/openai_source.go:238] OpenAI 流式 data 中的 error 事件被静默忽略，错误被吞、截断文本当作完整回复 [严重程度：中]
- 严重程度：中（条件触发：OpenAI 兼容网关/中转在流中途下发 `data: {"error": {...}}`（部分兼容端点在配额耗尽、上游故障时这样做）时，错误事件解析后被丢弃，循环继续，最终以聚合的部分文本作为正常 final 发送，用户看到被截断的回复且无任何错误提示）
- 问题描述：chunk 结构体没有 error 字段，解析失败/未知事件一律继续：

```go
// openai_responses 与 openai_source 相同模式：openai_source.go:238-240
			if err := json.Unmarshal([]byte(data), &chunk); err != nil {
				return false
			}
```

`{"error":{"message":"Insufficient quota","type":...}}` 能被 Unmarshal 成功（未知字段被忽略），chunk.Choices 为空、Usage 为 nil，循环体什么都不做，`return false` 继续读流；流关闭后走正常收尾（openai_source.go:287-306）发送聚合 final。同包 anthropic_source.go:273-280 有 `case "error":` 分支正确上报，openai_source 与 kimi_code_source（kimi_code_source.go:252-310 的 switch 同样没有 error 分支）缺失。sse.go 也只处理 `data:` 行、忽略 `event: error` 行名。
- 修复建议：

```go
// openai_source.go 的 chunk 结构体追加：
	Error *struct {
		Message string `json:"message"`
		Type    string `json:"type"`
	} `json:"error"`
// Unmarshal 成功后立即检查：
			if chunk.Error != nil {
				msg := chunk.Error.Message
				if msg == "" {
					msg = chunk.Error.Type
				}
				ch <- &provider.LLMResponse{Role: "err", CompletionText: "API stream error: " + msg}
				return true
			}
```

### [internal/provider/sources/helpers.go:93] fetchMediaData 本地文件与 data URL 分支无大小上限，远程分支无 SSRF 防护 [严重程度：低]
- 严重程度：低（本地文件/data URL 读入内存不受 maxImageBytes 约束，异常大文件直接 os.ReadFile 全量载入再 base64（体积再放大 4/3），可致内存尖峰；远程 URL 拉取无重定向/IP 校验，与 pipeline web_fetch 的 SSRF 纵深防御不对齐——但 media 引用来源仅限 WebUI（已认证）与插件 RPC，且 Python 原版 media_utils 同样接受本地路径，属移植对等 + 弱加固项）
- 问题描述：

```go
// helpers.go:92-99
	path := strings.TrimPrefix(trimmed, "file://")
	if info, err := os.Stat(path); err == nil && !info.IsDir() {
		data, err := os.ReadFile(path)
		if err != nil {
			return nil, "", err
		}
		return data, mediaTypeForExt(path), nil
	}
```

`os.ReadFile` 无上限；decodeDataURL（helpers.go:134-152）的 base64 解码同样无上限。远程分支（helpers.go:100-128）有 20MB 上限，但默认 http.Client 跟随重定向且不校验目标 IP。对照同文件 maxImageBytes 常量（helpers.go:76）只在远程分支生效，三个入口行为不一致。
- 修复建议：

```go
	if info, err := os.Stat(path); err == nil && !info.IsDir() {
		f, err := os.Open(path)
		if err != nil {
			return nil, "", err
		}
		defer f.Close()
		data, err := io.ReadAll(io.LimitReader(f, maxImageBytes+1))
		if err != nil {
			return nil, "", err
		}
		if len(data) > maxImageBytes {
			return nil, "", fmt.Errorf("media exceeds %d bytes", maxImageBytes)
		}
		return data, mediaTypeForExt(path), nil
	}
```

decodeDataURL 在 base64.DecodeString 前先检查 `len(payload) > base64.StdEncoding.EncodedLen(maxImageBytes)` 直接拒绝。

### [internal/provider/sources/openai_source.go:156] TextChat 非流式不解析 reasoning_content，空 choices 静默返回空回复 [严重程度：低]
- 严重程度：低（条件触发：推理模型（DeepSeek R1 等）走非流式路径时 reasoning_content 丢失，展示推理功能失效；上游返回空 choices 时不报错而是返回空 CompletionText，用户收到空回复且无日志）
- 问题描述：

```go
// openai_source.go:132-158（节选）
	var result struct {
		Choices []struct {
			Message struct {
				Role      string `json:"role"`
				Content   string `json:"content"`
				...
```

Message 结构体没有 `reasoning_content` 字段（流式路径 openai_source.go:220 有 Reasoning 字段，非流式没有；Python _extract_reasoning_content（openai_source.py:700-718）对非流式同样提取）。空 choices 分支：

```go
// openai_source.go:156-158
	if len(result.Choices) == 0 {
		return &provider.LLMResponse{Role: "assistant", CompletionText: ""}, nil
	}
```

Python 原版对"无可输出内容"显式抛错（openai_source.py:934），Go 静默成功。
- 修复建议：

```go
		Message struct {
			Role             string `json:"role"`
			Content          string `json:"content"`
			ReasoningContent string `json:"reasoning_content"`
			...
	// llmResp 增加：ReasoningContent: choice.Message.ReasoningContent
	if len(result.Choices) == 0 {
		return nil, fmt.Errorf("openai completion has no usable output")
	}
```

### [internal/provider/sources/anthropic_source.go:98] usage 未解析 cache_read/cache_creation token，缓存用量统计丢失 [严重程度：低]
- 严重程度：低（条件触发：启用 Anthropic prompt caching 的账号，TokenUsage.InputCached 恒为 0，用量统计偏低；Python 原版解析 cache_read_input_tokens 填 input_cached（anthropic_source.py:441、448-449））
- 问题描述：非流式与流式的 usage 结构体都只有两个字段：

```go
// anthropic_source.go:98-101
		Usage struct {
			InputTokens  int `json:"input_tokens"`
			OutputTokens int `json:"output_tokens"`
		} `json:"usage"`
```

流式 message_start/message_delta 处（anthropic_source.go:199-203、214-217、265-268）同样只取这两个字段；`cache_read_input_tokens`、`cache_creation_input_tokens`（含 5 分钟写入量）被丢弃。对照 kimi_code_source.go:147、227-230 已正确解析 cache_read_input_tokens。
- 修复建议：

```go
		Usage struct {
			InputTokens              int `json:"input_tokens"`
			OutputTokens             int `json:"output_tokens"`
			CacheReadInputTokens     int `json:"cache_read_input_tokens"`
			CacheCreationInputTokens int `json:"cache_creation_input_tokens"`
		} `json:"usage"`
	// TokenUsage 赋值处：
	// InputOther:  result.Usage.InputTokens,
	// InputCached: result.Usage.CacheReadInputTokens,
```

### [internal/provider/sources/kimi_code_source.go:252] 流式 switch 缺少 error 事件分支，Anthropic 协议错误被静默忽略 [严重程度：低]
- 严重程度：低（条件触发：Kimi/Anthropic 协议端点在流中下发 `{"type":"error",...}` 事件时被忽略，最终以部分文本冒充成功；同包 anthropic_source.go:273-280 已处理，属同协议两实现不一致）
- 问题描述：TextChatStream 的事件 switch 只处理 message_start/content_block_start/content_block_delta/content_block_stop/message_delta/message_stop：

```go
// kimi_code_source.go:252-310（switch 头部）
			switch event.Type {
			case "message_start":
			...
			case "message_stop":
				return true
			}
			return false
```

event 结构体也没有解析 Error 字段。流错误事件到达后什么也不做，scan 继续直到流关闭，`!sentFinal` 逻辑（kimi_code_source.go:319-342）会把已累积的部分内容当正常 final 发出。
- 修复建议：

```go
			case "error":
				msg := "Kimi Code stream error"
				if event.Error != nil && event.Error.Message != "" {
					msg = event.Error.Message
				}
				ch <- &provider.LLMResponse{Role: "err", CompletionText: msg}
				return true
```

（同时给事件结构体补 `Error *struct{ Type, Message string }` 字段，照抄 anthropic_source.go:218-221。）

### [internal/provider/sources/rerank_source.go:91] TEI rerank 结果未按分数排序也未做 topN 截断，与注释及其他 rerank 实现不一致 [严重程度：低]
- 严重程度：低（条件触发：TEI 服务端忽略 top_n 参数或返回乱序结果时，调用方拿到超量/乱序结果；函数注释"returns the top-N results ordered by score"与实现不符，Python 原版在客户端做 `results[:top_n]` 截断（tei_rerank_source.py:110-111））
- 问题描述：

```go
// rerank_source.go:45-46 注释声称：
// Rerank reranks the documents and returns the top-N results ordered by score.
...
// rerank_source.go:84-98 实现只做直通映射：
	var ranked []struct {
		Index int     `json:"index"`
		Score float64 `json:"score"`
	}
	...
	results := make([]*provider.RerankResult, 0, len(ranked))
	for _, r := range ranked {
		results = append(results, &provider.RerankResult{
			Index:          r.Index,
			RelevanceScore: r.Score,
		})
	}
	return results, nil
```

同目录 bailian/nvidia/vllm/xinference 四个 rerank 都有 `sort.Slice(... > ...) + results[:topN]`（如 vllm_rerank_source.go:112-115），唯独 TEI 缺失；它只是把 top_n 放进请求体（rerank_source.go:57-59，Python 甚至不发这个字段），客户端无兜底。
- 修复建议：

```go
	sort.Slice(results, func(i, j int) bool { return results[i].RelevanceScore > results[j].RelevanceScore })
	if topN > 0 && len(results) > topN {
		results = results[:topN]
	}
	return results, nil
```

### [internal/provider/sources/gemini_source.go:73] 模型名未做路径安全校验直接拼入 URL（gemini_tts 同），与 gemini_embedding 的白名单校验不一致 [严重程度：低]
- 严重程度：低（模型名来自管理员配置，非远程可控，实际暴露面小；但 model 含 `/`、`?`、`#`、空格等字符时会拼出错误 URL 或注入查询段，且同包 gemini_embedding_source.go:42-46/57-67 已有 geminiModelPathSafe 白名单却未在聊天/TTS 两处复用）
- 问题描述：

```go
// gemini_source.go:73
	url := fmt.Sprintf("%s/models/%s:generateContent", s.apiBase, model)
// gemini_source.go:133
	url := fmt.Sprintf("%s/models/%s:streamGenerateContent?alt=sse", s.apiBase, model)
// gemini_tts_source.go:86
	url := fmt.Sprintf("%s/models/%s:generateContent", s.apiBase, s.model)
```

模型名既不 url.PathEscape 也不做字符白名单；配置里形如 `gemini-2.5-flash?alt=` 的值会篡改请求语义。API key 走 x-goog-api-key 头（gemini_source.go:62），无 key 泄漏风险，故仅定为低。
- 修复建议：

```go
	if !geminiModelPathSafe(model) {
		return nil, fmt.Errorf("invalid gemini model name: %q", model)
	}
	url := fmt.Sprintf("%s/models/%s:generateContent", s.apiBase, model)
```

（geminiModelPathSafe 已在 gemini_embedding_source.go:57 定义，直接复用即可。）

### [internal/provider/sources/openai_responses_source.go:751] Responses/Kimi 两 provider 的 doRequest 未接入 DoWithRetry，重试配置对其无效 [严重程度：低]
- 严重程度：低（条件触发：429/5xx/瞬时网络错误时这两个 provider 无重试直接失败，而用户配置的 request_max_retries 对其静默无效；Python 原版两文件均使用 retry_provider_request（openai_responses_source.py:348、417）；kimi_code_source.go:448-467 同样直连）
- 问题描述：

```go
// openai_responses_source.go:743-760（节选）
func (s *OpenAIResponsesSource) doRequest(ctx context.Context, body map[string]interface{}, client *http.Client) (*http.Response, error) {
	jsonBody, err := json.Marshal(body)
	...
	httpReq, err := http.NewRequestWithContext(ctx, "POST", url, bytes.NewReader(jsonBody))
	...
	return client.Do(httpReq)
}
```

同包 openai_source.go:375、anthropic_source.go:62、gemini_source.go:56、ollama_source.go:53 均通过 DoWithRetry + RetryConfigFromSettings 发送，仅这两个 provider 掉队（工厂每次重建 request 的机制已就绪，DoWithRetry 的 RequestFactory 参数正好适配）。
- 修复建议：

```go
	cfg := RetryConfigFromSettings(s.Settings())
	return DoWithRetry(ctx, client, func() (*http.Request, error) {
		httpReq, err := http.NewRequestWithContext(ctx, "POST", url, bytes.NewReader(jsonBody))
		if err != nil {
			return nil, err
		}
		httpReq.Header.Set("Content-Type", "application/json")
		httpReq.Header.Set("Authorization", "Bearer "+s.apiKey)
		for k, v := range s.customHeaders {
			httpReq.Header.Set(k, v)
		}
		return httpReq, nil
	}, cfg, "OpenAIResponses")
```

### [internal/provider/sources/tts_helpers.go:121] TTS 音频落盘 io.Copy 无大小上限，异常端点可写满磁盘 [严重程度：低]
- 严重程度：低（条件触发：TTS 端点返回超大或无限流响应时（恶意/异常端点、代理故障页伪装 200），io.Copy 无限写入 data/temp 直至磁盘耗尽；STT 侧同文件模式已有 maxAudioBytes 50MB 上限（stt_source.go:157），TTS 侧缺失；tts_source.go:95 的 openai_tts GetAudio 同样直接 io.Copy(resp.Body, f)）
- 问题描述：

```go
// tts_helpers.go:111-125（节选）
	f, err := os.Create(path)
	if err != nil {
		return "", err
	}
	if _, err := io.Copy(f, r); err != nil {
		_ = f.Close()
		_ = os.Remove(path)
		return "", err
	}
```

正常 TTS 输出（几十秒语音几 MB）远小于上限，加限不影响功能；minimax（minimax_tts_source.go:140-162）与 edge_tts（edge_tts_source.go:127-148）还在内存中无上限累积 audio buffer，属同类问题。
- 修复建议：

```go
	const maxTTSBytes = 100 << 20 // 100MB，足够任意长语音
	n, err := io.Copy(f, io.LimitReader(r, maxTTSBytes+1))
	if err != nil {
		_ = f.Close()
		_ = os.Remove(path)
		return "", err
	}
	if n > maxTTSBytes {
		_ = f.Close()
		_ = os.Remove(path)
		return "", fmt.Errorf("tts audio exceeds %d bytes", maxTTSBytes)
	}
```

### [internal/provider/sources/openai_source.go:126] chat 类 provider 错误响应体 io.ReadAll 无上限且全量回显给用户 [严重程度：低]
- 严重程度：低（条件触发：上游返回超大错误体（网关 HTML 错误页、恶意中转）时全量读入内存，并把整段错误体塞进 CompletionText 经 pipeline 原样发给聊天用户；同包 TTS/embedding/rerank 全部用 `io.ReadAll(io.LimitReader(resp.Body, 1024/2048))` 截断，chat 类六个文件未对齐）
- 问题描述：

```go
// openai_source.go:125-131
	if resp.StatusCode != 200 {
		respBody, _ := io.ReadAll(resp.Body)
		return &provider.LLMResponse{
			Role:           "err",
			CompletionText: fmt.Sprintf("API error %d: %s", resp.StatusCode, string(respBody)),
		}, nil
	}
```

同模式还有 openai_source.go:99/192、anthropic_source.go:83/146、gemini_source.go:87/146、ollama_source.go:72/108、openai_responses_source.go:139/166/191、kimi_code_source.go:100/127/198。pipeline 收到 Role=="err" 后把 CompletionText 作为错误消息返回给发起用户（stages.go:1913-1923），超大错误体可能直接刷屏或携带网关内部信息。
- 修复建议：

```go
	if resp.StatusCode != 200 {
		respBody, _ := io.ReadAll(io.LimitReader(resp.Body, 2048))
		return &provider.LLMResponse{
			Role:           "err",
			CompletionText: fmt.Sprintf("API error %d: %s", resp.StatusCode, truncate(string(respBody), 1024)),
		}, nil
	}
```

（truncate 已在 retry.go:160 定义。）

---

# Part 10：Astrbot-golang/dashboard 前端（Vue3 + TS，约 228 文件，排除 node_modules/dist 及 src/api/generated 自动生成代码）

### [dashboard/src/api/http.ts:33-45] 全局请求拦截器把 Bearer Token 发送到任意第三方域名 [严重程度：高]
- 严重程度：高
- 问题所在行号：http.ts:33-45（axios 拦截器）、108-131（monkey-patch `window.fetch`）、PluginDetailPage.vue:635-639（触发点）
- 问题描述：`setupHttpClient()` 对 axios 全局默认实例和 `window.fetch` 打补丁，凡是存在 token，任何请求都会被附加 `Authorization: Bearer <token>`，且无同源/白名单限制。而 `PluginDetailPage.vue` 的 `fetchRemoteMarkdown` 会直接 `axios.get(url)` 拉取来自插件市场元数据的 `readme_url`（第三方可控内容）。恶意/被投毒的市场插件只需把 `readme_url` 指向自己的服务器，即可收集所有安装者的仪表盘 JWT——凭据泄露，攻击者可凭 token 完全接管仪表盘。
- 修复建议：拦截器按请求 URL 判断，仅对同源（或已知 API 前缀）附加 Authorization；外部链接改用不带拦截器的裸 fetch 并校验协议：

```ts
function attachAxiosHeaders(config: InternalAxiosRequestConfig) {
  const url = config.url || '';
  const isSameOrigin =
    !/^([a-z][a-z\d+\-.]*:)?\/\//i.test(url) ||
    new URL(url, window.location.origin).origin === window.location.origin;
  const token = isSameOrigin ? getToken() : null;
  ...
}
```

### [dashboard/src/api/v1.ts:803-831] 长期 Token 作为 URL 查询参数传输（WS/备份下载回退路径） [严重程度：中]
- 严重程度：中
- 问题所在行号：api/v1.ts:804、821-830；useMessages.ts:833-836；BackupDialog.vue:797-801；LiveMode.vue:287-289
- 问题描述：ws-ticket 机制失效（旧后端/网络异常返回空串）时回退 `?token=<长期JWT>`，token 会出现在服务端访问日志、反向代理日志、浏览器历史中。备份下载 `downloadUrl(filename, token)` 同样把长期 token 放进 URL。
- 修复建议：短期票据不可用时改为弹窗提示用户升级后端，而不是回退长期 token；备份下载改走 Authorization 头 + blob 下载：

```ts
const res = await apiV1Client.get(`/backups/${encodeURIComponent(filename)}`, { responseType: 'blob' });
const url = URL.createObjectURL(res.data); // 再触发 a[download]
```

### [dashboard/src/router/index.ts:30-54] 路由守卫权限判断仅依赖前端 token 存在性，且两套判断来源易脱节 [严重程度：中]
- 严重程度：中（以前端为唯一防线时为高；此处假定后端有校验）
- 问题所在行号：router/index.ts:36-53
- 问题描述：鉴权只检查 `auth.has_token()`（sessionStorage 中是否有字符串），不校验 token 有效性与过期。守卫逻辑不一致：`authRequired` 由 `publicPages.includes(to.path)` 计算，但真正生效的分支是 `to.matched.some(record => record.meta.requiresAuth)`——若某路由（如 ChatBoxRoutes 下路由）漏配 `meta.requiresAuth`，即使不在 publicPages 也会被 `next()` 放行。
- 修复建议：统一以 `meta.requiresAuth` 为唯一来源，删除 `publicPages`/`authRequired` 冗余判断；token 有效性交由后端 401 拦截（http.ts 已做）兜底，并在文档明确"前端守卫仅是 UX，不是安全边界"。

### [dashboard/src/components/chat/ThreadPanel.vue:207-226] readSseStream 函数在两处完整复制 [严重程度：中]
- 严重程度：中（质量：代码复制）
- 问题所在行号：useMessages.ts:1405-1433 与 ThreadPanel.vue:207-226
- 问题描述：SSE 解析循环（reader/decoder/split("\n\n")/JSON.parse）逐行重复实现，两份代码未来修 bug（如 CRLF 兼容）时极易只改一处，造成流式消息在不同入口行为不一致。
- 修复建议：抽取到 `src/utils/sse.ts` 导出共享 `readSseStream(body, onPayload)`，两处 import。

### [dashboard/src/layouts/full/vertical-header/VerticalHeader.vue] 单文件 2500 行的"上帝组件" [严重程度：中]
- 严重程度：中（函数长度/职责拆分）
- 问题所在行号：全文件；登录会话处理集中在 420-530、925-1003 附近
- 问题描述：同时承担密码修改对话框、TOTP、版本检查、GitHub 代理选择、更新检查、路由记忆、预发布开关等至少 7 个职责。同类问题：Chat.vue 2480 行、SessionManagementPage.vue 1984 行、Settings.vue 1881 行。
- 修复建议：按职责拆出 `usePasswordChange()`、`useUpdateChecker()`、`useRouteMemory()` 等 composable，把改密对话框抽成独立组件。

### [dashboard/src/components/shared/Logo.vue:32-39] v-html 渲染未经净化的 title prop [严重程度：低]
- 严重程度：低
- 问题所在行号：Logo.vue:10、32-39
- 问题描述：`formatTitle` 直接把 `title` 字符串（含插入的 `<wbr>`）通过 `v-html` 渲染，无 DOMPurify。当前唯一调用方传 i18n 静态文案，暂无实际注入面；但该组件是公共组件，任何后续调用方传入用户/服务端可控文本即成 XSS。
- 修复建议：不用 v-html，改为模板内 `<h2>{{ part1 }}<wbr /> {{ part2 }}</h2>`，或 DOMPurify.sanitize 且 ALLOWED_TAGS 仅含 wbr。

### [dashboard/src/layouts/full/vertical-header/VerticalHeader.vue:451-854] 错误仅 console.log，用户无感知且可能泄露响应细节 [严重程度：低]
- 严重程度：低
- 问题所在行号：451、515、553、557、562、578、795、854 等（全仓 44 处 console.log）
- 问题描述：多处 `catch (err) { console.log(err); }`——无 UI 反馈，生产环境把完整 error（含 axios response body）打到控制台，可能暴露后端内部信息，且 console.log 无法被日志级别过滤。
- 修复建议：统一错误上报工具，用户操作类错误给出 toast 提示，用 console.error 并附上下文。

### [dashboard/src/utils/token.ts:3-10] Token 存于 sessionStorage，XSS 可直接窃取 [严重程度：低]
- 严重程度：低（已知权衡，与上两条配合阅读）
- 问题所在行号：token.ts:4、9
- 问题描述：JWT 存 sessionStorage，任何成功的 XSS（如未来 v-html 误用、问题 1 场景）都可 `sessionStorage.getItem('token')` 直接拿走会话。sessionStorage（而非 localStorage）缩小了持久窗口，是合理折中，前提是 XSS 面被严格控制。
- 修复建议：保持现状可接受；更进一步可在后端支持短期 access token + refresh cookie（HttpOnly），前端无长效 token 可偷。

Part 10 值得肯定的实现（非问题）：Markdown/HTML 渲染统一 DOMPurify 白名单净化（ReadmeDialog/PluginDetailPage），外链加 noopener，渲染竞态用 requestId 计数器防护；HtmlGenUiNode 把 LLM 生成 HTML 放入 `sandbox="allow-scripts"`（无 allow-same-origin）iframe + CSP `default-src 'none'` 隔离；safeUrl 用 `new URL()` 仅放行 http/https；抽查 JSON.parse 全部有 try/catch 守卫；`while(true)` SSE 读取均有退出条件；WebSocket 按 sessionId 复用连接、pending 去重；wsTicket 一次性票据方向正确。

---

# Part 11：AstrbotDev/astrbot-go-plugin-sdk（Go 插件 SDK，7 个源文件 + 测试快读，排除 gen/、proto/ 生成代码）

### [types.go:243-267] SessionWait 注销用 UMO 冒充 wait_id，宿主侧等待可能无法注销 [严重程度：中]
- 严重程度：中
- 问题所在行号：types.go:246-249（注册）、261-266（注销）
- 问题描述：`RegisterSessionWait` 上报宿主后，宿主通过 HostServiceHooks 返回生成的 `wait_id`（host.go:425 注释明确），但 SDK 丢弃了 `resp.WaitId`；注销时却把 UMO 当作 WaitId 发送。若宿主侧等待注册表按生成的 wait_id 键控（proto 注释即如此约定），注销永远命中不到条目，宿主侧等待泄漏至超时，同一 UMO 多次注册/注销语义错乱。
- 修复建议：保存宿主返回的 wait_id 并在注销时使用：

```go
// SessionWait 增加 waitID 字段
type SessionWait struct {
    UMO     string
    Handler func(e *Event) bool
    waitID  string // 宿主返回的 wait_id
}
// RegisterSessionWait 中：
if resp, rerr := svc.RegisterSessionWait(ctx, ...); rerr == nil {
    w.waitID = resp.GetWaitId()
}
// UnregisterSessionWait 中：
if wid := w.waitID; wid != "" { req.WaitId = wid } else { req.WaitId = umo }
```

### [types.go:233-253 + serve.go:856-866] 等待被消费后未通知宿主注销，宿主侧条目泄漏 [严重程度：中]
- 严重程度：中
- 问题所在行号：serve.go:856（takeSessionWait 一次性消费）、types.go:273-285
- 问题描述：`FeedSessionWait` 中 `takeSessionWait(umo)` 只消费插件本地等待，插件侧条目已删除，但宿主的等待注册表条目仍在（直到超时）。此后同 UMO 每条消息宿主仍会推送一次 FeedSessionWait RPC；若插件又注册了新等待，宿主旧条目可能与新条目重复触发。数据一致性缺陷。
- 修复建议：FeedSessionWait 成功消费后异步回叫宿主注销：

```go
if w := s.impl.takeSessionWait(umo); w != nil && w.Handler != nil {
    // ... 原有处理 ...
    go s.impl.unregisterHostWait(umo) // 内部调 UnregisterSessionWait RPC，失败仅日志
}
```

### [host.go:1115-1155] 控制面鉴权只验证"身份存在"未做权限分级：任意插件可禁用/卸载/安装其他插件 [严重程度：中]
- 严重程度：中
- 问题所在行号：host.go:1115（SetPluginEnabled）、1129（InstallPlugin）、1143（UninstallPlugin）、496-501（requireIdentity）
- 问题描述：`requireIdentity` 只要求 `s.pluginID != ""`，不区分授权级别。任何第三方插件都能调用 `SetPluginEnabled(其他插件, false)`、`UninstallPlugin`、`InstallPlugin`（repo 参数还可触发宿主 git clone 任意源）。对比 GetConfig/SetConfig 做了归属校验，这三处对目标插件名完全没有归属/白名单校验——"防止匿名插件接管宿主插件生态（26-2）"的目标只实现了一半。
- 修复建议：为破坏性操作加宿主侧开关，默认关闭高危 RPC：

```go
// HostServiceHooks 增加开关
AdminOpsEnabled bool
// 三个入口处：
if !h.AdminOpsEnabled {
    return nil, status.Error(codes.PermissionDenied, "plugin management RPCs are disabled for plugins")
}
```

### [host.go:795-803] SetChatLLMRateLimit 只调整 ChatLLM 限流表，CallAction 限流无法配置且语义混用 [严重程度：低]
- 严重程度：低
- 问题所在行号：host.go:790、795-803
- 问题描述：`callActionRate` 与 `chatLLMRate` 共用 `maxChatLLMPerMinute` 常量初始化，但 setter 只改 `chatLLMRate.maxPer`；且 `perMinute <= 0` 时静默忽略，与注释"<=0 表示关闭限流"矛盾——传 0 并不会关闭限流。
- 修复建议：拆分 `SetChatLLMRateLimit` 与新增 `SetCallActionRateLimit`；若要支持关闭，引入 disabled 字段而非依赖 maxPer<=0。

### [serve.go:995-1000] Web 请求 500 响应直接回显 handlerErr.Error()，可能泄露内部路径/细节 [严重程度：低]
- 严重程度：低
- 问题所在行号：serve.go:995-1000
- 问题描述：把插件 handler 的原始错误（可能含文件路径、SQL 片段、内部地址）原样作为 HTTP 响应体返回给 dashboard 前端；safeErr 捕获 panic 时还附带完整 goroutine 堆栈进入响应体。
- 修复建议：对外只返回通用消息，详情写日志：

```go
if handlerErr != nil {
    logService().Error("HandleWebRequest handler error", "plugin", s.impl.Name, "err", handlerErr)
    errBody, _ := json.Marshal(map[string]string{"status": "error", "message": "internal plugin error"})
    ...
}
```

### [serve.go:549-551] invokeResultHook 静默丢弃 chain_json 解码错误 [严重程度：低]
- 严重程度：低
- 问题所在行号：serve.go:550
- 问题描述：`_ = json.Unmarshal(req.ChainJson, &chain)` 与同文件 decodePayload（失败打 Warn）和 client.go:197 风格不一致，result 装饰钩子收到损坏的入站链时完全静默。
- 修复建议：解码失败打 Warn，或复用 decodePayload。

### [serve.go:989-990] WebAPI.Handler 不接收 context，长耗时 handler 无法被取消 [严重程度：低]
- 严重程度：低
- 问题所在行号：serve.go:989-990、types.go:181
- 问题描述：宿主侧 30s RPC 超时后放弃等待，但插件进程内 `w.Handler(...)` 仍会继续跑完（goroutine 泄漏直至自然结束），handler 无法感知取消。Web 请求（可能做外部 HTTP 调用）最需要 ctx。
- 修复建议：Handler 签名加入 `ctx context.Context`（或加 HandlerCtx 字段过渡），把 RPC 的 ctx 传进去。

### [host.go:106-108] 反向 Host RPC 固定 30s 超时且不可透传调用方 context [严重程度：低]
- 严重程度：低
- 问题所在行号：host.go:102、106-108
- 问题描述：`hostRPCCtx()` 固定 `context.Background()+30s`。ChatLLM 调用（长思考模型）和 HtmlRender 经常超过 30s；插件 handler 本身已在宿主 30s 超时内运行，嵌套后实际可用时间更短，且插件无法延长或提前取消。注释称"与 python-sdk 的 30-180s 对齐"，但只取了下限。
- 修复建议：提供带 ctx 的变体并放大 ChatLLM/HtmlRender 默认值（如无 deadline 则 WithTimeout(ctx, 120s)）。

### [host.go:1208-1216] UnregisterBridgeHook 缺少身份校验，与 RegisterBridgeHook 不对称 [严重程度：低]
- 严重程度：低
- 问题所在行号：host.go:1208-1216
- 问题描述：`RegisterBridgeHook` 要求身份非空（FailedPrecondition），`UnregisterBridgeHook` 却不检查；未绑定身份的连接可用空 pluginID 注销钩子，宿主按名字删除可能误删/空操作。
- 修复建议：入口加 `if err := s.requireIdentity(); err != nil { return nil, err }`。

### [host.go:596-611] SetCurrentHostPluginID 依赖宿主串行加载，存在身份错绑窗口 [严重程度：低]
- 严重程度：低
- 问题所在行号：host.go:596-611、544-551
- 问题描述：全局 `hostPluginID` + `hostServers` 按 manifest id 键控。若宿主并发 Dispense 两个插件（或 manifest id 相同/为空），accept 时的 pid 可能取到另一个插件的 id，导致 GetConfig/SetConfig 的身份隔离被张冠李戴。pid 为空时 BindHostServiceName 静默 no-op，无任何日志提示。
- 修复建议：改为显式参数传递而非全局变量；至少在 acceptHostService 中对 pid=="" 打 Warn，并在宿主侧对 Dispense 加互斥。

### [serve.go:151-162] 预连接 goroutine 失败后无日志不可观测 [严重程度：低]
- 严重程度：低
- 问题所在行号：serve.go:153-162
- 问题描述：后台循环重试 20 次×250ms（最多 5 秒）后静默放弃，无任何 Warn；注释声称"循环重试直到缓存成功"与实际不符，排查反向调用超时缺线索。
- 修复建议：循环结束后加 `logService().Warn("HostService 预连接失败，将退回惰性拨号")`。

### [serve.go:879-935] webRouteCache 为包级全局且无失效机制 [严重程度：低]
- 严重程度：低
- 问题所在行号：serve.go:878-883、891-935
- 问题描述：缓存假设"路由集合在 Register 后固定"，但 `impl.WebAPIs` 是可变字段且 HandleWebRequest 每次实时读取；若插件运行时修改 WebAPIs，旧正则仍被复用。单插件单进程下影响小，属脆弱设计。
- 修复建议：文档化"WebAPIs 必须在 Serve 前固定"，或以 []WebAPI 快照为 key。

### [serve.go:366-367] 命令未命中时返回空 200 而非错误，与 HandleTool 未命中行为不对称 [严重程度：低]
- 严重程度：低
- 问题所在行号：serve.go:366-367（对比 794-796）
- 问题描述：`HandleCommand` 找不到命令时只打日志返回空响应（Handled 缺省 false），`HandleTool` 未找到返回 IsError=true，两处语义不一致，宿主侧易引入分支遗漏。
- 修复建议：统一返回 codes.NotFound 或都返回显式未命中标记。

Part 11 Python 原版对照结论：钩子事件集合是 Python 14 个 register_* 钩子的超集（events.go:7-13 已声明）；LLMResponse/ToolCall/PluginError JSON 字段名与 Python _bridge/dispatch.py 消费一致，跨语言载荷可互通；session_waiter 行为等价但注销/消费回告不闭环（见两个中危）；permission 归一化与 Python 侧一致；EventResult 的 Sent 语义缺口已被文档化。

---

# Part 12：AstrbotDev/astrbot-golang-plugin-python-sdk 一方 Python 代码（astrbot/ SDK 核心 + tests/，排除第三方捆绑库 aiocqhttp/botpy/slack_sdk/telegram/mcp、proto 生成代码、temp/）

对照 /home/xiaowei/AstrBot-py/ 原版移植语义审查。无 SQL/eval/pickle 反序列化风险面（数据层全部为宿主 RPC 降级 no-op）。

### [astrbot/_bridge/dispatch.py:875-917] 异步 Web handler 中 request 上下文丢失（ContextVar 跨线程不传播） [严重程度：高]
- 严重程度：高
- 问题所在行号：875-923（_run_web_handler），配合 _bridge/loop.py:40-49（run_coro）
- 问题描述：`_run_web_handler` 在 gRPC 线程上执行 `_cv_app.set(...)`、`_cv_request.set(fake)` 和 `bind_request_context(plugin_req)`，随后对协程走 `asyncio.run_coroutine_threadsafe(coro, loop)`。任务在常驻 loop 线程以其默认 context 创建，不会复制 gRPC 线程当前 context，因此协程内部 `from quart import request` 拿不到注入的 FakeQuartRequest（抛 LookupError/RuntimeError）。所有在 Web API handler 里 async def + 访问 request/json/session 的插件都会失败；只有同步 handler 能正常读到上下文。确定性功能缺陷。
- 修复建议：把 ContextVar 的 set 移到协程内部执行，在常驻 loop 上运行整个"注入+调用+重置"：

```python
async def _run_in_loop():
    cv_tokens = []
    try:
        from quart.globals import _cv_app, _cv_request
        cv_tokens.append((_cv_app, _cv_app.set(FakeQuartAppCtx())))
        cv_tokens.append((_cv_request, _cv_request.set(fake)))
    except Exception:
        pass
    try:
        with bind_request_context(plugin_req):
            bound = _bind(handler, self.inst)
            return _call_async_only(bound, **dict(path_params))
    finally:
        for var, token in reversed(cv_tokens):
            var.reset(token)

result = loop.run_coro(_run_in_loop(), timeout=HANDLER_TIMEOUT)
```

### [astrbot/_bridge/dispatch.py:424-462] HandleFilter 注册表 miss 时跳过全部过滤器直接调用 handler（权限过滤可被绕过） [严重程度：中]
- 严重程度：中
- 问题所在行号：431-448
- 问题描述：`md = star_handlers_registry.get_handler_by_full_name(request.name)`；`if md is not None:` 才逐个执行 PermissionTypeFilter/EventMessageTypeFilter 等过滤器。当 md is None（注册表被后续卸载/名字不一致等）时代码直接落到 `bound = _bind(handler, self.inst); results = _call(bound, event)`，admin 权限、事件类型、平台过滤全部失效，handler 无条件执行——鉴权旁路（fail-open）。
- 修复建议：注册表 miss 时拒绝执行：

```python
md = star_handlers_registry.get_handler_by_full_name(request.name)
if md is None:
    logger.warning(f"HandleFilter: 找不到 handler 元数据 {request.name}，拒绝执行")
    return plugin_pb2.HandleFilterResponse(allow=True)  # 放行管线，但不执行该 handler
```

### [astrbot/_bridge/dispatch.py:384, 733-736] 插件异常原文直接返回给聊天用户，未脱敏 [严重程度：中]
- 严重程度：中
- 问题所在行号：384（`resp.text = f"插件执行失败: {e}"`）、734-736（`text=f"工具 {request.name} 执行失败: {e}"`）
- 问题描述：异常消息常包含内部路径、上游 API 报文（含 api_key=...、Bearer ...、sk-...）。项目已有 astrbot/core/utils/error_redaction.py::safe_error 却未在此使用，敏感信息可能被推送到群聊。
- 修复建议：

```python
from astrbot.core.utils.error_redaction import safe_error
resp.text = safe_error("插件执行失败: ", e)
```

### [astrbot/core/message/components.py:747-766, 115-125] File.file 属性在事件循环线程上同步阻塞下载 [严重程度：中]
- 严重程度：中
- 问题所在行号：759、764（_download_sync），115-125（实现）
- 问题描述：`_download_sync` 注释声称"避免冻结事件循环"，但实现是 `ex.submit(_download_to_temp, ...).result()`——.result() 无超时阻塞调用线程；当 `File.file` 在 async handler（常驻 loop 线程）中被访问时，事件循环仍被阻塞最长 30s，期间所有协程停摆。且每次调用新建一个 ThreadPoolExecutor。
- 修复建议：复用模块级线程池并加超时，文档引导 async 代码使用 `await file_seg.get_file()`：

```python
_DL_POOL = concurrent.futures.ThreadPoolExecutor(max_workers=4, thread_name_prefix="dl")
def _download_sync(url, suffix=""):
    try:
        asyncio.get_running_loop()
    except RuntimeError:
        return _download_to_temp(url, suffix)
    return _DL_POOL.submit(_download_to_temp, url, suffix).result(timeout=35)
```

### [astrbot/core/utils/message_history_manager.py:190-243] async insert 持线程锁做全量 JSON 重写，阻塞事件循环且 O(n) 放大 [严重程度：中]
- 严重程度：中
- 问题所在行号：224-242（insert 锁内 _load + _save）、167-183（_save 全文件重写）
- 问题描述：insert 是 async 方法却在事件循环线程上直接 `with self._lock:` → 同步读盘（首次 _load）+ json.dump 整个缓存（默认每会话 2000 条 × 全部会话）+ os.replace。每条消息触发一次全量序列化与写盘（大对象频繁创建、IO 阻塞、无增量/批量缓存），高频消息下事件循环被明显阻塞。
- 修复建议：写盘移出事件循环并做写合并，简单方案与 shared_preferences 的 put_async 一致：

```python
async def insert(self, ...):
    await asyncio.to_thread(self._insert_sync, ...)
```

### [astrbot/core/utils/shared_preferences.py:151-169] get_async 在事件循环线程持锁做同步文件 IO；_persist 每次 put 全量 deepcopy 重写 [严重程度：中]
- 严重程度：中
- 问题所在行号：165-169（get_async）、135-149（_ensure_cache 首次读盘）、151-163（_persist）
- 问题描述：get_async 是 async 方法，却在事件循环线程上 `with _lock:` 并调用 _ensure_cache()——首次调用在循环线程同步读盘解析整个 JSON；锁竞争时阻塞循环。_persist 每次 put/remove 都对全缓存 deepcopy + json.dump 全量重写，键多时每次写入代价 O(全部偏好)。
- 修复建议：读路径首次初始化移到子线程，写路径去 deepcopy：

```python
async def get_async(self, scope, scope_id, key, default=None):
    if not self._cache_initialized:
        await asyncio.to_thread(self._ensure_cache)
    with _lock:
        value = self._cache.get((scope, scope_id, key), _MISSING)
        return default if value is _MISSING else deepcopy(value)

def _persist(self) -> None:
    with _lock:
        raw = {json.dumps(k[0], ensure_ascii=False):
               {"scope": k[0], "scope_id": k[1], "key": k[2], "value": v}
               for k, v in self._cache.items()}
        self._save(raw)   # 序列化本身即生成新对象，无需再 deepcopy
```

### [astrbot/core/utils/message_history_manager.py:307, 325] delete_by_id / update 对非数字 id 记录抛未捕获 ValueError [严重程度：低]
- 严重程度：低
- 问题所在行号：307、325
- 问题描述：`int()` 对畸形记录（id 为非数字字符串，如外部插件手改过的 JSON）抛 ValueError，异常在锁内且未捕获，整个 RPC 失败。
- 修复建议：抽安全转换函数 _safe_id，try/except (TypeError, ValueError) 返回 -1。

### [astrbot/_bridge/host.py:156-166] set_config 无异常处理，且无条件返回 True [严重程度：低]
- 严重程度：低
- 问题所在行号：156-166
- 问题描述：同文件其他方法均 try/except 降级，set_config 例外：gRPC RpcError 直接抛给插件，且恒 return True，调用方无法感知失败——数据一致性风险（插件以为保存成功）。
- 修复建议：对齐其他方法模式，except 后 logger.warning 并 return False。

### [astrbot/_bridge/dispatch.py:997-1002] FakeQuartRequest 的 url/query_string/host 为错误占位值 [严重程度：低]
- 严重程度：低
- 问题所在行号：997-1001
- 问题描述：插件 Web handler 里 request.url、request.query_string、request.host 拿到空串或裸 path，与 quart 真实语义不符，做签名校验/日志/回调 URL 拼接的插件会静默出错。
- 修复建议：用已有 pairs 还原：

```python
from urllib.parse import urlencode
self.query_string = urlencode(plugin_req.query.multi_items())
self.url = self.path + ("?" + self.query_string if self.query_string else "")
self.host = plugin_req.headers.get("host", "")
```

### [astrbot/_bridge/dispatch.py:1080-1099] FakeMultiDict.get 取"最后一次出现"，与 werkzeug/PluginMultiDict 语义相反 [严重程度：低]
- 严重程度：低
- 问题所在行号：1088（`for k, v in reversed(self._pairs)`）
- 问题描述：werkzeug 的 MultiDict.get 返回第一次出现的值，api/web.py 的 PluginMultiDict.get 也按正向首次匹配实现；FakeMultiDict 用 reversed 取最后一个。同名参数多次提交时 quart.request.args.get 与 astrbot.api.web.request.query.get 结果不一致。
- 修复建议：去掉 reversed，正向遍历取首个匹配，与 PluginMultiDict 对齐。

### [astrbot/core/utils/session_waiter.py:298-319] 同一 filter 实例复用时 FILTERS 清理不净（泄漏或误删） [严重程度：低]
- 严重程度：低
- 问题所在行号：314（FILTERS.append）、205-211（_cleanup 只 remove 一次）
- 问题描述：若插件把同一个 session_filter 实例传给多次并发/嵌套等待（filter 常为模块级对象），FILTERS 会被 append 多次而 _cleanup 只 remove 一份，且先结束的会话会把仍被另一会话使用的 filter 移除，全局 FILTERS 逐渐泄漏或误删。
- 修复建议：_cleanup 只在 USER_SESSIONS 中无其他 waiter 使用该 filter 时才移除：

```python
in_use = any(w.session_filter is self.session_filter for w in USER_SESSIONS.values())
if not in_use:
    try:
        FILTERS.remove(self.session_filter)
    except ValueError:
        pass
```

### [astrbot/core/conversation_mgr.py:355-377] get_human_readable_context 丢弃末尾未跟 assistant 回复的连续 user 消息 [严重程度：低]
- 严重程度：低
- 问题所在行号：357-377
- 问题描述：循环只在 role == "assistant" 分支归组，循环结束后残留的 temp_contexts（最后一条 assistant 之后的所有 user 消息——恰是最近一次提问）未并入结果。已对照原版 AstrBot-py 同函数，属"忠实移植了原版缺陷"，可在 SDK 侧顺手修复。
- 修复建议：循环后补 `if temp_contexts: contexts_groups.insert(0, temp_contexts)`。

# Part 07：Astrbot-golang/internal/star + db + sandbox + pysdk（插件API层 / 数据库层 / 沙箱 / Python桥接）

统计：发现 14 个问题（高 0 / 中 4 / 低 10）。

### [internal/star/registry.go:325] star 注册表 handler 字段的锁保护失效：管线无锁遍历 EventFilters 与 dashboard 写锁写入构成数据竞争 [严重程度：中]
- 严重程度：中（触发条件：消息处理高峰期，dashboard 用户在 WebUI 保存指令权限/启停/重命名——handlers.go:7278 调 SetHandlerPermission、lifecycle 启动注入 ApplyCommandConfigs 亦写入；数据竞争可导致 torn slice 读取 panic 使整个消息管线崩溃，`go test -race` 可稳定检出）
- 问题描述：`StarHandlerRegistry` 的互斥锁只保护 `handlers` 切线本身，`All()`/`GetFilterHandlers()` 在 RLock 内做浅拷贝后即释放锁返回共享指针。dashboard 侧的写入者正确持锁：

```go
// registry.go:317-332
func setPermissionLocked(h *StarHandlerMetadata, perm string) {
	...
	for i, filter := range h.EventFilters {
		if _, ok := filter.(*PermissionFilter); ok {
			h.EventFilters[i] = NewPermissionFilter(target)  // 写共享 slice
			return
		}
	}
	h.EventFilters = append(h.EventFilters, NewPermissionFilter(target))  // 追加共享 slice
}
```

但读取者全部不持锁。消息管线在 `GetFilterHandlers()` 返回后无锁遍历（stages.go:1243 `for _, filter := range handler.EventFilters`，stages.go:1222 读 `handler.Enabled`）；dashboard 的 `CollectCommandDescriptors` 同样无锁读（command_descriptors.go:23 与 40 两处遍历 `h.EventFilters`）。registry.go:304-313 的 `Mutate` 注释声称"与消息管线的 GetFilterHandlers（RLock 遍历）互斥"——但 GetFilterHandlers 的 RLock 在拷贝切片后就释放了，后续的 EventFilters/Enabled 访问完全在锁外，声称的互斥并不存在。`Enable/Disable`（registry.go:287-302）写 `h.Enabled`、`RenameCommand`（command.go:431）读 `handler.EventFilters` 也都遵循同样的"写者持锁、读者裸奔"模式。并发写 slice header/底层数组与并发读是教科书式 data race。
- 修复建议：

```go
// 方案一（改动最小）：给 StarHandlerMetadata 增加快照读入口，所有字段访问都走注册表锁，
// 使 Mutate/Enable/Disable/SetHandlerPermission 与管线读取真正互斥。
// registry.go 增加：
func (r *StarHandlerRegistry) SnapshotFilters(fullName string) ([]HandlerFilter, bool, int) {
	r.mu.RLock()
	defer r.mu.RUnlock()
	h, ok := r.handlersMap[fullName]
	if !ok {
		return nil, false, 0
	}
	filters := make([]HandlerFilter, len(h.EventFilters))
	copy(filters, h.EventFilters)
	return filters, h.Enabled, true
}

// stages.go findMatchingHandlers 改为经快照读取：
for _, handler := range all {
	filters, enabled, ok := registry.SnapshotFilters(handler.HandlerFullName)
	if !ok || !enabled {
		continue
	}
	for _, filter := range filters { ... }
}
// command_descriptors.go 的两处 EventFilters 遍历同理改为 SnapshotFilters。
```

### [internal/sandbox/manager.go:205] LocalBooter 相比 Python 缺失危险命令黑名单，且 docker 不可用时被静默选中为沙箱后端 [严重程度：中]
- 严重程度：中（触发条件：管理员启用 computer_use_runtime=sandbox 且 sandbox.booter 为 boxlite/未知类型，同时宿主无 docker——lifecycle 在启动时自动降级；此后 LLM 工具调用（astrbot 沙箱 shell/python，可被用户消息提示注入驱动）以宿主完整权限执行任意 shell，且无 rm -rf/mkfs/dd 等破坏性命令拦截、无输出上限、无资源限制。LocalBooter 注释自述"仅用于开发/测试场景"，但 lifecycle 的自动回退让生产部署实际用上它）
- 问题描述：Go 的 `LocalBooter.Exec` 直接 `exec.CommandContext(ctx, cmd, args...)` 执行，无任何命令过滤：

```go
// sandbox/manager.go:203-214
	// #nosec G204 -- 本地沙箱执行核心：cmd/args 由 Booter 从沙箱操作（LLM 给定的工具调用）构造，
	// 环境变量已最小化（localBooterEnv 剔除敏感变量），工作目录限定在沙箱映射路径内。
	c := exec.CommandContext(ctx, cmd, args...) // nosemgrep: ...
	c.Dir = dir
	...
	err = c.Run()
```

上游 Python 的 `LocalBooter`（booters/local.py:31-44）带有 `_BLOCKED_COMMAND_PATTERNS`（` rm -rf `、` mkfs`、` dd if=`、` shutdown`、` sudo `、fork 炸弹等）拦截 `PermissionError("Blocked unsafe shell command.")`，并强制 `timeout` 与 `max_output_chars`（local.py:233 默认 10_000）。Go 版三道防线全部缺失。更关键的是选择逻辑：Python 的 computer_client.py:635-641 在 boxlite boot 失败（含 docker 不可用）时抛错、绝不降级到本地执行；Go 的 lifecycle.go:498-501 则静默回退：

```go
// lifecycle.go:498-501
	if dockerAvailable() {
		l.sandboxMgr.SetBooter(sandbox.NewDockerBooter(image))
		...
		return
	}
	l.sandboxMgr.SetBooter(sandbox.NewLocalBooter())
	logger.I18nInfo("沙盒启动器已设置（local，docker 不可用）")
```

用户显式选择了容器沙箱，得到的是无隔离的宿主执行，仅有一条 info 级日志。mapPath 的路径穿越防护对 Exec 无效——被注入的命令本身即可 `cat data/config/default.json` 读走全部 API key。
- 修复建议：

```go
// 1) 移除 lifecycle 的静默降级：docker 不可用时让沙箱启动失败并给出明确指引
//（对齐 Python 的 raise 行为）：
	if !dockerAvailable() {
		l.sandboxMgr.SetBooter(nil)
		logger.I18nError("boxlite 沙箱需要 Docker，但当前不可用；请安装 Docker 或改用 shipyard_neo 后端")
		return
	}
	l.sandboxMgr.SetBooter(sandbox.NewDockerBooter(image))

// 2) 若确需保留 local 后端，至少移植 Python 的黑名单作为纵深防御（sandbox/manager.go）：
var blockedCmdPatterns = []string{" rm -rf ", " rm -fr ", " rm -r ", " mkfs", " dd if=",
	" shutdown", " reboot", " poweroff", " halt", " sudo ", ":(){:|:&};:", " kill -9 ", " killall "}

func isSafeLocalCommand(args []string) bool {
	if len(args) >= 2 && args[0] == "-c" {
		cmd := " " + strings.ToLower(strings.TrimSpace(args[1])) + " "
		for _, pat := range blockedCmdPatterns {
			if strings.Contains(cmd, pat) {
				return false
			}
		}
	}
	return true
}
// LocalBooter.Exec 开头：
	if !isSafeLocalCommand(args) {
		return "", "", -1, fmt.Errorf("blocked unsafe shell command")
	}
```

### [internal/sandbox/manager.go:211] 沙箱 Exec 输出无上限缓冲：stdout/stderr 全量进内存，可被单条命令 OOM [严重程度：中]
- 严重程度：中（触发条件：沙箱启用后，LLM 执行高输出命令——`yes`、`cat` 大文件、`find /`、失控循环打印——在工具超时窗口内（executeToolWithTimeout 的 toolCallTimeout）产生的输出全部缓存在宿主进程内存；Python 侧有 max_output_chars=10_000 截断，Go 的 pipeline 本地 shell 工具也有 cappedWriter/1MB 上限，唯独沙箱 Exec 路径没有）
- 问题描述：两个 booter 都把子进程输出无界地攒进内存缓冲：

```go
// sandbox/manager.go:211-214（LocalBooter.Exec）
	var stdout, stderr bytes.Buffer
	c.Stdout = &stdout
	c.Stderr = &stderr
	err = c.Run()

// sandbox/manager.go:489-490（DockerBooter.Exec）
	var stdout, stderr strings.Builder
	code, err := dockerRun(ctx, dockerArgs, nil, &stdout, &stderr)
```

同仓库 pipeline/computer_tools.go:655-683 已经为宿主本地 shell 工具实现了 `cappedWriter{max: maxShellOutput /* 1MB */}`，Python 的 LocalShellComponent 也以 `max_output_chars=10_000` 截断（local.py:233），沙箱路径未被覆盖。超时只能终止命令，不能阻止超时窗口内已缓冲的输出（`yes` 在 120 秒内可产出数 GB）。
- 修复建议：

```go
// sandbox/manager.go：把 pipeline 的 cappedWriter 思路内置到两个 booter（示例为 LocalBooter）：
	type cappedBuffer struct {
		mu   sync.Mutex
		buf  bytes.Buffer
		max  int
		trun bool
	}
	func (b *cappedBuffer) Write(p []byte) (int, error) {
		b.mu.Lock()
		defer b.mu.Unlock()
		if b.buf.Len() >= b.max {
			b.trun = true
			return len(p), nil // 丢弃但不报错，避免 SIGPIPE 噪音
		}
		if b.buf.Len()+len(p) > b.max {
			n, _ := b.buf.Write(p[:b.max-b.buf.Len()])
			b.trun = true
			return len(p), nil
		}
		return b.buf.Write(p)
	}

	var stdout, stderr cappedBuffer // max 置为 1<<20
	c.Stdout, c.Stderr = &stdout, &stderr
	// 返回前：if stdout.trun { out += "\n[输出超过 1MB 已截断]" }
	// DockerBooter.Exec 中 dockerRun 的 *strings.Builder 参数同样换成 cappedBuffer。
```

### [internal/pysdk/embed.go:74] 注释声称存在"下载后的完整性检查"，实际无任何校验：解释器二进制经可配置镜像落地即执行 [严重程度：中]
- 严重程度：中（触发条件：用户配置了 ASTRBOT_PYTHON_MIRROR / SetPythonMirror / github_proxy 加速前缀（国内部署的常见操作，前缀直接拼在官方 URL 前且不限制协议），镜像或中间人替换 tarball 即获得宿主上的任意代码执行；直连 GitHub 时有 TLS 保护，风险主要在镜像与代理路径）
- 问题描述：常量声明与文档承诺了一个并不存在的控制：

```go
// embed.go:69
//   - ASTRBOT_PYTHON_SKIP_VERIFY  跳过下载后的完整性检查
// embed.go:74
	EnvPythonSkipVerify = "ASTRBOT_PYTHON_SKIP_VERIFY"
```

全仓 grep 确认 `EnvPythonSkipVerify` 除声明外无任何使用；downloadPython（embed.go:342-381）→ downloadFile（embed.go:426-471）对下载内容不做哈希/签名/大小校验，直接落盘、解压、执行。pyDownloadURL 支持把任意镜像前缀拼在官方 URL 之前（embed.go:407-416），Ensure 的 SDK tarball 同样无校验。python-build-standalone 的 release 本身附带 SHA256SUMS 文件，实现校验的成本很低。此外 downloadFile 对响应体也无大小上限（镜像返回无限流可灌满磁盘）。
- 修复建议：

```go
// embed.go：固定 pin 每个 defaultPythonVersion 的 sha256，下载后校验（ASTRBOT_PYTHON_SKIP_VERIFY 仅允许显式跳过）：
var pythonArchiveSHA256 = map[string]string{
	"20260814": "…", // cpython-3.12.14+20260814-x86_64-unknown-linux-gnu-install_only.tar.gz 的哈希
}

func verifyArchive(dest, version, target string) error {
	if os.Getenv(EnvPythonSkipVerify) != "" {
		return nil
	}
	want, ok := pythonArchiveSHA256[version]
	if !ok {
		return fmt.Errorf("no pinned sha256 for python %s", version)
	}
	f, err := os.Open(dest)
	if err != nil {
		return err
	}
	defer f.Close()
	h := sha256.New()
	if _, err := io.Copy(h, f); err != nil {
		return err
	}
	if got := hex.EncodeToString(h.Sum(nil)); got != want {
		return fmt.Errorf("python archive checksum mismatch: got %s want %s", got, want)
	}
	return nil
}
// downloadFile 中同时限制总量：
	written, err := io.Copy(out, io.LimitReader(resp.Body, maxArchiveBytes /* 如 1<<30 */))
```

### [internal/star/subprocess_bridge.go:504] RemovePluginCommands 的 "plugin_" 前缀同时命中过滤器与钩子，三个清理函数语义互相重叠 [严重程度：低]
- 严重程度：低（当前唯一调用点 lifecycle.go:891-893 三函数连调，净效果恰好正确；但任何单独调用 RemovePluginCommands 的代码会连 "plugin_filter_*"/"plugin_hook_*" 一并删除，与函数名和注释承诺不符）
- 问题描述：子进程插件的三类 handler 全名前缀分别是 "plugin_<id>_"（命令，subprocess_bridge.go:170）、"plugin_filter_<id>_"（247）、"plugin_hook_<id>_"（301）。而命令清理用宽前缀：

```go
// subprocess_bridge.go:503-507
	for _, h := range starMgr.Handlers().All() {
		if strings.HasPrefix(h.HandlerFullName, "plugin_") {
			starMgr.Handlers().Remove(h.HandlerFullName)
		}
	}
```

"strings.HasPrefix(x, \"plugin_\")" 对三类前缀都为真，RemovePluginFilters/RemovePluginHooks 随后必为空转。legacy .so 插件命令也叫 "plugin_<name>"（pluginNameFor 的注释），无法用更窄的前缀区分——这正是应当用显式标记而非字符串前缀的信号。
- 修复建议：

```go
// 给子进程桥接的 handler 打显式来源标记，清理按标记而非前缀：
type StarHandlerMetadata struct {
	...
	Source string `json:"source,omitempty"` // "" | "subprocess_cmd" | "subprocess_filter" | "subprocess_hook" | "so_plugin"
}

// RegisterSubprocessPlugin 三处 Append 前分别设置 Source。
// 清理：
func RemovePluginCommands(starMgr *Manager) {
	for _, h := range starMgr.Handlers().All() {
		if h.Source == "subprocess_cmd" || h.Source == "so_plugin" {
			starMgr.Handlers().Remove(h.HandlerFullName)
		}
	}
}
// RemovePluginFilters / RemovePluginHooks 同理，去掉前缀匹配。
```

### [internal/db/database.go:1009] 多处行扫描失败被静默 continue，错误既不记日志也不向上返回（静默丢数据） [严重程度：低]
- 严重程度：低（触发条件：schema 与 Scan 目标不匹配、列为意外 NULL 等——多数 SELECT 已用 COALESCE，正常路径难触发；但一旦发生，KB chunk 列表少块、provider 统计少行、群消息历史少条，均无任何痕迹）
- 问题描述：五个读循环共享同一模式：

```go
// database.go:1007-1012（GetPlatformMessageHistory，其余同类）
	for rows.Next() {
		var r PlatformMessageRow
		if err := rows.Scan(&r.ID, &r.SenderID, &r.SenderName, &r.Content, &r.CreatedAt); err != nil {
			continue
		}
		out = append(out, r)
	}
```

同样写法见 PlatformMessageRank（1088-1090）、ProviderStatsSince（1162-1164）、ListKBs（1259-1262）、ListKBChunksPage（1335-1337）。对照同文件其他函数的错误处理风格（ListConversations:541、ListCronJobs:820 均返回错误），这五处是"错误吞掉"维度上的不一致点。知识库 chunk 静默缺失会直接缩小检索语料且无从排查。
- 修复建议：

```go
		if err := rows.Scan(&r.ID, &r.SenderID, &r.SenderName, &r.Content, &r.CreatedAt); err != nil {
			logger.Warn("GetPlatformMessageHistory: 跳过坏行: %v", err)
			continue
		}
// 至少补 Warn 日志；对 KB chunk / 统计两类强一致数据建议改为：
		if err := rows.Scan(...); err != nil {
			return nil, fmt.Errorf("scan row %d: %w", r.ID, err)
		}
```

### [internal/db/database.go:1120] MessageTimeSeries 对 bucketSec=0 无防御：除零 panic 或死循环 [严重程度：低]
- 严重程度：低（当前唯一调用方 dashboard/server.go:1193 固定传 3600，不可达；属防御缺失的隐患，一旦新增调用方传 0 即整进程 panic）
- 问题描述：

```go
// database.go:1120
		bucket := (ts / int64(bucketSec)) * int64(bucketSec)
// database.go:1128
	for t := start - start%int64(bucketSec); t <= now; t += int64(bucketSec) {
```

bucketSec==0 时 1120 行整数除零直接 panic；即使躲过（空结果集），1128 行 `t += 0` 是死循环。函数签名对入参无任何约束，offsetSec<=0 同样未拦截（start 恒 >= now 时只出一个桶，尚可接受）。
- 修复建议：

```go
func (d *Database) MessageTimeSeries(offsetSec, bucketSec int) [][]int {
	if bucketSec < 1 {
		bucketSec = 3600
	}
	if offsetSec < 1 {
		offsetSec = 3600
	}
	...
```

### [internal/db/database.go:1298] KB 写路径全部绕过 withRetry，与同文件其他写操作的 BUSY 重试策略不一致 [严重程度：低]
- 严重程度：低（触发条件：知识库导入/重建与消息统计、会话持久化等高频写并发时撞上 SQLITE_BUSY——尽管有 busy_timeout(30000) 兜底，30 秒仍忙时 CreateKB/InsertKBChunk 等直接失败，导入中途报错）
- 问题描述：文件头注释（database.go:102-104）明确"withRetry 作为 WAL + busy_timeout 之外的兜底：高并发写入下单个连接仍可能遇到 database is locked"，SetPreference（702）、CreateCronJob（797）、RecordPlatformMessage（958）、RecordProviderCall（1030）等写点都包了 withRetry，但 KB 全部写点没有：

```go
// database.go:1298-1305（InsertKBChunk，CreateKB:1194、UpdateKB:1206、DeleteKB:1219、DeleteKBChunks:1379 同）
func (d *Database) InsertKBChunk(c KBChunk) error {
	_, err := d.db.Exec(
		`INSERT INTO knowledge_base_chunks (chunk_id, kb_id, doc_id, doc_name, content, chunk_index)
		 VALUES (?, ?, ?, ?, ?, ?)`,
		...
	)
	return err
}
```

KB 批量导入（逐 chunk InsertKBChunk）恰恰是最容易与其他写点并发的场景。
- 修复建议：

```go
func (d *Database) InsertKBChunk(c KBChunk) error {
	return d.withRetry(func() error {
		_, err := d.db.Exec(
			`INSERT INTO knowledge_base_chunks (chunk_id, kb_id, doc_id, doc_name, content, chunk_index)
			 VALUES (?, ?, ?, ?, ?, ?)`,
			c.ChunkID, c.KBID, c.DocID, c.DocName, c.Content, c.ChunkIdx,
		)
		return err
	})
}
// CreateKB/UpdateKB/DeleteKB/DeleteKBChunks 同样包一层 withRetry。
```

### [internal/star/builtin/commands.go:266] /stop 与 /stats 是固定话术的空实现，与 Python 行为不符 [严重程度：低]
- 严重程度：低（必然触发但仅影响功能完整性：用户执行 /stop 得到"当前无运行任务"、/stats 得到"暂无统计"，即使确有运行中的 agent 任务与真实 token 统计；/stats 的误导性强于 /stop——数据库里明明有 provider_stats 数据）
- 问题描述：

```go
// builtin/commands.go:266-269
func stopCmd(e *core.Event) {
	// Go pipeline has no long-running agent registry yet; report idle.
	reply(e, i18n.Get("✅ No running tasks in the current session."))
}

// builtin/commands.go:275-277
func statsCmd(e *core.Event) {
	reply(e, i18n.Get("📊 No stats available for this conversation yet."))
}
```

Python 侧 main.py:49-63 将两者分别转发给 agent 管理（真的终止会话任务）与 conversation_c.stats（真实 token 用量）。至少 statsCmd 的数据在 Go 里可用（database.ProviderStatsSince / TodayProviderTokens 已实现），却未接上。
- 修复建议：

```go
func statsCmd(deps Deps, e *core.Event) {
	if deps.Database == nil {
		reply(e, i18n.Get("📊 No stats available for this conversation yet."))
		return
	}
	umo := e.UnifiedMsgOrigin()
	records, _ := deps.Database.ProviderStatsSince(time.Now().AddDate(0, 0, -7))
	var in, out int
	for _, r := range records {
		if r.UMO == umo {
			in += r.InputOther + r.InputCached
			out += r.Output
		}
	}
	reply(e, i18n.Get("📊 最近 7 天：输入 %d tokens，输出 %d tokens", in, out))
}
// stopCmd 若暂无 agent registry，至少改为诚实提示："Go 版暂不支持 /stop"，
// 与 dashboard_update 的处理方式（commands.go:85-87）保持一致。
```

### [internal/star/builtin/commands.go:213] /name 与 /provider 持久化丢弃全部错误：保存失败时重启即丢配置且无任何提示 [严重程度：低]
- 严重程度：低（触发条件：磁盘满/权限问题时 cfg.Set 或 cfg.Save 失败；用户当场收到"设置成功"，重启后昵称与 provider 选择双双回退，完全无从排查）
- 问题描述：

```go
// builtin/commands.go:212-214（persistUmoAlias）
	aliases[umo] = alias
	_ = cfg.Set("umo_alias", aliases)
	_ = cfg.Save()

// builtin/commands.go:396-398（persistSelectedProvider）
	sel[umo] = providerID
	_ = cfg.Set("provider_selection", sel)
	_ = cfg.Save()
```

调用方（nameCmd:183-184、providerCmd:341）在 persist* 返回后才回复"✅ 设置成功"，但 persist* 把失败吞成无返回值，成功回执与实际落盘状态脱钩。
- 修复建议：

```go
func persistUmoAlias(deps Deps, umo, alias string) error {
	...
	if err := cfg.Set("umo_alias", aliases); err != nil {
		return err
	}
	return cfg.Save()
}
// nameCmd：
	if err := persistUmoAlias(deps, umo, alias); err != nil {
		logger.Warn("persist umo_alias: %v", err)
		reply(e, i18n.Get("⚠️ 昵称已生效但持久化失败（%v），重启后可能丢失。", err))
		return
	}
	reply(e, i18n.Get("UMO name set to: %s\nUMO: %s", alias, umo))
// persistSelectedProvider 同理。
```

### [internal/pysdk/embed.go:519] tar 解包只处理 Dir/Reg/Symlink 三种条目，硬链接条目被静默丢弃 [严重程度：低]
- 严重程度：低（触发条件：上游 python-build-standalone 某个 release 的 install_only 归档改用硬链接表达 bin/python3 → python3.12 这类共享文件时，解压产物缺文件；当前会因 downloadPython 的 bin 存在性检查（embed.go:375-377）响亮失败或因次要文件缺失在运行时报错，不会静默错下去，但错误信息难以定位到解包器）
- 问题描述：两个解包函数的 switch 均无 `tar.TypeLink` 分支：

```go
// embed.go:519-558（extractTarGzKeepTop；extractTarGzStripTop 的 switch 在 722-744，同样只有三个 case）
		switch hdr.Typeflag {
		case tar.TypeDir:
			...
		case tar.TypeReg:
			...
		case tar.TypeSymlink:
			...
		}
		// tar.TypeLink（'1' 硬链接）落到这里被静默跳过
```

GNU tar 归档中硬链接的第二个及后续条目 typeflag 为 '1'、内容为空、Linkname 指向第一个实体；跳过即丢失该路径。
- 修复建议：

```go
		case tar.TypeLink:
			_ = os.Remove(target)
			if err := os.MkdirAll(filepath.Dir(target), 0o755); err != nil {
				return err
			}
			src, err := safeJoin(dir, filepath.FromSlash(hdr.Linkname))
			if err != nil {
				return fmt.Errorf("hardlink %q -> %q escapes root: %w", name, hdr.Linkname, err)
			}
			if err := os.Link(src, target); err != nil {
				// 跨设备或不支持硬链接时退化为复制
				data, rerr := os.ReadFile(src)
				if rerr != nil {
					return err
				}
				if werr := os.WriteFile(target, data, os.FileMode(uint32(hdr.Mode&0o777))); werr != nil {
					return werr
				}
			}
```

### [internal/pysdk/embed.go:612] SDK 目录的下载/解包无跨进程锁（venv 有锁、SDK 没有），双实例共享 data 目录时会互相踩踏 [严重程度：低]
- 严重程度：低（触发条件：两个宿主进程共享同一 dataDir 且都触发首装或版本变更——同机多实例部署；Ensure 会 RemoveAll(root) 后解包，与另一实例的解包/读取交错可产出损坏的 SDK 目录，插件随机 import 失败）
- 问题描述：ensureVenvReady 对 venv 供给有完整的跨进程锁（embed.go:826-831 acquireVenvLock + 锁内双重检查），SDK 目录的 Ensure 却完全没有：

```go
// embed.go:630-635
	} else if data, err := os.ReadFile(versionFile); err != nil || strings.TrimSpace(string(data)) != SDKVersion {
		logger.Info("Python SDK 版本变化（磁盘 %q vs 期望 %q），重新下载…", ...)
		needFetch = true
		_ = os.RemoveAll(root)   // 另一实例可能正从中读取/解包
	}
```

单进程内由 SubprocessManager.mu（runtime.go:1605）串行化，跨进程无保护——与 venv 路径的防护水平不对称。versionFile 的读取错误（非 not-exist 的 IO 错误）也会被误判为版本不匹配触发全量重下，放大了竞态窗口。
- 修复建议：

```go
	// Ensure 的下载/解包段落包进与 venv 相同的跨进程锁：
	lockPath := filepath.Join(absData, ".python-sdk.lock")
	release, err := acquireVenvLock(lockPath, 10*time.Minute)
	if err != nil {
		return "", fmt.Errorf("获取 SDK 锁失败: %w", err)
	}
	defer release()
	// 锁内重新检查 marker/versionFile（双重检查），再决定 needFetch 与 RemoveAll。
	// versionFile 读取错误也应区分：
	data, rerr := os.ReadFile(versionFile)
	if os.IsNotExist(rerr) {
		needFetch = true
	} else if rerr == nil && strings.TrimSpace(string(data)) != SDKVersion {
		needFetch = true
		_ = os.RemoveAll(root)
	} else if rerr != nil {
		return "", fmt.Errorf("read VERSION: %w", rerr)
	}
```

### [internal/sandbox/shipyard_neo.go:95] Start 持有 booter 互斥锁进行最长 180 秒的就绪轮询，阻塞同实例的全部沙箱操作 [严重程度：低]
- 严重程度：低（触发条件：Bay 后端创建慢/异常时（neoReadinessTimeout=180s），同一 booter 的 Exec/ReadFile/WriteFile/IsRunning 全部卡在 b.mu 上；默认后端即 shipyard_neo，启动期间用户消息触发沙箱工具会挂到就绪或超时为止）
- 问题描述：

```go
// shipyard_neo.go:94-96
func (b *ShipyardNeoBooter) Start(ctx context.Context) error {
	b.mu.Lock()
	defer b.mu.Unlock()
	...
	deadline := time.Now().Add(neoReadinessTimeout)  // 137 行，循环体直到 171 行
```

轮询循环（137-171）全程持锁，而 Exec（234-238）/ReadFile（289-293）/WriteFile（308-312）开头都要抢 b.mu 读运行状态。对比 DockerBooter.Start（manager.go:376-441）虽也持锁但每步 docker CLI 有 ctx 约束；ShipyardNeo 的 180 秒是最长路径。轮询本身只需要读 endpoint/token/sandboxID 三个不可变字段。
- 修复建议：

```go
func (b *ShipyardNeoBooter) Start(ctx context.Context) error {
	// 阶段一：短持锁读取不可变配置并做 running 快速返回
	b.mu.Lock()
	if b.running {
		b.mu.Unlock()
		return nil
	}
	ep, token := b.endpointURL, b.accessToken
	b.mu.Unlock()

	// 阶段二：无锁解析 profile / 创建 sandbox（创建失败不会留下半状态）
	...
	// 阶段三：就绪后短持锁写回 sandboxID/running/caps
	b.mu.Lock()
	b.sandboxID, b.running, b.caps = id, true, caps
	b.mu.Unlock()
	return nil
}
// 轮询期间的临时失败清理（b.delete）本就不依赖锁内状态。
```

### [internal/sandbox/shipyard_neo.go:67] NewShipyardNeoBooter 中的空赋值死代码 [严重程度：低]
- 严重程度：低（无功能影响；空 if 分支降低可读性，且容易让读者误以为此处存在默认值逻辑而漏改）
- 问题描述：

```go
// shipyard_neo.go:63-69
func NewShipyardNeoBooter(endpointURL, accessToken, profile string, ttl int) *ShipyardNeoBooter {
	if ttl <= 0 {
		ttl = 3600
	}
	if profile == "" {
		profile = ""   // 空操作
	}
```

`profile` 的空值语义由 resolveProfile（176-216）正确处理（空则自动选择），这四行是残留代码。
- 修复建议：

```go
func NewShipyardNeoBooter(endpointURL, accessToken, profile string, ttl int) *ShipyardNeoBooter {
	if ttl <= 0 {
		ttl = 3600
	}
	return &ShipyardNeoBooter{ ... }  // 直接删除 if profile == "" 分支
```

---

以下文件未发现问题：

- /home/xiaowei/Astrbot-golang/internal/star/manager.go（36 行，薄封装，无状态逻辑）
- /home/xiaowei/Astrbot-golang/internal/star/filters.go（112 行；PermissionFilter 的 admin 判定与 Python is_admin()（role=="admin"）及 pipeline 的 Role 归一化（stages.go:227-247，平台 IsAdmin/admins_id → "admin"）核对一致，set/reset 的权限模型与 Python 装饰器逐条比对一致）
- /home/xiaowei/Astrbot-golang/internal/star/command.go（492 行；命令组重命名缓存失效传播正确、子→父方向的锁序无死锁、GetCompleteCommandNames 双重检查；HandlerRegistry 与 StarHandlerRegistry 的重复属可接受的遗留结构）
- /home/xiaowei/Astrbot-golang/internal/star/builtin/reachability.go（108 行；并发结果 map 有锁保护、15s 超时、provider 配置只读不落盘）
- /home/xiaowei/Astrbot-golang/internal/pysdk/version.go（18 行，常量声明）
- /home/xiaowei/Astrbot-golang/internal/pysdk/venv_lock_unix.go（44 行；flock + EINTR/EWOULDBLOCK 正确区分、持有者死亡内核自动释放）
- /home/xiaowei/Astrbot-golang/internal/pysdk/venv_lock_windows.go（47 行；staleLockAge(30min) > venvLockTimeout(10min) 的不变量成立，陈旧锁窃取不会打断在途安装）

另：db 层按审查要求逐条核查了全部 SQL 拼接点——database.go 中仅有的 4 处非纯占位符 SQL（418 行 PRAGMA user_version 为编译期常量、588/613 行 IN 子句由 placeholders() 只生成 "?"、901 行 UPDATE SET 列名经 cronJobWritableFields 白名单校验），未发现可注入点。

# Part 08：Astrbot-golang/internal/t2i + lifecycle + config + cron（文本转图 / 生命周期 / 配置 / 定时任务）

### [internal/t2i/image.go:1004] fetchRemoteImage 的 SSRF 防御可被域名解析与非点分 IP 绕过 [严重程度：中]
- 严重程度：中（触发条件：t2i 端点为不受信/被接管的远程服务，或其返回的 JSON envelope 中 `data.url` 被攻击者控制——使用公共 t2i 服务或 http 明文端点时暴露面现实存在）
- 问题描述：函数用黑名单方式拒绝回环与纯 IP 主机，但三类常见向量全部放行：(1) 解析到内网/回环 IP 的域名（`127.0.0.1.nip.io`、`10-0-0-1.sslip.io`、内网主机名、云元数据域名）——`net.ParseIP` 只识别点分十进制/IPv6 字面量，域名一律通过；(2) 非 `net.ParseIP` 可识别的 IP 文本形式，如 `http://0x7f000001/`、`http://2130706433/`（cgo resolver 下解析为 127.0.0.1）；(3) `client.Get` 默认跟随最多 10 次重定向，重定向目标（`http://localhost/...`、`http://169.254.169.254/...`）不经过任何检查。注释声称的"SSRF 纵深防御"实际不成立：

```go
	host := strings.ToLower(parsed.Hostname())
	...
	if host == "localhost" || strings.HasSuffix(host, ".localhost") {
		return nil, fmt.Errorf("fetch image: loopback host %q rejected", host)
	}
	if ip := net.ParseIP(host); ip != nil {
		return nil, fmt.Errorf("fetch image: IP target %q rejected", host)
	}
	client := &http.Client{Timeout: 120 * time.Second}
	resp, err := client.Get(u)
```
- 修复建议：改为"解析后校验 IP"的白名单模式，并对重定向统一复用同一校验（自定义 CheckRedirect），拒绝环回/私网/链路本地/组播/未指定地址：
```go
func fetchRemoteImage(u string) ([]byte, error) {
	parsed, err := url.Parse(u)
	if err != nil {
		return nil, fmt.Errorf("fetch image: invalid URL: %w", err)
	}
	if parsed.Scheme != "http" && parsed.Scheme != "https" {
		return nil, fmt.Errorf("fetch image: unsupported scheme %q", parsed.Scheme)
	}
	checkRedirect := func(req *http.Request, via []*http.Request) error {
		if len(via) >= 3 {
			return fmt.Errorf("too many redirects")
		}
		return nil // 每个 redirect 目标由下面的 dialer/control 再校验一次
	}
	dialer := &net.Dialer{Timeout: 10 * time.Second}
	transport := &http.Transport{
		DialContext: dialer.DialContext,
		// 在连接建立时按实际连接的对端地址校验（同时覆盖 DNS rebinding）
		DialContextTLSHandshakeTimeout: 10 * time.Second,
	}
	transport.DialContext = func(ctx context.Context, network, addr string) (net.Conn, error) {
		host, port, _ := net.SplitHostPort(addr)
		ips, err := net.DefaultResolver.LookupIPAddr(ctx, host)
		if err != nil {
			return nil, err
		}
		for _, ip := range ips {
			if !ip.IP.IsGlobalUnicast() || ip.IP.IsPrivate() {
				return nil, fmt.Errorf("fetch image: non-public target %s rejected", ip.IP)
			}
		}
		return dialer.DialContext(ctx, network, addr)
	}
	client := &http.Client{Timeout: 120 * time.Second, Transport: transport, CheckRedirect: checkRedirect}
	// ... 原有 GET + 大小限制 + magic bytes 校验保持不变
}
```
（若部署环境确需访问内网对象存储，可将私网放行做成配置开关，但环回与链路本地应始终拒绝。）

### [internal/cron/manager.go:189] Add/AddActiveJob 返回活 Job 指针、persist 在锁外读 Job 字段，与 tick 的持锁写构成数据竞争 [严重程度：中]
- 严重程度：中（触发条件：任务创建后立即被 tick 触发重算 NextRun，同时调用方读返回值或 persist 读取；race detector 必报，time.Time 为多字结构存在撕裂读）
- 问题描述：`Get`/`List` 都通过 `clone()` 返回副本，但 `Add` 把入参 Job 直接入表后由 `AddActiveJob` 原样返回给调用方（pipeline/cron_tools.go:115 随即在锁外读 `job.NextRun`），且 `persist`（Add:118、UpdateJob:161 均在解锁后调用）直接读取活对象的 `Name/Payload/NextRun` 等字段。而 tick 持锁执行 `m.computeNextRunLocked(job, now)`（manager.go:325）写 `job.NextRun`（manager.go:362）：
```go
func (m *CronJobManager) Add(job *Job) {
	m.mu.Lock()
	...
	m.computeNextRunLocked(job, time.Now())
	m.mu.Unlock()
	m.persist(job) // 锁外读 job.Payload / job.NextRun
	...
}
...
func (m *CronJobManager) AddActiveJob(...) (*Job, error) {
	...
	m.Add(job)
	return job, nil // 返回 m.jobs 里的同一活指针
}
```
- 修复建议：统一"锁外只见副本"的边界——persist 持锁或对 clone 操作，Add 返回克隆：
```go
func (m *CronJobManager) Add(job *Job) {
	m.mu.Lock()
	job.Enabled = true
	m.jobs[job.ID] = job
	m.armJobLocked(job)
	m.computeNextRunLocked(job, time.Now())
	snapshot := job.clone() // clone 补拷 NextRun/RunAt（值字段随结构体复制）
	m.mu.Unlock()
	m.persist(snapshot)
}

func (m *CronJobManager) AddActiveJob(...) (*Job, error) {
	...
	m.Add(job)
	return m.Get(job.ID), nil // Get 返回 clone
}
```
（注意现有 `clone()` 的浅拷贝 Payload 对 persist 的 json.Marshal 已够用，但应确保在锁内完成克隆。）

### [internal/cron/manager.go:339] 到期任务的 handler 在锁外读 job.Payload，与 UpdateJob 持锁写同一 map 并发，可致不可恢复 panic [严重程度：中]
- 严重程度：中（触发条件：某 active_agent 任务到点触发的同一时刻，任务所有者通过 future_task edit 修改该任务——Go map 并发读写触发 `fatal error: concurrent map read and map write`，进程直接崩溃且无法 recover）
- 问题描述：tick 解锁后在 goroutine 里调用 `j.Handler(ctx)`，而 handler（lifecycle.go:242-243）直接读 `job.Payload["session"]`、`job.Payload["note"]`，不持有 `m.mu`；另一边 `UpdateJob` 的 mutate 回调（pipeline/cron_tools.go:183 `job.Payload["note"] = note`）在锁内写同一个 map。锁只保护了写方，读方裸奔：
```go
	for _, job := range due {
		m.wg.Add(1)
		go func(j *Job) {
			...
			if err := j.Handler(ctx); err != nil { // handler 内锁外读 j.Payload
```
```go
	// lifecycle.go:242
	session, _ := job.Payload["session"].(string)
	note, _ := job.Payload["note"].(string)
```
- 修复建议：tick 收集 due 任务时在持锁阶段克隆任务，把副本交给 handler 执行（语义上也更正确：本次执行使用触发时刻的任务快照）：
```go
		due = append(due, job)
		job.running = true
		...
	}
	// 持锁阶段结束后、启动 goroutine 前，将 due 中的活指针替换为快照
	for i, j := range due {
		due[i] = j.clone() // clone 需补上 Payload 内嵌套 map 的深拷贝
	}
	m.mu.Unlock()
```
同时给 `clone()` 的 Payload 做一层深拷贝（当前仅浅拷贝顶层键），避免嵌套结构继续共享。

### [internal/config/config.go:194] Get 仅对 map 值深拷贝，slice 值返回内部引用，违背注释承诺并与 save 遍历竞态 [严重程度：中]
- 严重程度：中（触发条件：调用方修改 Get 返回的列表及其元素——当前调用方多为只读，但注释明确承诺不可变，属于防御边界破损；一旦有调用方写入即与 save 的 orderedWrite 遍历产生 map/slice 并发读写）
- 问题描述：注释声称"callers can never mutate the live config data"，但只有顶层值为 `map[string]interface{}` 时才走 `copyMap` 深拷贝；值为 `[]interface{}`（如 platform、provider、admins_id、id_whitelist 等大量配置段）时直接返回内部 slice 引用，其元素（含嵌套 map）均与 live data 共享：
```go
func (c *AstrBotConfig) Get(key string) interface{} {
	c.mu.RLock()
	v := c.data[key]
	c.mu.RUnlock()
	if m, ok := v.(map[string]interface{}); ok {
		return copyMap(m)
	}
	return v // slice 值原样返回内部引用
}
```
- 修复建议：复用已有的 `deepCopyValue` 对所有容器值深拷贝：
```go
func (c *AstrBotConfig) Get(key string) interface{} {
	c.mu.RLock()
	v := c.data[key]
	c.mu.RUnlock()
	switch t := v.(type) {
	case map[string]interface{}:
		return copyMap(t)
	case []interface{}:
		out := make([]interface{}, len(t))
		for i, e := range t {
			out[i] = deepCopyValue(e)
		}
		return out
	}
	return v
}
```

### [internal/config/defaults.go:50] 双默认配置源漂移：DefaultConfig() 手写 schema 与生成的 DefaultConfigJSONRaw 不一致，且被用作主配置完整性基准 [严重程度：中]
- 严重程度：中（触发条件：全新安装——lifecycle.go:112 用 `config.NewConfig("data/cmd_config.json", config.DefaultConfig())` 创建主配置，完整性校验与缺失键补全只对照这份仅 9 个顶层键的过时 schema）
- 问题描述：项目存在两套默认：`default_config.go` 的 `DefaultConfigJSONRaw`（生成自 Python DEFAULT_CONFIG，与 WebUI 共用）与 `defaults.go` 手写的 `DefaultConfig()`。两者已实质漂移：dashboard.username `admin`（defaults.go:52）vs `astrbot`（default_config.go:14）；`max_context_length` 50（defaults.go:73）vs -1；`dequeue_context_length` 10 vs 1；`prompt_prefix` "" vs `"{{prompt}}"`；`wake_prefix` "" vs `["/"]`。后果：新装用户配置文件仅含这 9 个键且写入偏离原版的默认值——例如 `provider_settings.max_context_length=50` 会让 pipeline（stages.go:1019 `MaxContextLength > 0` 即截断）对历史上下文强制截断，而原版默认 -1 表示不截断；`dashboard.username="admin"` 与 WebUI 宣传的 Python 默认 `astrbot` 不同。其余几百个键（platform_settings、t2i、sandbox 等）缺失时不补全，全靠各组件零值 fallback 兜底：
```go
// defaults.go:50-58
"username": {
	Type:         SchemaString,
	DefaultValue: "admin",
	Optional:     true,
},
```
- 修复建议：删除手写 schema，让 `NewConfig` 的完整性基准直接来自生成的完整默认（已具备顺序保持能力），单一事实来源：
```go
// defaults.go
func DefaultConfig() *SchemaNode { ... } // 删除

// lifecycle.go:112 改为基于完整默认创建：
func defaultConfigMap() map[string]interface{} {
	var m map[string]interface{}
	if err := json.Unmarshal([]byte(config.DefaultConfigJSON()), &m); err != nil {
		return map[string]interface{}{}
	}
	return m
}
cfg := config.NewWithDefaults("data/cmd_config.json", defaultConfigMap())
```
（`New` 已支持 defaults 参数，可将其改造为导出的构造入口；迁移幂等性由 checkConfigIntegrity 的"只补缺失键"语义保证。）

### [internal/cron/expression.go:206] 单值+步长（如 `5/15`）忽略步长，与 croniter 语义不符 [严重程度：中]
- 严重程度：中（触发条件：用户或 LLM 经 future_task 工具创建形如 `5/15 * * * *` 的表达式——croniter/标准 cron 中等价于 `5-59/15`（5,20,35,50 分各触发一次），本实现只在第 5 分钟触发一次，任务静默漏跑）
- 问题描述：`applyFieldPart` 处理 `part/step` 时，`*` 与 `a-b` 分支都正确展开了步长，唯独单值分支直接 `spec.values[v] = true` 后返回，先前解析出的 `step` 被丢弃：
```go
	if i := strings.Index(part, "-"); i >= 0 {
		... // range 分支正确使用 step
	}
	v, err := parseFieldValue(part, min, max, names)
	if err != nil {
		return err
	}
	spec.values[v] = true // `5/15` 走到这里，step=15 被忽略
	return nil
```
测试仅覆盖 `*/15`，未覆盖该形态。`ParseCron` 校验通过（不报错），偏差完全静默。
- 修复建议：单值带步长时按 croniter 语义展开为 `v..max` 步进：
```go
	v, err := parseFieldValue(part, min, max, names)
	if err != nil {
		return err
	}
	if step > 1 {
		spec.rangeAny = true
		spec.from, spec.to = v, max
		for x := v; x <= max; x += step {
			spec.values[x] = true
		}
		return nil
	}
	spec.values[v] = true
	return nil
```
并在 expression_test.go 补充 `5/15` → 5,20,35,50 的用例。

### [internal/t2i/t2i.go:34] NewRenderer 吞掉默认模板加载错误，且 Renderer 整体未被生产代码接线 [严重程度：低]
- 严重程度：低（触发条件：模板目录缺失/模板语法错误时静默失败，首次 Render 才报"template not found"；全项目无 NewRenderer/LoadTemplate/Render 生产调用方，属预留未接线代码）
- 问题描述：
```go
	// Try to load default template
	r.LoadTemplate("default", "default.html") // 返回的 error 被丢弃
```
- 修复建议：至少记录失败原因；若近期不计划接线，建议删除或标注明确的 build tag：
```go
	if err := r.LoadTemplate("default", "default.html"); err != nil {
		logger.Debug("t2i default template not loaded: %v", err)
	}
```

### [internal/t2i/t2i.go:81] enabled 字段无锁并发读写 [严重程度：低]
- 严重程度：低（触发条件：SetEnabled 与 IsEnabled 并发调用——race detector 可报；结构体已持有 mu 却未用于该字段）
- 问题描述：
```go
func (r *Renderer) IsEnabled() bool {
	return r.enabled // 无锁读
}
func (r *Renderer) SetEnabled(enabled bool) {
	r.enabled = enabled // 无锁写
}
```
- 修复建议：
```go
func (r *Renderer) IsEnabled() bool {
	r.mu.RLock()
	defer r.mu.RUnlock()
	return r.enabled
}
func (r *Renderer) SetEnabled(enabled bool) {
	r.mu.Lock()
	r.enabled = enabled
	r.mu.Unlock()
}
```

### [internal/t2i/image.go:732] emojiImage 无并发去重，同一 emoji 可被并发重复下载 [严重程度：低]
- 严重程度：低（触发条件：多条消息并发渲染且包含相同未缓存 emoji——缓存 Load miss 后各自发起 HTTP 下载；写盘有原子保护，仅浪费带宽与请求量）
- 问题描述：
```go
	if v, ok := emojiImgCache.Load(cp); ok {
		return v.(image.Image), true
	}
	if _, failed := emojiFailedCache.Load(cp); failed {
		return nil, false
	}
	path := filepath.Join(ensureEmojiDir(), cp+".png")
	data, err := os.ReadFile(path)
	if err != nil {
		data, err = downloadTwemoji(cp) // 并发下同一 cp 会重复下载
```
- 修复建议：用 `golang.org/x/sync/singleflight` 或 per-codepoint mutex 合并并发请求：
```go
var emojiFlight singleflight.Group

func emojiImage(cluster string) (image.Image, bool) {
	cp := emojiCodepoint(cluster)
	if cp == "" {
		return nil, false
	}
	if v, ok := emojiImgCache.Load(cp); ok {
		return v.(image.Image), true
	}
	if _, failed := emojiFailedCache.Load(cp); failed {
		return nil, false
	}
	v, err, _ := emojiFlight.Do(cp, func() (interface{}, error) {
		return loadEmojiOnce(cp) // 原有读盘/下载/解码/缓存逻辑
	})
	if err != nil {
		return nil, false
	}
	return v.(image.Image), true
}
```

### [internal/cron/manager.go:245] RunNow 不检查也不设置 running 标志，可与 tick 重叠执行同一任务 [严重程度：低]
- 严重程度：低（触发条件：WebUI/调试入口 RunNow 立即执行的任务恰逢到点触发——tick 的防重叠设计被绕过，同任务两个 handler 并发运行）
- 问题描述：tick 专门用 `job.running` 防止重叠（manager.go:309-314），但 RunNow 直接起 goroutine 执行，既不检查也不置位：
```go
	go func(j *Job) {
		if err := j.Handler(runCtx); err != nil {
			logger.Error("Cron job %s run-now failed: %v", j.ID, err)
		}
	}(job)
```
- 修复建议：RunNow 复用同一互斥语义：
```go
	m.mu.Lock()
	job := m.jobs[id]
	if job != nil && job.running {
		m.mu.Unlock()
		return fmt.Errorf("job %s is already running", id)
	}
	if job != nil {
		job.running = true
	}
	runCtx := m.ctx
	m.mu.Unlock()
	...
	go func(j *Job) {
		defer func() {
			m.mu.Lock()
			j.running = false
			m.mu.Unlock()
		}()
		...
	}(job)
```

### [internal/cron/manager.go:330] tick 的 wg.Add 可能发生在 Stop 的 wg.Wait 返回之后，违反 WaitGroup 使用约定 [严重程度：低]
- 严重程度：低（触发条件：Stop 与正在执行的 tick 并发——Stop 的 Wait 已返回并继续关闭数据库，tick 随后 wg.Add(1) 启动的任务 goroutine 在资源关闭后仍运行；当前 active_agent handler 只发事件，实际危害有限）
- 问题描述：`Stop` 先 `close(m.stop)` 再等待 `m.wg`；而已通过 select 进入主体的 tick 仍会在锁外执行 `m.wg.Add(1)`。WaitGroup 文档要求"计数从零开始的正增量 Add 必须发生在 Wait 之前"，此处无法保证：
```go
	done := make(chan struct{})
	go func() {
		m.wg.Wait()
		close(done)
	}()
```
```go
	for _, job := range due {
		m.wg.Add(1) // 可能晚于上面的 Wait 返回
```
- 修复建议：tick 进入时（在 select 收到 ticker 后、扫描任务前）先 `m.wg.Add(1)` 并在整个 tick（含 goroutine 启动）完成后 Done，使 Add 全部发生在任何 Wait 可观察零计数之前：
```go
func (m *CronJobManager) tick(ctx context.Context, now time.Time) {
	m.wg.Add(1)          // tick 自身计入，保证后续 per-job Add 不与 Wait 竞争
	defer m.wg.Done()
	...
	for _, job := range due {
		m.wg.Add(1)
		go func(j *Job) { defer m.wg.Done(); ... }(job)
	}
}
```

### [internal/lifecycle/lifecycle.go:708] 热路径反复 cfg.All() 深拷贝整个配置 map [严重程度：低]
- 严重程度：低（触发条件：每条消息的别名解析、每次插件 ChatLLM 反向调用、每次配置热更新都完整深拷贝数百键的配置树——大对象频繁创建，CPU 与 GC 压力随消息量线性增长）
- 问题描述：`umoAliasResolver`（pipeline 每消息调用）、`chatLLMForPlugins`（每次插件 LLM 调用）都为了读一两个字段而 `cfg.All()` 全量深拷贝：
```go
func (l *Lifecycle) umoAliasResolver(umo string) string {
	...
	all := cfg.All() // 深拷贝整棵配置树，只为了读 umo_alias 一个键
	aliases, _ := all["umo_alias"].(map[string]interface{})
```
- 修复建议：为高频读取提供按路径取值或缓存接口，例如在 AstrBotConfig 上增加 `GetNested(keys ...string)`（锁内只走需要的分支），或对 alias 表做带 mtime 失效的小缓存（参照本文件 personaCache 的做法）。

### [internal/lifecycle/lifecycle.go:814] personaSkillsResolver 对 skills 字段类型错误 fail-open（返回 nil = 不限制） [严重程度：低]
- 严重程度：低（触发条件：data/personas.json 中某 persona 的 skills 被写成字符串等非数组类型——该 persona 的技能白名单失效，变成全量放行，与"empty slice = no skills allowed"的收紧方向相反）
- 问题描述：
```go
	list, ok := skillsRaw.([]interface{})
	if !ok {
		return nil // 类型错误时返回 nil = unrestricted
	}
```
- 修复建议：类型不合法时按最严格语义处理并告警：
```go
	list, ok := skillsRaw.([]interface{})
	if !ok {
		logger.I18nWarn("persona %s 的 skills 字段类型错误（应为数组），已按禁止全部技能处理", personaID)
		return []string{}
	}
```

### [internal/cron/expression.go:194] 区间 from>to 静默交换而非 wrap-around，matches 存在死分支 [严重程度：低]
- 严重程度：低（触发条件：dow 写 `fri-mon`（5-1）——croniter 语义为周五/六/日/周一环绕匹配，本实现交换为周一至周五，行为静默改变）
- 问题描述：
```go
		if from > to {
			from, to = to, from // 静默交换
		}
```
另 `matches` 的两个分支返回完全相同的表达式，`rangeAny` 判断是死代码：
```go
func (f *fieldSpec) matches(v int) bool {
	if f.rangeAny && !f.any {
		// Range already expanded into values during parse.
		return f.values[v]
	}
	return f.values[v]
}
```
- 修复建议：dow 字段按 croniter 支持 wrap-around 展开，或至少在交换时输出告警日志；删除 matches 的冗余分支。

### [internal/config/config.go:144] New 与 Timestamp 为无生产调用方的导出面 [严重程度：低]
- 严重程度：低（触发条件：无直接触发——`New`（含 schemaToDefaults 路径）与 `Timestamp` 在生产代码中零调用，仅测试使用，徒增维护面且易与 NewConfig 混淆）
- 问题描述：`New(configPath, defaults, schema)`（config.go:144）与 `Timestamp()`（config.go:710）经全仓库 grep 无任何非测试调用方；生产入口是 `NewConfig`。
- 修复建议：删除或降为非导出（`newWithDefaults`）；若保留 `New`，在文档注释标注仅供测试。

以下文件未发现问题：
- /home/xiaowei/Astrbot-golang/internal/config/default_config.go（生成代码，仅包含与 Python 原版对齐的默认 JSON 常量）
- /home/xiaowei/Astrbot-golang/internal/config/ordered_json.go（顺序保持实现正确；UnmarshalJSON/MarshalJSON 的重复键边角行为不影响现有持锁调用路径，Set/Delete 的无锁性在 config.go 中均由 c.mu 串行保护）

# Part 09：Astrbot-golang/internal 其余小模块（agent/conversation/skills/utils/core/toolchain/log/knowledgebase/backup/session/contentsafety/i18n/ratelimit/persona/version）+ cmd + pkg

### [internal/conversation/manager.go:196] Manager 对外返回缓存内"活指针"，锁外读写构成数据竞争 [严重程度：中]
- 严重程度：中（触发条件：dashboard 导出会话、插件 host_service 序列化会话、pipeline 读取 conv.Persona 等任一操作与 AppendHistory/SetTitleByCID/SetPersonaByCID 并发；`go build -race` 可稳定检出）
- 问题描述：模块内部写路径都在 `m.mu` 保护下（如 AppendHistory 546-567、SetTitleByCID 462-476、SetPersonaByCID 479-493），但四个读取 API 把 map 里的活指针直接交出去，调用方在**不持锁**的情况下读写字段：
```go
// GetConversation（196-214 行），conv 是 m.byCID[cid] 的活指针
func (m *Manager) GetConversation(unifiedMsgOrigin string) *Conversation {
	m.mu.RLock()
	cid := m.current[unifiedMsgOrigin]
	conv := m.byCID[cid]
	m.mu.RUnlock()
	if conv != nil || m.db == nil {
		return conv   // ← 活指针逃逸
	}
	...
	return loaded     // ← 213 行，loaded 同时也是刚放进 map 的活指针
}
```
同类还有 AllConversations（217-227）、FindByCID（451-459）、GetOrCreateConversation（170-175）。已证实的无锁消费方：`internal/dashboard/handlers.go:9551` 用 `FindByCID` 拿指针后直接 `json.Marshal(conv.History)`（与 AppendHistory 的 append 并发）；`internal/pipeline/stages.go:1470` 读 `conv.Persona`（与 SetPersonaByCID 写 string 头并发，string 双字头读可撕裂）；`internal/plugin/host_service.go:506/511` 序列化标量字段。另外 SwitchConversation（419-431）把**深拷贝快照**塞回 `m.byCID`，导致此前拿到旧活指针的 goroutine 继续在"已脱离缓存"的旧对象上操作，写丢失且不可见。模块内其实已备好 `copyConversationLocked`（161-165）但只用于 GetConversationSnapshot/GetAllConversations。
- 修复建议：
```go
// GetConversation / AllConversations / FindByCID / GetOrCreateConversation 一律返回深拷贝快照，
// 与 GetConversationSnapshot 保持同一契约：
func (m *Manager) GetConversation(unifiedMsgOrigin string) *Conversation {
	m.mu.RLock()
	cid := m.current[unifiedMsgOrigin]
	conv := m.byCID[cid]
	var snap *Conversation
	if conv != nil && !conv.IsDeleted {
		snap = copyConversationLocked(conv)
	}
	m.mu.RUnlock()
	if snap != nil || m.db == nil {
		return snap
	}
	// DB 回填路径：加载后存 map，返回给外界的仍是拷贝
	row, found, err := m.db.GetConversationByUserID(unifiedMsgOrigin)
	if err != nil || !found {
		return nil
	}
	loaded := rowToConversation(row)
	m.mu.Lock()
	m.byCID[loaded.CID] = loaded
	m.current[unifiedMsgOrigin] = loaded.CID
	m.mu.Unlock()
	return copyConversationLocked(loaded)
}
```
（AllConversations/FindByCID 同理在锁内 `copyConversationLocked`；SwitchConversation 建议改为在锁内对现有对象做"指针保留式"更新：仅刷新 `current` 与 `UpdatedAt`，不替换 map 里的对象，避免对象身份分裂。）

### [internal/conversation/manager.go:566] 删除路径与锁外 persist 竞态：已删除会话被重新写回数据库、重启后复活 [严重程度：中]
- 严重程度：中（触发条件：用户在 dashboard 删除会话的同时，该会话所属管道正在完成一轮 LLM 回复并 AppendHistory；LLM 回复落库恰好发生在删除之后）
- 问题描述：AppendHistory 在解锁后才持久化（避免锁内 IO，方向正确），但 persist 的 Upsert 无任何"会话仍存在"的校验：
```go
// AppendHistory（546-567 行）
	m.mu.Unlock()
	m.persist(conv)        // ← 566 行：锁外执行，此间会话可能已被删除

// DeleteConversationByCID（512-531 行）
	m.mu.Unlock()
	if m.db != nil {
		_ = m.db.DeleteConversationByID(cid)   // ← 528 行：先删 DB
	}

// persist（632-649 行）
	if err := m.db.UpsertConversation(cid, uid, platformID, string(historyJSON), title, persona); err != nil {
```
时序：AppendHistory 解锁 →（调度暂停）→ DeleteConversationByCID 完成 DB 删除 → AppendHistory 恢复执行 `UpsertConversation`，把刚删除的会话整行重新插回数据库。内存 map 里它已不存在，但下次重启 `loadFromDB` 会把它重新载入并选为 current——用户删除的会话（含全部历史）复活。DeleteConversation（434-448）与 SetTitleByCID 等的锁外 DB 写同理，只是后果较轻。
- 修复建议：
```go
func (m *Manager) AppendHistory(unifiedMsgOrigin, role, content string) {
	m.mu.Lock()
	conv := m.byCID[m.current[unifiedMsgOrigin]]
	if conv == nil { ... }
	// ... append / trim / title ...
	m.mu.Unlock()
	m.persistIfAlive(conv) // 只在仍被缓存引用时才落库
}

func (m *Manager) persistIfAlive(conv *Conversation) {
	if m.db == nil {
		return
	}
	m.mu.RLock()
	history := copyHistory(conv.History)
	cid, uid, platformID := conv.CID, conv.UserID, conv.PlatformID
	title, persona := conv.Title, conv.Persona
	alive := m.byCID[cid] == conv // 仍是被管理的对象才允许写库
	m.mu.RUnlock()
	if !alive {
		return
	}
	historyJSON, err := json.Marshal(history)
	if err != nil { ... }
	if err := m.db.UpsertConversation(cid, uid, platformID, string(historyJSON), title, persona); err != nil { ... }
}
```
（更彻底的做法是给每个会话加单调递增的 revision，删除时 bump 全局 tombstone，persist 前比对；上面的 `alive` 检查已能消除最常见复活路径，另建议 DeleteConversationByCID 删除 DB 后对同一 cid 做一次惰性防写标记。）

### [internal/utils/media.go:155] DetectAudioFormat 判断 m4a 的 "ftyp" 魔数偏移错误，m4a 永远识别失败 [严重程度：中]
- 严重程度：中（触发条件：输入为 MP4/M4A 容器音频时 100% 触发；唯一调用方 internal/platform/sources/telegram/adapter.go:506 依赖它选择发送 API）
- 问题描述：MP4 家族的 `ftyp` box 位于文件偏移 4-8（前 4 字节是 box 长度），代码却比较了前 4 个字节：
```go
// DetectAudioFormat（124-162 行）
	if bytes.Equal(header[:4], []byte("ftyp")) {
		return "m4a"
	}
```
真实 m4a 文件头是 `00 00 00 20 66 74 79 70`（size+"ftyp"），`header[:4]` 是长度字段，永不相等，函数对 m4a 恒返回 ""，调用方只能走兜底格式，导致 Telegram 语音/音频按错误类型发送。
- 修复建议：
```go
	if len(header) >= 8 && bytes.Equal(header[4:8], []byte("ftyp")) {
		return "m4a"
	}
```

### [internal/toolchain/toolchain.go:102] Ensure 无互斥：并发 provisioning 互相破坏，且损坏的半成品工具链会永久残留 [严重程度：中]
- 严重程度：中（触发条件：首次部署时两个并发触发点同时调用 Ensure——如 dashboard 插件安装（plugin/compiler.go:138、180、314、runtime_prompt.go:172）与启动期自动编译；下载耗时数分钟，碰撞窗口很大）
- 问题描述：`EnsureWithProgress` 先 `GoBin()` 探测、未命中则 `downloadAndExtract`，全程无锁：
```go
func (tc *Toolchain) EnsureWithProgress(progress ProgressFunc) (string, error) {
	if bin, err := tc.GoBin(); err == nil {
		return bin, nil
	}
	return tc.downloadAndExtract(progress)   // ← 两个 goroutine 都会走到这里
}
```
并发后果链：① 两个 `downloadFile` 用 `os.Create(dest)` 写同一归档（截断+交叉写，归档损坏，一侧 checksum 失败）；② 两个 `extractArchive` 向同一 `base` 交错解压同名文件；③ 最关键的——注释声明 "It never overwrites an existing bundle"，一旦部分解压已写入 `<base>/go/bin/go`（tar 顺序里它很靠前），后续所有 `Ensure` 都在 GoBin 处直接返回这个**残缺 bundle**，之后所有插件编译持续失败，需用户手删目录才能恢复。
- 修复建议：
```go
var provisionMu sync.Mutex // 包级，或挂在 Toolchain 上

func (tc *Toolchain) EnsureWithProgress(progress ProgressFunc) (string, error) {
	if bin, err := tc.GoBin(); err == nil {
		return bin, nil
	}
	provisionMu.Lock()
	defer provisionMu.Unlock()
	// 双重检查：等锁期间另一 goroutine 可能已完成 provisioning
	if bin, err := tc.GoBin(); err == nil {
		return bin, nil
	}
	return tc.downloadAndExtract(progress)
}

// downloadAndExtract 内部改为"先解压到临时目录 + 校验 go 可执行 + 原子 rename"，
// 避免中途失败留下残缺 bundle：
//	staging := base + ".staging"
//	extractArchive(dest, staging)
//	if !tc.isExecutable(filepath.Join(staging, "go", "bin", exe("go"))) { return err }
//	os.RemoveAll(filepath.Join(base, "go"))
//	os.Rename(filepath.Join(staging, "go"), filepath.Join(base, "go"))
```

### [internal/core/event_bus.go:342] 出队用 queue[1:] 切片头前移，已消费事件滞留底层数组不被 GC [严重程度：低]
- 严重程度：低（触发条件：队列经历过一次扩容到 maxQueueCap（10 万）后长期低负载运行；滞留上限为当前底层数组容量，属内存滞留而非泄漏）
- 问题描述：
```go
		event := bus.queue[0]
		bus.queue = bus.queue[1:]   // ← 342 行：data 指针前移，头部旧元素仍被底层数组引用
```
`append` 只会在 len 追上 cap 时重新分配，稳态低流量下这个时刻可能永远不来；突发期扩到的大数组会把最多约 10 万个已出队的 `*Event`（每个挂 MessageChain、RawMessage 等）钉在内存里。log.go:304/369 的 `history = history[1:]` 同模式但量级小得多。
- 修复建议：
```go
		event := bus.queue[0]
		bus.queue[0] = nil            // 释放头部引用
		bus.queue = bus.queue[1:]
		// 或定期在长度降到 cap/4 时收缩：copy 到新切片
```

### [internal/log/log.go:368] PublishTrace 按条数淘汰时未扣减 historyBytes，字节记账永久漂移 [严重程度：低]
- 严重程度：低（触发条件：trace 记录持续写入使 history 超过 maxHistory=200 条；后果是 WebUI 日志历史缓冲被提前/过度清空，或在记账偏低时超出 1MB 预算）
- 问题描述：`log()` 的淘汰循环（302-308 行）正确地先减 `entrySize` 再弹出，而 `PublishTrace` 的条数淘汰循环没有对应扣减：
```go
	l.history = append(l.history, entry)
	for len(l.history) > maxHistory {
		l.history = l.history[1:]          // ← 369 行：未执行 historyBytes -= entrySize(...)
	}
	for l.historyBytes > maxHistoryBytes && len(l.history) > 1 {
		l.historyBytes -= entrySize(l.history[0])
		l.history = l.history[1:]
	}
	l.historyBytes += entrySize(entry)
```
每个被条数上限挤出的 trace 条目其字节数永久留在 `historyBytes` 里，账面值单向虚高，随后字节淘汰循环会按真实大小扣减，账面又可能跌破真实值——两个方向都偏离，84-90 行的注释恰好说明作者对"账目漂移"很敏感，此处是漏改。
- 修复建议：
```go
	for len(l.history) > maxHistory {
		l.historyBytes -= entrySize(l.history[0])
		l.history = l.history[1:]
	}
```

### [internal/knowledgebase/vecdb.go:37] OpenVecDB 用原始 kbID 拼接文件名，与调用侧的 sanitize 防御不一致 [严重程度：低]
- 严重程度：低（触发条件：需要 dashboard 鉴权被突破或管理员经 legacy 接口 `?kb_id=../../...`（handlers.go:2951）传入含分隔符的 kbID；RESTful 路径形式受 mux 分段限制无法携带 `/`）
- 问题描述：dashboard 侧对目录做了净化：`kbVecDir` 用 `filepath.Join(..., sanitizePath(kbID))`（dashboard/kb_vec.go:28），但随后把**原始 kbID** 再传进 OpenVecDB 拼文件名：
```go
func OpenVecDB(dir, kbID string, dimension int) (*VecDB, error) {
	...
	path := filepath.Join(dir, kbID+".vec.db")   // ← 37 行：dir 已净化，文件名未净化
```
`kbID="../../evil"` 时 `filepath.Join(dir, "../../evil.vec.db")` 仍会逃出 dir。目录级防御与文件级拼接脱节，属纵深防御缺口（kbID 正常由服务端生成 `kb_%d`，handlers.go:3307）。
- 修复建议：
```go
func OpenVecDB(dir, kbID string, dimension int) (*VecDB, error) {
	safe := filepath.Base(filepath.Clean(kbID)) // 拍平成单段文件名
	if safe == "." || safe == ".." || safe == string(filepath.Separator) {
		return nil, fmt.Errorf("invalid kb id %q", kbID)
	}
	if err := os.MkdirAll(dir, 0o750); err != nil {
		return nil, err
	}
	path := filepath.Join(dir, safe+".vec.db")
	...
```

### [internal/knowledgebase/manager.go:201] 多处 stub / 死代码 / 参数静默丢弃，检索入口恒返回空结果且无告警 [严重程度：低]
- 严重程度：低（触发条件：调用 Manager.Retrieve 的任何未来代码都会拿到"无结果"且不报错；当前无调用方，属潜伏陷阱）
- 问题描述：四个具体点。① `Retrieve` 是未接线 stub，收集完 kbIDs 后直接丢弃：
```go
	if len(kbIDs) == 0 {
		return nil, nil
	}

	// In a full implementation, this would delegate to the retrieval manager.
	// For now, return nil (no results) which the caller handles gracefully.
	return nil, nil   // ← 203 行
```
真实检索在 dashboard/kb_vec.go `kbRetrieve` 里实现，此方法若被误用将静默返回空。② `KBHelper.UploadFromURL`（77 行）签名收下 `batchSize, tasksLimit, maxRetries, progressCB` 四个参数但全部丢弃，只透传 chunkSize/chunkOverlap。③ `conversation.Manager.UpdateConversation`（manager.go:406-413，全仓无调用方）与 `ratelimit.RateLimiter.Wait`（ratelimit.go:71-84，全仓无调用方）为死代码，且 Wait 的 `stall==0` 分支存在忙等隐患。④ `utils.EnsureJPEG/EnsureWAV`（media.go:111-119/164-170）为占位实现，与 Python 版真实转码行为不符，调用方若依赖格式转换会拿到原样文件。
- 修复建议：删除 `Retrieve` 或让其显式返回 `errors.New("retrieval not wired in this manager; use the dashboard KB backend")`，避免"成功返回空"语义；UploadFromURL 未使用的参数要么接线要么从签名移除并更新调用方；删除 UpdateConversation/Wait 死代码；EnsureJPEG/EnsureWAV 至少在返回错误与"原样返回"之间给出可配置行为或文档化声明。

### [internal/toolchain/toolchain.go:240] sha256 校验文件与归档同源下载，镜像即信任锚，校验门形同虚设 [严重程度：低]
- 严重程度：低（触发条件：管理员配置了第三方镜像 ASTRBOT_GO_MIRROR/SetGoMirror；恶意镜像可同时提供匹配的归档与 .sha256，供应链校验被整体绕过——但镜像本身需管理员主动配置）
- 问题描述：
```go
	sumURL := checksumURL(archive)          // ← 240 行
	resp, err := dlClient.Get(sumURL)       // checksumURL 与 downloadURL 共用 mirrorBase()
```
`downloadURL`（305-307）与 `checksumURL`（312-314）都从 `mirrorBase()` 出发，校验值与被校验数据来自同一实体，`verifyChecksum` 只能防传输损坏，防不了被篡改的镜像；注释中"供应链防护"的定位（196 行）与实际强度不符。
- 修复建议：将官方 checksum 内置为代码常量表（按版本固定 `sha256<空格>hash`），或至少当 `mirrorBase` 非 go.dev/dl.google.com 默认值时仍从 `https://dl.google.com/go/<archive>.sha256` 拉取校验值并在日志中标注"镜像下载+官方校验"。
```go
func checksumURL(archive string) string {
	return "https://dl.google.com/go/" + archive + ".sha256" // 校验锚点固定为官方源
}
```

### [internal/skills/manager.go:585] saveSandboxCache 非原子写，与同文件 saveConfig 的原子策略不一致 [严重程度：低]
- 严重程度：低（触发条件：写缓存瞬间进程崩溃/断电；后果是 sandbox_skills_cache.json 截断，下次 loadSandboxCache 静默回退空缓存）
- 问题描述：同文件里 `saveConfig`（524-562 行）精心实现了"临时文件 + fsync + rename"原子替换，而 `saveSandboxCache` 直接 `os.WriteFile`：
```go
func (sm *SkillManager) saveSandboxCache(cache SandboxCache) {
	cache.Version = SandboxCacheVersion
	cache.UpdatedAt = time.Now().UTC().Format(time.RFC3339)
	data, _ := json.MarshalIndent(cache, "", "  ")
	// #nosec G306 -- cache file stores non-sensitive skill metadata; ...
	if err := os.WriteFile(sm.sandboxSkillsCachePath, data, 0600); err != nil {
		logger.Debug("write sandbox skills cache failed: %v", err)
	}
}
```
崩溃窗口内文件截断后，`loadSandboxCache`（570 行）unmarshal 失败静默返回空——sandbox 技能列表悄悄清空，无任何告警日志（且此错误只记 Debug）。
- 修复建议：抽出公共 `atomicWriteFile(path string, data []byte, perm os.FileMode) error`（temp+Sync+Rename），saveConfig 与 saveSandboxCache 共用；loadSandboxCache 的 unmarshal 失败改为 Warn 级日志。

### [internal/persona/manager.go:42] Register 自动 ID 用 len(byID)+1 生成，删除后发生碰撞覆盖既有 persona [严重程度：低]
- 严重程度：低（触发条件：注册未指定 ID 的 persona、期间发生过删除；当前全仓未发现 Register 的运行时调用方，主要为潜伏缺陷）
- 问题描述：
```go
func (m *PersonaManager) Register(p *Persona) {
	m.mu.Lock()
	defer m.mu.Unlock()
	if p.ID == "" {
		p.ID = fmt.Sprintf("persona_%d", len(m.byID)+1)   // ← 42 行
	}
	m.byID[p.ID] = p
```
注册 A、B、C 得 persona_1/2/3，删除 B 后再注册 D：`len(byID)+1 == 3`，D 覆盖 C。同理 `byName` 以 Name 为键，同名注册互相覆盖、Delete 时 `delete(m.byName, p.Name)` 会误删另一个同名 persona 的索引。
- 修复建议：
```go
	if p.ID == "" {
		var n int
		for {
			n++
			if _, exists := m.byID[fmt.Sprintf("persona_%d", n)]; !exists {
				p.ID = fmt.Sprintf("persona_%d", n)
				break
			}
		}
	}
	// 或直接 p.ID = fmt.Sprintf("persona_%d", time.Now().UnixNano())
```

### [internal/contentsafety/contentsafety.go:107] 默认构建下内容安全 fail-open：baidu_aip 开启但 SDK 未编译时静默放行 [严重程度：低]
- 严重程度：低（触发条件：管理员在配置中启用 baidu_aip 但使用默认构建（未加 `-tags baidu_aip`）；所有消息不经审核直接通过，仅启动时打一条 Error 日志）
- 问题描述：
```go
			if !baiduAipSDKEnabled {
				logger.Error("baidu_aip 已开启，但未编译官方 golang-sdk。…当前将 fail-open 不拦截消息。")
			}
			s.checkers = append(s.checkers, NewBaiduAipChecker(appID, apiKey, secretKey))
			s.enabled = true
```
stub 的 `Check` 恒返回 `(true, ...)`（baidu_stub.go:29-31）。fail-open 是文档化的有意设计（避免默认构建强依赖），但对运营者而言"开启了内容安全"与"实际未生效"之间的落差只在日志里体现，`IsEnabled()` 仍返回 true，WebUI 无从感知策略实际未生效；SDK 版解析失败/接口报错时（baidu_sdk.go:51-54）同样放行。
- 修复建议：为 stub 分支增加可观测的状态位（如 `Selector.HasDegradedStrategy() bool`）供 WebUI 展示"baidu_aip 已配置但未生效"；或在 Check 放行的同时按低频节流输出 Warn，避免长期静默；文档化 SDK 版的 fail-open 边界（当前至少有注释说明）。

### [internal/utils/io.go:137] 两个 DownloadFile 实现行为不一致：HTTPClient 版无大小上限，media 版失败不清理半成品文件 [严重程度：低]
- 严重程度：低（触发条件：io.go 版下载超大响应可占满磁盘（无 LimitReader）；media.go 版下载中途失败会在目标路径留下截断文件）
- 问题描述：同包存在两个同名下载实现，防护各缺一半。media.go 版有 64MB 上限与超限报错（67-73 行），但 copy 失败时不清理：
```go
	written, err := io.Copy(f, io.LimitReader(resp.Body, maxDownloadBytes+1))
	if err != nil {
		return err            // ← media.go:68-70：半成品文件留在 destPath
	}
```
io.go 版失败会 `os.Remove` 清理（138-141 行）但完全不设大小上限：
```go
	if _, err := io.Copy(out, resp.Body); err != nil {   // ← io.go:137：无上限
```
- 修复建议：统一收敛为一个实现：io.go 的 `DownloadFile` 补 `io.LimitReader(resp.Body, maxDownloadBytes+1)` 与超限判断；media.go 的失败分支补 `os.Remove(destPath)`（与 io.go:138-141 一致）。两处均可在复制前通过 `resp.ContentLength` 做预检。

---

Part 10/11/12 深度审查报告（全部问题）

- 审查日期：2026-08-24
- 依据：审查流程.md（22 项检查清单）
- 范围：Part 10 Astrbot-golang/dashboard 前端（Vue3+TS）、Part 11 astrbot-go-plugin-sdk（Go）、Part 12 astrbot-golang-plugin-python-sdk 一方代码
- 方式：一次一个子任务；本轮为逐文件精读的深度审查，每条问题均附代码原文与修复代码
- 统计：高危 4、中危 18、低危 30+（明细见各 Part）

---

# Part 10：Astrbot-golang/dashboard 前端（Vue3 + TS）

审查范围：`dashboard/src`（排除 node_modules、dist、src/api/generated）。对 api/、stores/、composables/、router/、utils/、components/chat/、components/shared/ 关键文件及全部主要 views 逐行精读。

## 一、安全类问题

### [dashboard/src/api/http.ts:108-131] 全局劫持 window.fetch，将 Bearer Token 注入页面内一切 fetch 请求（含第三方域名） [严重程度：高]
- 严重程度：高
- 问题所在行号：108-131、133-145（axios 侧 33-45 同理）
- 问题描述：

```ts
export function fetchWithAuth(input: RequestInfo | URL, init?: RequestInit) {
  const fetchImpl = originalFetch ?? window.fetch.bind(window);
  const token = getToken();
  const locale = getLocale();

  if (!token && !locale) {
    return fetchImpl(input, init);
  }
  ...
  if (token && !headers.has(AUTH_HEADER)) {
    headers.set(AUTH_HEADER, `Bearer ${token}`);
  }
  ...
  return fetchImpl(input, { ...init, headers });
}

export function setupHttpClient() {
  ...
  originalFetch = window.fetch.bind(window);
  window.fetch = fetchWithAuth;
  configured = true;
}
```

`setupHttpClient` 把 `window.fetch` 整体替换为 `fetchWithAuth`。此后页面上任何第三方库（markdown 渲染器、图表库等）对任意外部域名的 fetch 请求，只要没有显式设置 Authorization 头，都会被自动附加当前 dashboard 的 Bearer Token。这是**全页面、全协议出口**的 token 泄露通道——未来引入任何请求外部资源的依赖，token 都会随之发出。结合 PluginDetailPage.vue:635-639 直接 `axios.get(readme_url)`（插件市场元数据可控 URL），构成现实凭据窃取链。
- 修复建议：只对同源请求附加 token，不做全局 monkey-patch：

```ts
function isSameOriginRequest(input: RequestInfo | URL): boolean {
  try {
    const url =
      typeof input === 'string'
        ? new URL(input, window.location.origin)
        : input instanceof URL
          ? input
          : new URL(input.url, window.location.origin);
    return url.origin === window.location.origin;
  } catch {
    return false;
  }
}

export function fetchWithAuth(input: RequestInfo | URL, init?: RequestInit) {
  const fetchImpl = originalFetch ?? window.fetch.bind(window);
  const token = getToken();
  const locale = getLocale();

  const shouldAttach =
    (token || locale) &&
    isSameOriginRequest(input) &&
    !(init?.headers instanceof Headers && init.headers.has(AUTH_HEADER));

  if (!shouldAttach) {
    return fetchImpl(input, init);
  }

  const headers = new Headers(init?.headers);
  if (token) headers.set(AUTH_HEADER, `Bearer ${token}`);
  if (locale) headers.set(LOCALE_HEADER, locale);
  return fetchImpl(input, { ...init, headers });
}
```

axios 侧同理：在 `attachAxiosHeaders`（33-45 行）加入对 `config.url` 的同源判断。

### [dashboard/src/api/v1.ts:818-831 + composables/useMessages.ts:832-837 + components/chat/LiveMode.vue:280-291] WebSocket URL 中回退携带 `?token=` 长效凭证 [严重程度：中]
- 严重程度：中
- 问题所在行号：api/v1.ts 818-831（backupApi.downloadUrl 803-805 同理）；useMessages.ts 832-837；LiveMode.vue 280-291；BackupDialog.vue 797-800
- 问题描述：

```ts
  liveWebSocketUrl(ticket: string, token: string, host = window.location.host) {
    const protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:';
    const credential = ticket
      ? `ticket=${encodeURIComponent(ticket)}`
      : `token=${encodeURIComponent(token)}`;
    return `${protocol}//${host}/api/v1/live-chat/ws?${credential}`;
  },
```

ticket 换取失败（后端旧版本、超时 5s、网络抖动）即静默回退把长效 Bearer Token 拼进 URL。URL 会进入浏览器历史补全、代理与反代（nginx/CDN）访问日志、服务端访问日志；且 `getWSTicket` 的 catch 完全吞错（utils/wsTicket.ts:12-14 返回空串），无法区分"端点不存在"与"暂时性失败"。
- 修复建议：ticket 失败时区分场景，仅确认 404（旧后端）才允许回退：

```ts
// utils/wsTicket.ts
export async function getWSTicket(): Promise<string> {
  try {
    const response = await apiV1Client.post('/auth/ws-ticket', undefined, { timeout: 5000 });
    const ticket = response.data?.data?.ticket ?? response.data?.ticket;
    return typeof ticket === 'string' && ticket ? ticket : '';
  } catch (error) {
    const status = (error as { response?: { status?: number } })?.response?.status;
    if (status !== 404) {
      console.warn('ws-ticket fetch failed, refusing token-in-url fallback:', error);
      throw error;
    }
    return '';
  }
}
```

备份下载改走 Authorization 头 + blob：

```ts
const res = await apiV1Client.get(`/backups/${encodeURIComponent(filename)}`, { responseType: 'blob' });
const url = URL.createObjectURL(res.data); // 再触发 a[download]
```

### [dashboard/src/views/PluginPagePage.vue:59-72,522-548] 插件页 postMessage 桥接接受 origin "null"，且响应用 targetOrigin "*" 广播 [严重程度：中]
- 严重程度：中
- 问题所在行号：59-72、527-533（postToIframe）、64-67（origin 校验）
- 问题描述：

```js
const postToIframe = (payload) => {
  const iframeWindow = getIframeWindow();
  if (!iframeWindow) {
    return;
  }
  const targetOrigin =
    typeof iframeMessageOrigin === "string" && iframeMessageOrigin !== "null"
      ? iframeMessageOrigin
      : "*";
  iframeWindow.postMessage(
    { channel: BRIDGE_CHANNEL, ...payload },
    targetOrigin,
  );
};
```

```js
  if (event.origin !== BRIDGE_TARGET_ORIGIN && event.origin !== "null") {
    return;
  }
```

iframe 使用了 `sandbox="allow-scripts allow-forms allow-downloads"`（无 allow-same-origin），origin 恒为 `"null"`，消息校验实际退化为"任何沙箱内文档均可发起桥接请求"；所有桥接响应（含用 dashboard 凭证代理请求到的 API 数据、SSE 数据）都以 `targetOrigin: "*"` 发送。若插件页面内容导航/重定向到任意外部站点，新文档仍占据同一 iframe，可继续接收经过认证的 API 响应。`normalizePluginEndpoint`（95-120 行）的路径防护正确，但通道本身的数据出口不受限。
- 修复建议：用一次性握手 token 锁定通道：

```js
const handleWindowMessage = (event) => {
  const iframeWindow = getIframeWindow();
  if (!iframeWindow || event.source !== iframeWindow) {
    return;
  }
  if (event.origin !== "null" && event.origin !== BRIDGE_TARGET_ORIGIN) return;
  const message = event.data;
  if (!message || message.channel !== BRIDGE_CHANNEL) return;
  if (message.kind === "ready") {
    if (!pageBridgeToken) return;              // loadPluginPage 时 crypto.randomUUID() 生成
    if (message.bridgeToken !== pageBridgeToken) return;
    iframeReady = true;
    sendIframeContext();
    return;
  }
  if (!iframeReady) return;                     // 未完成握手不处理请求
  if (message.kind === "request") {
    void handleBridgeRequest(message);
  }
};
```

### [dashboard/src/stores/common.js:28-154] 日志 SSE 连接在登出后永不停止；done 分支绕过退避固定 2s 重连 [严重程度：中]
- 严重程度：中
- 问题所在行号：common.js 28-154、155-160（方法名 `closeEventSourcet` 拼写错误）
- 问题描述：

```js
          const processStream = ({ done, value }) => {
            if (done) {
              console.log("SSE stream closed");
              setTimeout(() => {
                this.eventSource = null;
                this.createEventSource();
              }, 2000);
              return;
            }
```

```js
    closeEventSourcet() {
      if (this.eventSource) {
        this.eventSource.abort();
        this.eventSource = null;
      }
    },
```

1) 正常 `done`（服务端关闭）也固定 2 秒后无限重连，`sseRetryCount` 只在失败分支递增，done 分支完全绕过退避——服务端主动断开时形成 2s 一次的打雷式重连；2) 登出（auth.ts logout()）从未调用 `closeEventSourcet()`，登出后仍在后台无限重试日志 SSE；3) 方法名拼写错误 `closeEventSourcet`（多一个 t）。
- 修复建议：

```js
// stores/auth.ts logout() 中增加：
const common = useCommonStore();
common.closeEventSource();

// stores/common.js 重命名并统一退避：
closeEventSource() {
  if (this.eventSource) {
    this.eventSource.abort();
    this.eventSource = null;
  }
  this.sse_connected = false;
},
// done 分支：
if (done) {
  this.sse_connected = false;
  const delay = Math.min(30000, 1000 * 2 ** this.sseRetryCount++);
  setTimeout(() => { this.eventSource = null; this.createEventSource(); }, delay);
  return;
}
```

### [dashboard/src/views/Settings.vue:1037-1087] API Key 创建：名称不校验、明文 key 长驻界面 [严重程度：低]
- 严重程度：低
- 问题所在行号：1037-1087、385-393
- 问题描述：

```js
const createApiKey = async () => {
  const selectedScopeSet = new Set(newApiKeyScopes.value);
  ...
  const payload = {
    name: newApiKeyName.value,
    scopes: selectedScopes
  };
```

`newApiKeyName` 可为空串直接创建；`createdApiKeyPlaintext`（385-393 行）创建后一直显示直到下一次创建，无"我已保存"关闭动作，也无超时清除。
- 修复建议：

```js
const createApiKey = async () => {
  const name = newApiKeyName.value.trim();
  if (!name) {
    showToast(tm('apiKey.messages.nameRequired') || '请输入名称', 'warning');
    return;
  }
  ...
  createdApiKeyPlaintext.value = res.data.data?.api_key || '';
  setTimeout(() => { createdApiKeyPlaintext.value = ''; }, 5 * 60 * 1000);
```

### [dashboard/src/components/chat/ChatInput.vue:214-220,920-927 + Chat.vue:1719-1728] 聊天附件上传无文件类型与大小限制 [严重程度：低]
- 严重程度：低
- 问题所在行号：ChatInput.vue 214-220、920-927；Chat.vue 1719-1728；useMediaHandling.ts 18-29
- 问题描述：

```html
          <input
            type="file"
            ref="imageInputRef"
            @change="handleFileSelect"
            style="display: none"
            multiple
          />
```

无 `accept`、无大小上限；`handleFilesSelected` 直接对每个文件走 `processAndUploadFile`（含拖拽、粘贴路径）。误选超大文件会整文件读入内存做 SHA-256（`file.arrayBuffer()` 全量加载）并完整上传，前端无拦截与提示。
- 修复建议：

```ts
const MAX_UPLOAD_BYTES = 4 * 1024 * 1024 * 1024; // 后端上传文件限制也要改为4GB

async function handleFilesSelected(files: FileList | File[]) {
  const selectedFiles = Array.from(files || []);
  for (const file of selectedFiles) {
    if (file.size > MAX_UPLOAD_BYTES) {
      toast.error(tm("input.fileTooLarge", { name: file.name }));
    }
  }
  const okFiles = selectedFiles.filter((f) => f.size <= MAX_UPLOAD_BYTES);
  await Promise.all(
    okFiles.map((file) =>
      file.type.startsWith("image/") ? processAndUploadImage(file) : processAndUploadFile(file),
    ),
  );
}
```

### [dashboard/src/stores/auth.ts:96-113 + AuthLogin.vue:49-57] 登录 returnUrl 读取逻辑失效（hash 路由下 window.location.search 恒空），redirect 全链路死代码 [严重程度：低]
- 严重程度：低
- 问题所在行号：auth.ts 96-113（升级恢复 token 入 sessionStorage）；AuthLogin.vue 50-51；router/index.ts 47
- 问题描述：

```ts
    // @ts-ignore
    authStore.returnUrl = new URLSearchParams(window.location.search).get('redirect');
```

路由使用 hash 模式（createWebHashHistory），`useSessions.ts:44` 跳转的是 `#/auth/login?redirect=/chatbox`，query 在 hash 内，`window.location.search` 恒为空 → returnUrl 永远是 null；且 `finishAuthenticatedSession` 直接 `this.returnUrl = null` 从未消费，路由守卫写入的 `auth.returnUrl = to.fullPath` 同样是死代码——登录后永远进 /welcome 或 onboarding。另外 legacy token 进入 sessionStorage（XSS 可读）。
- 修复建议：

```ts
// AuthLogin.vue
const hash = window.location.hash.replace(/^#/, "");
const redirect = new URLSearchParams(hash.split("?")[1] || "").get("redirect");
authStore.returnUrl = redirect;

// stores/auth.ts finishAuthenticatedSession 末尾：
const redirect = this.returnUrl;
this.returnUrl = null;
if (redirect && redirect.startsWith("/") && !redirect.startsWith("//")) {
  router.push(redirect); // 仅允许站内相对路径，防开放重定向
  return;
}
```

## 二、明显 Bug / 崩溃路径

### [dashboard/src/views/SessionManagementPage.vue:1295-1311] 保存成功后先 closeRuleEditor() 置空 selectedUmo，随后访问 this.selectedUmo.umo 必然 TypeError [严重程度：高（必现功能性 bug）]
- 严重程度：高
- 问题所在行号：1273-1277（closeRuleEditor）、1295-1311；同模式四处：1354-1374（saveProviderConfig）、1414-1428（savePluginConfig）、1470-1484（saveKbConfig）
- 问题描述：

```js
    closeRuleEditor() {
      this.ruleDialog = false
      this.selectedUmo = null
      this.editingRules = {}
    },

    async saveServiceConfig() {
      ...
        if (response.data.status === 'ok') {
          this.showSuccess(this.tm('messages.saveSuccess'))
          this.closeRuleEditor()
          this.editingRules.session_service_config = config

          // 更新或添加到列表
          let item = this.rulesList.find((u) => u.umo === this.selectedUmo.umo)
```

`closeRuleEditor()` 将 `this.selectedUmo` 置为 null，紧接着 `this.selectedUmo.umo` 抛 TypeError。异常被外层 catch 捕获并 showError —— 用户每次保存规则都会看到"错误"提示，且本地列表更新代码全部中断。四个保存方法同模式。
- 修复建议：先缓存 umo、closeRuleEditor 后置：

```js
    async saveServiceConfig() {
      if (!this.selectedUmo) return
      const targetUmo = this.selectedUmo.umo   // 提前缓存
      this.saving = true
      try {
        const config = { ...this.serviceConfig }
        if (!config.custom_name) delete config.custom_name
        if (config.persona_id === null) delete config.persona_id

        const response = await sessionApi.upsertRule({
          umo: targetUmo,
          rule_key: 'session_service_config',
          rule_value: config,
        })

        if (response.data.status === 'ok') {
          this.editingRules.session_service_config = config
          const item = this.rulesList.find((u) => u.umo === targetUmo)
          if (item) {
            item.rules = { ...item.rules, session_service_config: config }
          } else {
            this.rulesList.push(
              this.buildUmoItem(targetUmo, { session_service_config: config }),
            )
          }
          this.showSuccess(this.tm('messages.saveSuccess'))
          this.closeRuleEditor()               // 最后再关闭
        } else {
          this.showError(response.data.message || this.tm('messages.saveError'))
        }
      } catch (error) {
        this.showError(error.response?.data?.message || this.tm('messages.saveError'))
      } finally {
        this.saving = false
      }
    },
```

（saveProviderConfig / savePluginConfig / saveKbConfig 做同样改造。）

### [dashboard/src/views/extension/useExtensionPage.js:235-263,2818-2820] 安装进度轮询 setInterval 在组件卸载时不清理 [严重程度：中]
- 严重程度：中
- 问题所在行号：235-263（startInstallProgressPolling）、2818-2820（onUnmounted 只移除事件监听）、2823-2834（searchDebounceTimer 同漏）
- 问题描述：

```js
    pollTimer = setInterval(async () => {
      try {
        const res = await pluginApi.installProgress(installId);
        ...
      } catch {
        // 轮询失败静默，下次重试
      }
    }, 1000);
```

```js
  onUnmounted(() => {
    window.removeEventListener("astrbot-locale-changed", handleLocaleChange);
  });
```

安装进行中用户切换路由后，`pollTimer` 每秒继续请求 `/plugins/install/progress` 永不停止；`searchDebounceTimer` 也未 clearTimeout。
- 修复建议：

```js
  onUnmounted(() => {
    window.removeEventListener("astrbot-locale-changed", handleLocaleChange);
    stopInstallProgressPolling();
    if (searchDebounceTimer) {
      clearTimeout(searchDebounceTimer);
      searchDebounceTimer = null;
    }
  });
```

### [dashboard/src/views/ConfigPage.vue:516-541] getConfig 无请求序号/取消，快速切换配置文件时旧响应可能覆盖新配置并导致跨文件误写 [严重程度：中]
- 严重程度：中
- 问题所在行号：516-541、712-759
- 问题描述：

```js
    getConfig(abconf_id) {
      this.fetched = false
      const request = this.isSystemConfig
        ? systemConfigApi.get()
        : configProfileApi.get(abconf_id || this.selectedConfigID);

      request.then((res) => {
        this.config_data = res.data.data.config;
        this.lastSavedConfigSnapshot = this.getConfigSnapshot(this.config_data);
        this.fetched = true
```

两个 getConfig 并发时，若先发的请求后返回，界面显示错误配置的内容，且 `lastSavedConfigSnapshot` 被错误内容刷新——此时用户点保存会把 A 配置的内容写进 B 配置文件（updateConfig 使用当前 selectedConfigID，数据却是旧请求的 config_data）。ConversationPage（770-861 行 requestId + AbortController）已示范正确做法，此处未复用。
- 修复建议：

```js
data() { return { configRequestId: 0, ... } },
getConfig(abconf_id) {
  const requestId = ++this.configRequestId;
  this.fetched = false;
  const request = this.isSystemConfig
    ? systemConfigApi.get()
    : configProfileApi.get(abconf_id || this.selectedConfigID);

  request.then((res) => {
    if (requestId !== this.configRequestId) return; // 过期响应直接丢弃
    this.config_data = res.data.data.config;
    this.lastSavedConfigSnapshot = this.getConfigSnapshot(this.config_data);
    this.fetched = true;
    ...
  });
},
```

### [dashboard/src/views/ConfigPage.vue:557-579] validateStatus 放行 401，普通 token 过期时既不清 token 也不提示，保存静默失败 [严重程度：中]
- 严重程度：中
- 问题所在行号：557-579（Settings.vue 893-915 同样模式）
- 问题描述：

```js
        const requestConfig = {
          headers,
          validateStatus: (status) => (status >= 200 && status < 300) || status === 401,
        };
        const res = this.isSystemConfig
          ? await systemConfigApi.update(postData.config, requestConfig)
          : await configProfileApi.update(confId, postData.config, requestConfig);

        if (res.status === 401 && res.data?.data?.totp_required) {
          ...
        }

        if (res.data.status === "ok") { ... }
```

401 未携带 totp_required 时（真正的 token 过期），因为 validateStatus 放行，axios 拦截器的 401 处理（http.ts:48-92 清 token + 跳登录）不会触发；随后落入"保存失败"提示，但 token 仍保留、页面不跳登录，用户重试永远失败且不知原因。
- 修复建议：

```js
        if (res.status === 401) {
          // 普通鉴权失败：清除本地凭证并引导重新登录
          removeToken();
          this.$router.push('/auth/login');
          return { success: false };
        }
        if (res.data.status === "ok") { ... }
```

（Settings.vue 的 saveSystemConfig 做同样补充分支。）

### [dashboard/src/views/CronJobPage.vue:879-891] 删除定时任务无任何确认 [严重程度：低]
- 严重程度：低
- 问题所在行号：879-891（菜单项 161-169 直接调用）
- 问题描述：

```js
async function deleteJob(job: any) {
  try {
    const res = await cronApi.delete(job.job_id);
    if (res.data.status === "ok") {
      toast(tm("messages.deleteSuccess"));
      jobs.value = jobs.value.filter((item) => item.job_id !== job.job_id);
```

菜单里"删除"一点即删，无确认。定时任务是用户长期数据，误触即丢。
- 修复建议：

```js
async function deleteJob(job: any) {
  const message = `确定删除任务 ${job.name || job.job_id} 吗？`;
  if (!(await askForConfirmation(message, useConfirmDialog()))) return;
  try { ... }
```

### [dashboard/src/components/chat/Chat.vue:1438-1439] 发送消息主流程 catch 仅 console.error，消息凭空消失 [严重程度：低]
- 严重程度：低
- 问题所在行号：1385-1444（重点 1438-1439）
- 问题描述：

```js
    sendMessageStream({...});
  } catch (error) {
    console.error("Failed to send message:", error);
  } finally {
    sending.value = false;
```

`newSession()` / `addSessionToProject()` / `getSessions()` 任一失败时，用户输入已被清空（draft 已置 ""），界面无任何提示，消息凭空消失。
- 修复建议：

```js
  } catch (error) {
    console.error("Failed to send message:", error);
    toast.error(
      isAxiosError(error)
        ? error.response?.data?.message || error.message
        : tm("errors.createSessionFailed"),
    );
  } finally {
```

## 三、质量类问题

### [dashboard/src/views/ConversationPage.vue:589,607,870] computed 内 console.log 打印全部消息内容（隐私泄露 + 性能开销） [严重程度：中]
- 严重程度：中
- 问题所在行号：589、607、870
- 问题描述：

```js
        isDark() {
            console.log('isDark', this.customizerStore.uiTheme);
            return this.customizerStore.uiTheme === 'PurpleThemeDark';
        },
```

```js
                .map(msg => {
                    console.log('处理消息:', msg.role, msg.content);
```

`formattedMessages` 是 computed，每条用户/机器人消息（含私聊正文）在每次重算时都被完整打印到控制台；870 行还打印 user_id/cid。打开 DevTools 即可看到会话内容；大对话重算时序列化开销显著。
- 修复建议：删除三处 console.log，或改为 `import.meta.env.DEV` 门控的 debug 函数。

### [dashboard/src/composables/useSessions.ts:34-48 + useConversations.ts:28-55] 两个 composable 近乎整文件复制（约 90% 重复）且已行为漂移 [严重程度：中]
- 严重程度：中
- 问题所在行号：useSessions.ts 全文 / useConversations.ts 全文
- 问题描述：

```ts
    async function getSessions() {
        try {
            const response = await chatApi.listSessions();
            sessions.value = response.data.data;
```

```ts
    async function getConversations() {
        try {
            const response = await chatApi.listSessions();
            conversations.value = (response.data.data || []).map((session: any) => ({
                cid: session.session_id,
```

两 composable 调同一接口、维护两份会话列表与编辑弹窗状态，已出现行为漂移（newSession 会绑定 config，newConversation 不会）；"401 跳转带 redirect 参数但 hash 路由读不到"就在两份代码里各错一次。
- 修复建议：删除 useConversations.ts，统一到 useSessions，ChatBox 场景用适配层映射 cid/title 字段；401 处理统一交给 http.ts 拦截器。

### [dashboard/src/composables/useMessages.ts:1010-1189 + ThreadPanel.vue:~280-338] SSE payload 处理逻辑两份拷贝且已分叉 [严重程度：中]
- 严重程度：中
- 问题所在行号：useMessages.ts 1010-1189（processStreamPayload）；ThreadPanel.vue 300-338
- 问题描述：

```ts
  if (msgType === "plain") {
    markMessageStarted(botRecord);
    if (chainType === "reasoning") {
      appendReasoningPart(botRecord, payloadText(data));
      return;
    }
    ...
    appendPlain(botRecord, payloadText(data), normalized.streaming !== false);
    return;
  }

  if (["image", "record", "file", "video"].includes(msgType)) {
```

拷贝版已分叉：主版处理 `follow_up_captured/run_snapshot/user_message_saved/data: URL` 等事件并异步 `resolvePartMedia`，ThreadPanel 版不支持这些事件、直接 push mediaPart（无 embedded_url 解析），线程内图片在部分后端形态下无法显示；`error`、`complete` 的补齐逻辑也缺失。
- 修复建议：提炼纯函数共享：

```ts
// useMessages.ts
export function applyStreamPayloadToRecord(
  botRecord: ChatRecord,
  payload: unknown,
  opts: { resolveMedia?: (part: MessagePart) => void } = {},
) { /* 现 processStreamPayload 的主体，去掉 connection 相关分支 */ }

// ThreadPanel.vue
import { applyStreamPayloadToRecord } from "@/composables/useMessages";
applyStreamPayloadToRecord(botRecord, payload);
```

### [dashboard/src/composables/useProviderSources.ts:498-543] 几乎所有写操作 finally 中无条件 loadConfig()，一次保存触发全量 schema 拉取 [严重程度：中]
- 严重程度：中
- 问题所在行号：493-496、498-543、621-640、646-659、661-684
- 问题描述：

```js
    } finally {
      savingSource.value = false
      loadConfig()
    }
```

deleteProviderSource/saveProviderSource/addModelProvider/deleteProvider/toggleProviderEnable 全部在 finally 里 loadConfig()（内部 providerApi.schema() 返回全部 providers+sources+model_metadata 的大对象）；saveProviderSource 成功路径已手动更新本地列表，紧接着又被 loadConfig 覆盖。切换 N 个开关 = N 次全量 schema 请求。
- 修复建议：

```js
let loadConfigTimer: number | null = null;
function scheduleLoadConfig() {
  if (loadConfigTimer !== null) return;
  loadConfigTimer = window.setTimeout(() => {
    loadConfigTimer = null;
    void loadProviderTemplate();
  }, 300);
}
// 各 finally 改为 scheduleLoadConfig()
```

### [dashboard/src/layouts/full/vertical-header/VerticalHeader.vue:451-948] 2500 行上帝组件 + 8 处 console.log(err) 吞错误 [严重程度：中]
- 严重程度：中
- 问题所在行号：451、515、553、557、562、578、795、854、926、931、948；全文件
- 问题描述：

```js
      console.log(err);
      ...
      console.log("401");
      ...
      console.log("Route changed:", { ... });
```

该文件承担顶栏、通知、配置保存、路由持久化（localStorage 读写聊天 ID/route）、主题、聊天入口等至少 6 类职责；错误处理大量使用 console.log(err)（非 console.error、无用户提示）。`console.log("Route changed", ...)` 每次路由切换打印完整 fullPath，属日志噪音。同类问题：Chat.vue 2480 行、SessionManagementPage.vue 1984 行、Settings.vue 1881 行。
- 修复建议：按职责拆为 useHeaderConfigSave / useRoutePersistence / useHeaderNotifications composables；console.log(err) 全部替换为 console.error + toast；路由日志删除。

### [dashboard/src/views/SessionManagementPage.vue:1037-1048,1092-1097] loadData 竞态：搜索防抖后仍可能与 onTableOptionsUpdate 并发 [严重程度：低]
- 严重程度：低
- 问题所在行号：1037-1048、1064-1090
- 问题描述：

```js
  watch: {
    searchQuery: {
      handler() {
        if (this.searchTimeout) { clearTimeout(this.searchTimeout) }
        this.searchTimeout = setTimeout(() => { this.onSearchChange() }, 300)
      },
    },
  },
```

loadData 无 requestId/abort，翻页 + 快速输入搜索时两个 listRules 并发，晚到的旧响应会把 rulesList/totalItems 回写为过期分页数据。
- 修复建议：仿 ConversationPage 增加 listRequestId 递增序号，响应回来先比对再落库。

### [dashboard/src/views/ProviderPage.vue:316-337,243-245,450-475] 硬编码中文文案绕过 i18n 体系 [严重程度：低]
- 严重程度：低
- 问题所在行号：316-337、243（另 DashboardTotpSetupDialog.vue:20-21、152-158；useExtensionPage.js:635/644）
- 问题描述：

```html
        <v-card-text class="py-4 text-body-1 text-medium-emphasis">
          Agent 执行器的测试请在「配置文件」页进行。
          <ol class="ml-4 mt-4 mb-4">
```

```html
          <v-btn variant="text" @click="showManualModelDialog = false">取消</v-btn>
          <v-btn color="primary" variant="tonal" @click="confirmManualModel">添加</v-btn>
```

全站其余部分均走 tm()，英文用户在这些位置会看到中文。
- 修复建议：补充 i18n 键并替换为 tm('agentRunner.dialogTitle') 等。

### [dashboard/src/components/shared/ConsoleDisplayer.vue:203-219] 日志去重对每条新日志做 O(n) 线性扫描，整体 O(n²) [严重程度：低]
- 严重程度：低
- 问题所在行号：198-227
- 问题描述：

```js
      newLogs.forEach(log => {

        const exists = this.localLogCache.some(existing => 
          existing.time === log.time && 
          existing.data === log.data &&
          existing.level === log.level
        );
```

缓存上限 200 条，每条新日志 some 全表比对（data 是长字符串比较）。日志爆发时每秒几十条 ×200 次字符串全等比较，会卡 UI 线程。
- 修复建议：

```js
const seen = new Set(this.localLogCache.map(l => `${l.time}|${l.level}|${l.data.length}`));
newLogs.forEach(log => {
  const key = `${log.time}|${log.level}|${log.data?.length ?? 0}`;
  if (!seen.has(key)) { seen.add(key); this.localLogCache.push(log); ... }
});
```

### [dashboard/src/views/Settings.vue:1144-1146] 服务器时钟 tick 每秒触发响应式更新，页面不可见时仍运行 [严重程度：低]
- 严重程度：低
- 问题所在行号：1142-1161、708-734
- 问题描述：

```js
    serverClockTimer.value = window.setInterval(() => {
        serverClockTickMs.value = performance.now();
    }, 1000);
```

serverClockTickMs 是 ref，每秒写入触发依赖它的 computed 与模板更新；document.hidden 时依然运行。
- 修复建议：显示时计算或 shallowRef + 手动更新预览文本；至少 document.visibilitychange 时暂停。

### [dashboard/src/views/extension/useExtensionPage.js:624] toast(err, "error") 直接把 AxiosError 对象交给 snackbar [严重程度：低]
- 严重程度：低
- 问题所在行号：624、1114、1216、1224、1299、580-584（共约 6 处）
- 问题描述：

```js
    } catch (err) {
      toast(err, "error");
    } finally {
```

toast 会把对象渲染为 [object Object]。同文件其他 catch 已用 resolveErrorMessage，此处漏改。
- 修复建议：统一 `toast(resolveErrorMessage(err, tm("messages.operationFailed")), "error")`。

### [dashboard/src/composables/useMessages.ts:163-169] onBeforeUnmount 后仍在途的 blob URL 永不 revoke [严重程度：低]
- 严重程度：低
- 问题所在行号：152-169、205-241
- 问题描述：

```ts
  onBeforeUnmount(() => {
    cleanupConnections();
    for (const promise of attachmentBlobCache.values()) {
      promise.then((url) => URL.revokeObjectURL(url)).catch(() => {});
    }
    attachmentBlobCache.clear();
  });
```

resolvePartMedia 中组件卸载后仍在途的请求完成后会执行 attachmentBlobCache.set（clear 之后重新写入），这些 URL 永不 revoke，长会话多附件时累积泄漏。
- 修复建议：加 disposed 标志，disposed 后新完成的 promise 直接 revoke 且不入 cache。

### [dashboard/src/views/ConfigPage.vue:443-447] originalConfigData 双轨脏检查机制脆弱，deep watch 对大配置做两次全量 JSON.stringify [严重程度：低]
- 严重程度：低
- 问题所在行号：443-447、304-307、336-341、347-354
- 问题描述：

```js
    this.$watch('config_data', (newVal) => {
      if (!this.originalConfigData && newVal) {
        this.originalConfigData = JSON.parse(JSON.stringify(newVal));
      }
    }, { immediate: false, deep: true });
```

configHasChanges（originalConfigData 深比较）与 hasUnsavedChanges（lastSavedConfigSnapshot）两套脏检查并存，watch 中互相赋值；deep watch 对大配置每次变更做两次 JSON.stringify 全量序列化。
- 修复建议：删掉 originalConfigData/configHasChanges 双轨制，仅保留 snapshot 机制。

### [dashboard/src/views/SessionManagementPage.vue:1742-1753,1755-1768,1770-1780] mounted 三接口无并发去重，loadAllSessions 与 loadAvailableUmos 重复调同一 API [严重程度：低]
- 严重程度：低
- 问题所在行号：1742-1753、1755-1768、1770-1780、1051-1055
- 问题描述：

```js
    async loadAvailableUmos() {
      if (this.availableUmos.length > 0) return
      this.loadingUmos = true
      try {
        const response = await sessionApi.activeUmos()
```

mounted 同时调用 loadData/loadGroups/loadAllSessions，其中 loadAllSessions 与 loadAvailableUmos 调同一个 activeUmos()（后者有缓存短路，前者没有）——首屏重复请求；失败时用户只看到空列表无错误提示。
- 修复建议：首屏合并为一次 activeUmos() 结果共享；catch 中补充 showError。

### [dashboard/src/views/CronJobPage.vue:600-603] parseTimeValue 把无效/缺失时间统一折叠为 0 [严重程度：低]
- 严重程度：低
- 问题所在行号：599-603、543-560
- 问题描述：

```js
function parseTimeValue(value: any): number {
  if (!value) return 0;
  const ts = new Date(value).getTime();
  return Number.isNaN(ts) ? 0 : ts;
}
```

一次性任务过期后 run_at 仍存在会继续参与排序，与"无时间"（显示 N/A）混排。
- 修复建议：如需区分"无时间"与"过去时间"，让 parseTimeValue 返回 null 并在 sortedJobs 中单独归组。

### [dashboard/src/router/index.ts:36-53] 路由守卫两套判断来源并存，漏配 meta.requiresAuth 的路由不受保护 [严重程度：中]
- 严重程度：中（假定后端有校验；前端仅 UX 层）
- 问题所在行号：router/index.ts:36-53
- 问题描述：`authRequired` 由 `publicPages.includes(to.path)` 计算，但真正生效的分支是 `to.matched.some(record => record.meta.requiresAuth)`——若某路由（如 ChatBoxRoutes 下）漏配 meta.requiresAuth，即使不在 publicPages 也会被 next() 放行；鉴权也只检查 auth.has_token() 不校验有效性。
- 修复建议：统一以 `meta.requiresAuth` 为唯一来源（默认 true），删除 publicPages 冗余判断。

### [dashboard/src/utils/token.ts:4,9] Token 存 sessionStorage，XSS 可直接窃取 [严重程度：低]
- 严重程度：低（已知权衡，与全局 fetch 注入问题组合会放大）
- 修复建议：保持现状可接受；更进一步可在后端支持短期 access token + refresh cookie（HttpOnly）。

---

# Part 11：AstrbotDev/astrbot-go-plugin-sdk（Go 插件 SDK）

审查范围：client.go、event.go、events.go、host.go（1221 行）、register.go、serve.go（1030 行）、types.go 及全部 _test.go，host.go 与 serve.go 全文逐行读完。

## 发现的问题

### [types.go:243-252] RegisterSessionWait 在 broker 未就绪时完全静默失败，等待永不触发 [严重程度：中]
- 严重程度：中
- 问题所在行号：types.go:243-252
- 问题描述：

```go
	// 反向告知宿主注册等待；失败时仅日志（宿主不支持该特性时返回空 wait_id）。
	if svc, err := hostServiceClient(); err == nil {
		ctx, cancel := hostRPCCtx()
		defer cancel()
		if _, rerr := svc.RegisterSessionWait(ctx, &sdkv1.RegisterSessionWaitRequest{
			Umo:            umo,
			TimeoutSeconds: int32(timeoutSec),
		}); rerr != nil {
			logService().Warn("RegisterSessionWait 上报宿主失败", "umo", umo, "err", rerr)
		}
	}
	return w
```

`hostServiceClient()` 返回 error（errNoBroker——典型场景是插件在 OnLoad 或包初始化阶段注册等待，此时 GRPCServer 还没被调用）时，`err == nil` 分支整体跳过，**连一条日志都没有**。本地 map 里留下 wait，但宿主侧从未登记，FeedSessionWait 永远不会被推送，session_waiter 静默失效。注释声称"失败时仅日志"，实际是"失败时无日志"。
- 修复建议：

```go
	if svc, err := hostServiceClient(); err != nil {
		logService().Warn("RegisterSessionWait 无法连接宿主 HostService，等待可能永远不会触发（是否在 OnLoad 中过早注册？）",
			"umo", umo, "err", err)
	} else {
		ctx, cancel := hostRPCCtx()
		defer cancel()
		if _, rerr := svc.RegisterSessionWait(ctx, &sdkv1.RegisterSessionWaitRequest{
			Umo:            umo,
			TimeoutSeconds: int32(timeoutSec),
		}); rerr != nil {
			logService().Warn("RegisterSessionWait 上报宿主失败", "umo", umo, "err", rerr)
		}
	}
```

### [types.go:233-267 + host.go:1161-1187] wait_id 被丢弃，Unregister 用 UMO 冒充 wait_id，且宿主侧无归属校验可跨插件删等待 [严重程度：中]
- 严重程度：中
- 问题所在行号：types.go:233-267；host.go:1161-1187
- 问题描述：

```go
		if _, rerr := svc.UnregisterSessionWait(ctx, &sdkv1.UnregisterSessionWaitRequest{WaitId: umo}); rerr != nil {
			logService().Warn("UnregisterSessionWait 上报宿主失败", "umo", umo, "err", rerr)
		}
```

(1) 宿主的 RegisterSessionWait hook 明确返回真实 wait_id（host.go:1171-1172），但插件侧把返回值整个丢弃、未存进 SessionWait，事后永远无法按 id 注销；(2) 宿主侧 UnregisterSessionWait（host.go:1185）只做"有身份"校验，没有校验该 wait_id 属于本插件——恶意插件可以枚举其他插件会话的 UMO（格式公开的 platform:type:conv）反复调用注销，破坏其他插件的会话等待。
- 修复建议：

```go
type SessionWait struct {
	UMO string
	// WaitID 是宿主 RegisterSessionWait 返回的凭据；Unregister 时回传，
	// 宿主据此做归属校验。空表示宿主不支持或注册失败。
	WaitID  string
	Handler func(e *Event) bool
}

// RegisterSessionWait 中：
	if resp, rerr := svc.RegisterSessionWait(ctx, ...); rerr != nil {
		logService().Warn("RegisterSessionWait 上报宿主失败", "umo", umo, "err", rerr)
	} else if resp != nil {
		p.sessionWaitsMu.Lock()
		w.WaitID = resp.GetWaitId()
		p.sessionWaitsMu.Unlock()
	}

// UnregisterSessionWait 中：
	waitID := umo
	if w != nil && w.WaitID != "" {
		waitID = w.WaitID
	}
	svc.UnregisterSessionWait(ctx, &sdkv1.UnregisterSessionWaitRequest{WaitId: waitID})
```

宿主侧应由 hook 实现按 (pluginName, waitID) 校验归属后再删除。

### [serve.go:856-866] FeedSessionWait 先删后调：handler panic 或返回 false 都永久丢失等待 [严重程度：中]
- 严重程度：中
- 问题所在行号：serve.go:856-866
- 问题描述：

```go
	if w := s.impl.takeSessionWait(umo); w != nil && w.Handler != nil {
		handled := false
		if err := safeErr(func() error {
			handled = w.Handler(e)
			return nil
		}); err != nil {
			logService().Error("FeedSessionWait: wait handler panic", "umo", umo, "error", err)
			return &sdkv1.FeedSessionWaitResponse{Handled: false}, nil
		}
		return &sdkv1.FeedSessionWaitResponse{Handled: handled}, nil
	}
```

takeSessionWait 在调用 handler 之前就把 wait 删除（one-shot）。后果：(1) handler panic 时 wait 已被消费，插件侧无恢复，宿主侧登记也未注销，宿主继续为该 UMO 推送事件、插件侧永远 handled=false，直到超时；(2) handler 返回 false（"让事件落入正常处理"）时 wait 同样已被删除——与文档直觉不符，用户通常期望"没消费掉就继续等下一条"。
- 修复建议：

```go
	if w := s.impl.takeSessionWait(umo); w != nil && w.Handler != nil {
		handled := false
		if err := safeErr(func() error {
			handled = w.Handler(e)
			return nil
		}); err != nil {
			logService().Error("FeedSessionWait: wait handler panic", "umo", umo, "error", err)
			s.notifyHostWaitConsumed(w) // 尽力注销宿主侧登记，避免僵尸推送
			return &sdkv1.FeedSessionWaitResponse{Handled: false}, nil
		}
		if handled {
			s.notifyHostWaitConsumed(w)
		} else {
			// 未消费：重新放回，等待下一条匹配事件
			s.impl.sessionWaitsMu.Lock()
			s.impl.sessionWaits[umo] = w
			s.impl.sessionWaitsMu.Unlock()
		}
		return &sdkv1.FeedSessionWaitResponse{Handled: handled}, nil
	}
```

### [host.go:866-872] NewConversation 变更宿主状态但无身份校验，与 Delete/Switch 不对称 [严重程度：中]
- 严重程度：中
- 问题所在行号：host.go:866-872（对照 902-944）
- 问题描述：

```go
func (s *hostServiceServer) NewConversation(_ context.Context, req *sdkv1.NewConversationRequest) (*sdkv1.ConversationIDResponse, error) {
	h := getHostHooks()
	if h.NewConversation == nil {
		return &sdkv1.ConversationIDResponse{}, nil
	}
	return &sdkv1.ConversationIDResponse{Cid: h.NewConversation(req.UnifiedMsgOrigin, req.PlatformId, req.PersonaId)}, nil
}
```

DeleteConversation（902）、SwitchConversation（916）、UpdateConversationTitle（930）、UpdateConversationPersonaID（944）都先 `s.requireIdentity()`，唯独同样会改变会话状态的 NewConversation 没有：它会"新建会话并设为当前"，对任意 umo 生效——未绑定身份的调用者可以为任意用户会话创建新会话并切换当前会话，等效于重置该用户的对话上下文。
- 修复建议：入口加 `if err := s.requireIdentity(); err != nil { return nil, err }`。

### [host.go:886-900] GetConversations 空 umo 返回全部用户会话历史，无任何鉴权 [严重程度：中]
- 严重程度：中
- 问题所在行号：host.go:886-900（hook 注释 host.go:379"umo 空 = 全部"）
- 问题描述：

```go
func (s *hostServiceServer) GetConversations(_ context.Context, req *sdkv1.GetConversationsRequest) (*sdkv1.ConversationsResponse, error) {
	h := getHostHooks()
	if h.GetConversations == nil {
		return &sdkv1.ConversationsResponse{}, nil
	}
	resp := &sdkv1.ConversationsResponse{}
	for _, c := range h.GetConversations(req.UnifiedMsgOrigin) {
```

该 RPC 既不 requireIdentity，也没有 GetConfig/SetConfig 那样的自我范围限制：任意插件（甚至空身份连接）传空 UnifiedMsgOrigin 即可导出宿主上所有用户、所有平台的全部会话，含 history（用户与 bot 的完整聊天记录）。隐私敏感数据的大批量泄露面。
- 修复建议：

```go
func (s *hostServiceServer) GetConversations(_ context.Context, req *sdkv1.GetConversationsRequest) (*sdkv1.ConversationsResponse, error) {
	if err := s.requireIdentity(); err != nil {
		return nil, err
	}
	if req.UnifiedMsgOrigin == "" {
		// 全量列举属于宿主级敏感操作，禁止经插件 RPC 进行。
		return nil, status.Error(codes.PermissionDenied, "listing all conversations is not allowed over the plugin RPC")
	}
	...
}
```

### [host.go:1115-1155] SetPluginEnabled/Install/Uninstall 只验"有身份"不验目标，任意插件可接管插件生态 [严重程度：中]
- 严重程度：中
- 问题所在行号：host.go:1115-1155
- 问题描述：

```go
func (s *hostServiceServer) SetPluginEnabled(_ context.Context, req *sdkv1.SetPluginEnabledRequest) (*sdkv1.Empty, error) {
	if err := s.requireIdentity(); err != nil {
		return nil, err
	}
	h := getHostHooks()
	if h.SetPluginEnabled == nil {
		return &sdkv1.Empty{}, nil
	}
	if err := h.SetPluginEnabled(req.PluginName, req.Enabled); err != nil {
```

requireIdentity 语义只是"本连接绑定了某个插件名"，req.PluginName / req.Repo / 卸载目标完全不受限，与 GetConfig/SetConfig 的"只能操作自己名字"形成鲜明对比。组合拳：插件 A 可 `SetPluginEnabled(安全管理插件, false)` 后 `UninstallPlugin(...)`，再 `InstallPlugin(恶意repo)`——InstallPlugin 接受任意 git/url 源，等于把 RCE 安装面暴露给任意已装插件。
- 修复建议：

```go
	if req.PluginName != s.identity() && !hostAdminAuthorized(s.identity()) {
		return nil, status.Errorf(codes.PermissionDenied,
			"插件 %q 无权操作插件 %q（仅允许操作自身，或经宿主授权的管理插件）", s.identity(), req.PluginName)
	}
```

InstallPlugin/UninstallPlugin 直接要求 `hostAdminAuthorized(s.identity())`（宿主经 SetHostHooks 旁注入授权名单，或新增 SetPluginAdminList([]string) API）。

### [host.go:628-641] CallAction 的 params_json 解码失败后 fail-open，以空参数继续执行平台动作 [严重程度：中]
- 严重程度：中
- 问题所在行号：host.go:628-641（SendMessage 658-661 同模式轻量版）
- 问题描述：

```go
	params := map[string]any{}
	if len(req.ParamsJson) > 0 {
		warnJSON("CallAction params_json", json.Unmarshal(req.ParamsJson, &params))
	}
	result, err := h.CallAction(req.Platform, req.Api, params)
```

warnJSON 只打日志不改变控制流。params_json 损坏（或被恶意构造为非法 JSON）时，宿主仍会以空参数执行该平台 API。对 OneBot 的 delete_friend、set_group_kick、delete_msg 这类动作，空参数可能触发缺省行为或意外删除；对 send_msg 则发出空消息。把数据完整性问题升级成了副作用问题。
- 修复建议：

```go
	params := map[string]any{}
	if len(req.ParamsJson) > 0 {
		if err := json.Unmarshal(req.ParamsJson, &params); err != nil {
			warnJSON("CallAction params_json", err)
			return nil, status.Errorf(codes.InvalidArgument, "params_json decode failed: %v", err)
		}
	}
```

SendMessage 同理：chain 解码失败时返回 InvalidArgument 而不是发空链。

### [host.go:544-551, 508-525] hostServers 同 id 覆盖 + dropPluginHostState 误删后继连接的登记（重载竞态） [严重程度：中]
- 严重程度：中
- 问题所在行号：host.go:508-525、544-551
- 问题描述：

```go
	pid := currentHostPluginID()
	server := &hostServiceServer{pluginID: pid, connKey: pid}
	if pid != "" {
		hostServersMu.Lock()
		hostServers[pid] = server
		hostServersMu.Unlock()
	}
```

```go
	hostServersMu.Lock()
	if s, ok := hostServers[connKey]; ok {
		registered = s.identity()
		delete(hostServers, connKey)
	}
	hostServersMu.Unlock()
```

hostServers 以 manifest id 为 key，同 id 只能有一条登记。时序：插件 P 旧连接 A 尚未 Close，宿主已重载 P 并 accept 新连接 B（hostServers[pid] = B 覆盖 A）→ A 的 Client.Close() 执行 dropPluginHostState(pid)，delete 把 **B 的登记删掉**，且限流条目被清；此后 BindHostServiceName(pid, name) 查不到条目，B 的身份永远停在 connKey，GetConfig 等自我访问被 PermissionDenied 拒绝——插件重载后"自己读自己配置"失败（"重载后 401"症状的结构性根因之一）。
- 修复建议：比对 server 指针再删：

```go
func dropPluginHostState(connKey string, srv *hostServiceServer) {
	if connKey == "" {
		return
	}
	hostServersMu.Lock()
	cur, ok := hostServers[connKey]
	if ok && cur == srv { // 仅当登记仍是本连接时才删
		delete(hostServers, connKey)
	}
	hostServersMu.Unlock()
	if !ok || cur == srv { // 无后继连接时才清理限流
		chatLLMRate.drop(connKey)
		callActionRate.drop(connKey)
	}
}
```

对应 Client.Close()（client.go:323-328）改传 server 本身：`dropPluginHostState(c.hostSrvServer.connKey, c.hostSrvServer)`。

### [host.go:99-108] hostRPCCtx 固定 30s 且基于 Background，ChatLLM/HtmlRender 等长操作必然被截断 [严重程度：中]
- 严重程度：中
- 问题所在行号：host.go:99-108
- 问题描述：

```go
// hostRPCTimeout 是插件→宿主反向调用的默认超时。宿主 hook 是第三方代码，
// 卡死时插件 handler 不能无限阻塞（26-7），与 python-sdk 的 30-180s timeout 对齐。
const hostRPCTimeout = 30 * time.Second

func hostRPCCtx() (context.Context, context.CancelFunc) {
	return context.WithTimeout(context.Background(), hostRPCTimeout)
}
```

(1) Host.ChatLLM 走同一 30s 上限，而 LLM 生成（工具循环、长上下文、推理模型）普遍超过 30s——插件任何绕过宿主管线的直接 LLM 调用都会在 30s 处被掐断，功能性 bug 级别的默认值；python-sdk 对齐的是 30-180s，这里只取了下界；(2) 基于 context.Background()，宿主取消插件调用后反调仍在跑，出现双层 30s 竞速。
- 修复建议：

```go
const (
	hostRPCTimeout = 30 * time.Second
	hostLLMTimeout = 180 * time.Second // LLM/渲染类长操作
)

func hostLLMRPCCtx() (context.Context, context.CancelFunc) {
	return context.WithTimeout(context.Background(), hostLLMTimeout)
}
```

ChatLLMFull（host.go:234-246）与 TextToImage/HtmlRender 改用 hostLLMRPCCtx()；理想方案是 Host 各方法接受可选 context.Context 透传。

### [serve.go:150-163] 预连接 goroutine 20 次重试全败后静默退出，插件永久失去反向调用能力 [严重程度：中]
- 严重程度：中
- 问题所在行号：serve.go:150-163
- 问题描述：

```go
		go func() {
			// 宿主 accept 9000 发生在 Dispense（插件 GRPCServer 之后），
			// 需稍候 ConnInfo 到达；循环重试直到缓存成功。
			for i := 0; i < 20; i++ {
				if _, err := hostServiceClient(); err == nil {
					return
				}
				time.Sleep(250 * time.Millisecond)
			}
		}()
```

go-plugin broker 的 ConnInfo 约 5s 后被 timeoutWait 删除。该 goroutine 5 秒内（20×250ms）拨号全部失败时**无任何输出地退出**，此后每次 handler 里 hostServiceClient() 重新 Dial 但 ConnInfo 已被删，**永久超时**——插件所有 Host.* 调用挂 30s 后报 DeadlineExceeded，且没有任何日志解释原因。极难排查的"插件一启动就废"模式。
- 修复建议：

```go
		go func() {
			for i := 0; i < 20; i++ {
				if _, err := hostServiceClient(); err == nil {
					return
				}
				time.Sleep(250 * time.Millisecond)
			}
			logService().Error("预连接宿主 HostService 失败（broker ConnInfo 即将过期），" +
				"本插件的反向调用（GetConfig/SendMessage/ChatLLM 等）可能永久不可用")
		}()
```

### [serve.go:208-211, 285-289, 833-840] nil schema 序列化输出字面量 "null" 而非 "{}"，宿主端 nil map 写入 panic [严重程度：中]
- 严重程度：中
- 问题所在行号：serve.go:208-211、285-289、833-840
- 问题描述：

```go
	schema, err := json.Marshal(s.impl.ConfigSchema)
	if err != nil {
		schema = []byte("{}")
	}
```

err != nil 分支处理了 marshal 错误，但最常见的情形——字段为 nil——不产生错误：`json.Marshal(nil map)` 成功返回 `null`。未声明 ConfigSchema 的插件上报 `ConfigSchemaJson: "null"`，未写 ParamsSchema 的工具上报 `ParamsJson: "null"`。宿主端 `json.Unmarshal("null", &m)` 把目标 map 置为 nil（不报错），随后宿主若对 nil map 写入（合并默认值）即 panic。测试未覆盖 nil 字段路径。
- 修复建议：

```go
	marshalSchema := func(v map[string]any) []byte {
		if v == nil {
			return []byte("{}")
		}
		b, err := json.Marshal(v)
		if err != nil {
			return []byte("{}")
		}
		return b
	}
```

Register、工具循环、GetConfigSchema 三处均改用。

### [host.go:786-803] SetChatLLMRateLimit 无法关闭限流、不影响 callActionRate，注释与实现矛盾 [严重程度：低]
- 严重程度：低
- 问题所在行号：host.go:786-803
- 问题描述：

```go
var chatLLMRate = &rateTable{maxPer: maxChatLLMPerMinute, table: map[string]rateWindow{}}

var callActionRate = &rateTable{maxPer: maxChatLLMPerMinute, table: map[string]rateWindow{}}

// SetChatLLMRateLimit 调整每插件每分钟 ChatLLM 反向调用上限（<=0 表示关闭
// 限流），供宿主在启动前配置（26-6）。
func SetChatLLMRateLimit(perMinute int) int {
	chatLLMRate.mu.Lock()
	defer chatLLMRate.mu.Unlock()
	old := chatLLMRate.maxPer
	if perMinute > 0 {
		chatLLMRate.maxPer = perMinute
	}
	return old
}
```

(1) 注释说"<=0 表示关闭限流"，但 `if perMinute > 0` 使 <=0 成为 no-op，文档承诺的关闭能力不存在；(2) setter 只调 chatLLMRate，同源的 callActionRate 没有任何配置入口；(3) rateTable.allow 的 r == nil 分支（745-747 行）意味着若真用 maxPer<=0 实现关闭，nil 表反而全部放行，方向正好相反。
- 修复建议：

```go
func SetChatLLMRateLimit(perMinute int) int {
	old := setRateLimit(chatLLMRate, perMinute)
	setRateLimit(callActionRate, perMinute)
	return old
}

func setRateLimit(r *rateTable, perMinute int) int {
	r.mu.Lock()
	defer r.mu.Unlock()
	old := r.maxPer
	if perMinute > 0 {
		r.maxPer = perMinute
	}
	return old
}
```

同时删掉注释里的"（<=0 表示关闭限流）"或真正实现关闭。

### [host.go:744-762] rateTable 固定窗口任意起点，边界突刺可达 2 倍上限；drop 后插件重载即重置限流窗口 [严重程度：低]
- 严重程度：低
- 问题所在行号：host.go:744-762（配合 508-525 dropPluginHostState）
- 问题描述：

```go
	r.mu.Lock()
	defer r.mu.Unlock()
	now := time.Now()
	e := r.table[id]
	if e.window.IsZero() || now.Sub(e.window) >= time.Minute {
		e.window = now
		e.count = 0
	}
	e.count++
	r.table[id] = e
	return e.count <= r.maxPer
```

(1) 窗口起点是首次调用时刻而非整分钟对齐，边界突刺可达 2 倍上限（窗口尾+新窗口头各 maxPer）；(2) dropPluginHostState 在连接关闭时 drop 限流条目——反复触发插件重载即可每次拿到全新 30 次配额，限流可被周期性绕过。
- 修复建议（对齐整分钟）：

```go
	bucket := now.Truncate(time.Minute)
	e := r.table[id]
	if !e.window.Equal(bucket) {
		e.window = bucket
		e.count = 0
	}
```

### [host.go:41-91] hostServiceClient 持锁拨号阻塞所有反调方；setBroker 关闭旧 conn 可击落飞行中的 RPC [严重程度：低]
- 严重程度：低
- 问题所在行号：host.go:41-91
- 问题描述：

```go
	hostMu.Lock()
	defer hostMu.Unlock()
	if hostDialDone && hostSvc != nil {
		return hostSvc, nil
	}
	conn, err := b.DialWithOptions(HostServiceAppID, ...)
```

(1) b.DialWithOptions 在持有 hostMu 期间执行，若卡住（accept 端异常），所有插件的 Host.* 反调在锁上排队，放大 30s 超时成全局停摆；(2) setBroker 关闭旧 hostConn 时，飞行中的 RPC 收到 connection is closed，表现为偶发不可复现错误。
- 修复建议：dial 移出锁（专用拨号锁 + 双检缓存）：

```go
	brokerMu.RLock()
	b := broker
	brokerMu.RUnlock()
	if b == nil {
		return nil, errNoBroker
	}
	hostMu.Lock()
	if hostDialDone && hostSvc != nil {
		svc := hostSvc
		hostMu.Unlock()
		return svc, nil
	}
	hostMu.Unlock() // 先放锁再拨号

	hostDialMu.Lock()
	defer hostDialMu.Unlock()
	hostMu.Lock()
	if hostDialDone && hostSvc != nil { /* 双检返回 */ }
	hostMu.Unlock()
	conn, err := b.DialWithOptions(...)
	...
```

### [serve.go:949-959] WebAPI.Methods 为空时路由永不匹配，静默 404 [严重程度：低]
- 严重程度：低
- 问题所在行号：serve.go:949-959
- 问题描述：

```go
	for _, w := range s.impl.WebAPIs {
		match := false
		for _, m := range w.Methods {
			if strings.EqualFold(m, method) || strings.EqualFold(m, "ANY") {
				match = true
				break
			}
		}
		if !match {
			continue
		}
```

插件作者漏填 Methods 时 w.Methods 为空切片，match 恒 false，路由静默 404。Register 阶段不校验、运行时不告警。
- 修复建议：空 Methods 规范为 ["ANY"]：

```go
		methods := w.Methods
		if len(methods) == 0 {
			methods = []string{"ANY"}
		}
```

### [serve.go:989-1006] Web handler 错误原文（含完整堆栈）写入 500 响应体；status=0 未归一 [严重程度：低]
- 严重程度：低
- 问题所在行号：serve.go:989-1006
- 问题描述：

```go
		if handlerErr != nil {
			errBody, _ := json.Marshal(map[string]string{"status": "error", "message": handlerErr.Error()})
			return &sdkv1.HandleWebRequestResponse{
				StatusCode: 500,
				Body:       errBody,
			}, nil
		}
		out := &sdkv1.HandleWebRequestResponse{StatusCode: int32(status), Body: body}
```

(1) handlerErr.Error() 含 panic 时 safeErr 拼入的完整 goroutine 堆栈（serve.go:129 debug.Stack()），原样返回给 dashboard HTTP 客户端——泄露源码路径、插件内部结构；(2) handler 返回 status=0（零值）时代理层写出非法 HTTP 状态。
- 修复建议：

```go
		if handlerErr != nil {
			logService().Error("WebAPI handler failed", "route", w.Route, "error", handlerErr)
			errBody, _ := json.Marshal(map[string]string{"status": "error", "message": "internal error"})
			return &sdkv1.HandleWebRequestResponse{StatusCode: 500, Body: errBody}, nil
		}
		if status < 100 || status > 599 {
			status = 200
		}
```

### [serve.go:330-364] HandleCommand 错误语义与 HandleTool/WebRequest 三种入口三种语义 [严重程度：低]
- 严重程度：低
- 问题所在行号：serve.go:330-364（对照 775-792、989-1001）
- 问题描述：

```go
		if c.ChainHandler != nil {
			var chain []Component
			var err error
			if cerr := safeErr(func() error {
				chain, err = c.ChainHandler(e, req.Args)
				return err
			}); cerr != nil {
				return nil, cerr
			}
```

```go
		if c.Handler == nil {
			return &sdkv1.HandleCommandResponse{}, nil
		}
```

(1) 命中命令但 ChainHandler == nil && Handler == nil 时返回空响应（Handled=false），宿主无法区分"命令不存在"和"存在但没执行"；(2) panic/错误以 gRPC error 返回，HandleTool 把同样错误转 IsError=true 正常响应、HandleWebRequest 转 500——宿主侧极易漏记 plugin_error。
- 修复建议：统一为"内联错误 + 显式标记"并全量应用；至少补齐 nil-handler 分支的 Result 与注释中写明三入口错误语义对照。

### [serve.go:547-551] invokeResultHook 入站 chain_json 解码失败仍被静默丢弃 [严重程度：低]
- 严重程度：低
- 问题所在行号：serve.go:547-551
- 问题描述：

```go
	var chain []Component
	if len(req.ChainJson) > 0 {
		_ = json.Unmarshal(req.ChainJson, &chain)
	}
```

client.go 侧 handleHook 的出站 chain 解码失败已加 logWarnf（client.go:197），但插件侧 invokeResultHook 对入站 chain_json 仍 `_ =` 完全静默：损坏时 handler 以空链为基础"装饰"，最终把空链写回 resp.ChainJson——宿主侧表现为"回复消息被结果钩子清空"，无任何日志线索。
- 修复建议：

```go
	if len(req.ChainJson) > 0 {
		if err := json.Unmarshal(req.ChainJson, &chain); err != nil {
			logService().Warn("invokeResultHook: 入站 chain_json 解码失败，以空链继续", "error", err)
		}
	}
```

### [serve.go:1019-1030] eventFromJSON 解码失败返回零值 Event 并照常分发 [严重程度：低]
- 严重程度：低
- 问题所在行号：serve.go:1019-1030
- 问题描述：

```go
func eventFromJSON(b []byte) *Event {
	if len(b) == 0 {
		return &Event{}
	}
	var e Event
	if err := json.Unmarshal(b, &e); err != nil {
		logService().Warn("eventFromJSON: invalid event JSON, decoding to zero value", "error", err)
		return &Event{}
	}
	return &e
}
```

空/损坏 payload 有 Warn 但不阻断分发：HandleCommand/HandleFilter/HandleHook/HandleTool/FeedSessionWait 全部以零值 Event（SenderID=""、IsAdmin=false）调用插件 handler。基于 e.SenderID 做去重/记账的插件产生脏数据；基于 IsAdmin 做权限的命令在事件损坏时以"非管理员"执行（fail-safe 方向对，但纯靠巧合）。
- 修复建议：加 strict 变体供命令/工具入口使用：

```go
func eventFromJSONStrict(b []byte) (*Event, error) {
	if len(b) == 0 {
		return &Event{}, nil
	}
	var e Event
	if err := json.Unmarshal(b, &e); err != nil {
		return nil, status.Errorf(codes.InvalidArgument, "event_json decode failed: %v", err)
	}
	return &e, nil
}
```

### [client.go:319-341] Close() 先 dropPluginHostState 再 Stop 服务器，在途控制面 RPC 期间状态已被清空 [严重程度：低]
- 严重程度：低
- 问题所在行号：client.go:319-341
- 问题描述：

```go
	if c.hostSrvServer != nil {
		// 清理该连接遗留的宿主侧状态（hostServers 登记 + 限流窗口），
		// 避免表只增不减（26-3）。用 connKey（manifest id）清除。
		dropPluginHostState(c.hostSrvServer.connKey)
		c.hostSrvServer = nil
	}
	if c.hostSrv != nil {
		c.hostSrv.Stop()
```

dropPluginHostState 先删登记和限流条目，然后才 Stop()。Stop 之前仍在飞行中的 RPC（如插件 Cleanup 钩子里最后的 GetConfig/SetConfig）命中"登记已删"状态；配合 hostServers 覆盖竞态时，这个窗口放大了误删后继连接的概率。
- 修复建议：先 Stop 再清表，并传 server 指针做归属比对：

```go
func (c *Client) Close() error {
	if c == nil {
		return nil
	}
	if c.hostSrv != nil {
		c.hostSrv.Stop()
		c.hostSrv = nil
	}
	if c.hostLis != nil {
		_ = c.hostLis.Close()
		c.hostLis = nil
	}
	if c.hostSrvServer != nil {
		dropPluginHostState(c.hostSrvServer.connKey, c.hostSrvServer)
		c.hostSrvServer = nil
	}
	if c.conn != nil {
		return c.conn.Close()
	}
	return nil
}
```

### [host.go:1193-1217] UnregisterBridgeHook 无 requireIdentity，与 Register 不对称 [严重程度：低]
- 严重程度：低
- 问题所在行号：host.go:1193-1217
- 问题描述：

```go
func (s *hostServiceServer) UnregisterBridgeHook(_ context.Context, req *sdkv1.BridgeHookRequest) (*sdkv1.Empty, error) {
	h := getHostHooks()
	if h.UnregisterBridgeHook == nil {
		return &sdkv1.Empty{}, nil
	}
	if err := h.UnregisterBridgeHook(s.identity(), req.HookName); err != nil {
```

Register 侧显式拒绝空身份，Unregister 侧没有。更现实的风险：已绑定身份 A 的连接在身份被 BindHostServiceName 更名为 B 后，A 名下的旧钩子无法被本连接注销、B 名义可注册同名钩子造成宿主侧双份登记。
- 修复建议：入口加 `if err := s.requireIdentity(); err != nil { return nil, err }`。

### [host.go:596-611] SetCurrentHostPluginID 依赖宿主串行加载，存在身份错绑窗口 [严重程度：低]
- 严重程度：低
- 问题所在行号：host.go:596-611、544-551
- 问题描述：全局 hostPluginID + hostServers 按 manifest id 键控。若宿主并发 Dispense 两个插件（或 manifest id 相同/为空），accept 时的 pid 可能取到另一个插件的 id，导致 GetConfig/SetConfig 的身份隔离被张冠李戴；pid 为空时 BindHostServiceName 静默 no-op 无日志。
- 修复建议：改为显式参数传递而非全局变量；至少对 pid=="" 打 Warn，宿主侧对 Dispense 加互斥。

### [serve.go:878-935] webRouteCache 为包级全局且无失效机制 [严重程度：低]
- 严重程度：低
- 问题所在行号：serve.go:878-883、891-935
- 问题描述：缓存假设"路由集合在 Register 后固定"，但 impl.WebAPIs 是可变字段且 HandleWebRequest 每次实时读取；插件运行时修改 WebAPIs 时旧正则仍被复用。
- 修复建议：文档化"WebAPIs 必须在 Serve 前固定"，或以 []WebAPI 快照为 key。

### [serve.go:22-43] ProtocolVersion 与新旧字段双写并存，无版本协商纪律 [严重程度：低]
- 严重程度：低（维护性）
- 问题所在行号：serve.go:22-43
- 问题描述：`ProtocolVersion: 1` 与 proto 中并存的"新旧字段双写"（sent/stop/handled 与 result 并存，client.go:95-104 normalizeResult）说明协议已实质演化而握手版本未动——一旦某次改动破坏兼容，没有任何机制在握手期拦住。
- 修复建议：在 Handshake 注释中写明版本纪律：行为不兼容变更时必须 bump ProtocolVersion；新增字段/新增 RPC 不需要 bump。

---

# Part 12：astrbot-golang-plugin-python-sdk 一方 Python 代码

审查范围：astrbot/ 全部一方 .py（_bridge/、core/、api/ 逐行读完，dispatch.py 1202 行全文读完）、tests/run_tests.py 快读。排除第三方捆绑（aiocqhttp/botpy/slack_sdk/telegram/mcp）、proto 生成代码、temp/。

## 发现的问题

### [astrbot/core/utils/shared_preferences.py:151-163] `_persist` 序列化键只用 scope，同 scope 所有条目互相覆盖（持久化数据丢失） [严重程度：高]
- 严重程度：高
- 问题所在行号：151-163（及加载侧 135-148）
- 问题描述：

```python
    def _persist(self) -> None:
        with _lock:
            raw = {
                json.dumps(k[0], ensure_ascii=False): {
                    "scope": k[0],
                    "scope_id": k[1],
                    "key": k[2],
                    "value": deepcopy(v),
                }
                for k, v in self._cache.items()
            }
            # 锁内落盘：避免并发 _save 交错写同一 tmp 文件
            self._save(raw)
```

raw 的键是 `json.dumps(k[0])`——只用了三元组 (scope, scope_id, key) 的第一个元素 scope。dict 推导中同 scope 的所有条目键完全相同，后面的条目覆盖前面的。触发条件：任意两次 put_async 的 scope 相同（如两个会话各写一个 session_put(umoA, "k1")、session_put(umoB, "k2")，scope 均为 "umo"）。进程内缓存 _cache 仍正确，但每次 _persist 落盘只剩同 scope 的最后一条；进程重启后其余偏好全部静默丢失。跨插件共享偏好存储核心路径上的严重数据丢失缺陷。
- 修复建议：键必须包含完整三元组：

```python
    def _persist(self) -> None:
        with _lock:
            raw = {
                json.dumps([k[0], k[1], k[2]], ensure_ascii=False): {
                    "scope": k[0],
                    "scope_id": k[1],
                    "key": k[2],
                    "value": deepcopy(v),
                }
                for k, v in self._cache.items()
            }
            self._save(raw)
```

（_ensure_cache 的旧格式展平分支仍能兼容新格式值；若需兼容已写坏的旧文件，可在加载时按值三元组重建键。）

### [astrbot/_bridge/dispatch.py:875-923] `_run_web_handler` 的 quart/astrbot ContextVar 在常驻 loop 上不生效 [严重程度：高]
- 严重程度：高
- 问题所在行号：899-917（根因在 loop.py:40-49 run_coro）
- 问题描述：

```python
        fake = FakeQuartRequest(plugin_req, path_params)
        # 注入 quart 全局（插件代码 from quart import request/jsonify/session 等）。
        # gRPC 线程池复用线程，ContextVar 会残留到下一个请求 → 必须 reset。
        cv_tokens: list[tuple[Any, Any]] = []
        try:
            from quart.globals import _cv_app, _cv_request

            cv_tokens.append((_cv_app, _cv_app.set(FakeQuartAppCtx())))
            cv_tokens.append((_cv_request, _cv_request.set(fake)))
        except Exception as e:
            logger.debug(f"quart 全局上下文注入失败: {e}")

        try:
            with bind_request_context(plugin_req):
                bound = _bind(handler, self.inst)
                kwargs = dict(path_params)
                results = _call(bound, **kwargs)
                return results[-1] if results else None
```

_cv_app.set / _cv_request.set / bind_request_context 都在 gRPC handler 线程上执行；但 _call 内部对 async handler 走 loop.run_coro（asyncio.run_coroutine_threadsafe），协程在常驻 loop 线程运行。run_coroutine_threadsafe 的任务继承 loop 线程当时的 context，而不是 gRPC 线程的 context——插件 Web handler 协程里 `from quart import request` 抛 "working outside of request context"，`from astrbot.api.web import request` 抛 LookupError 被 Proxy 转成 RuntimeError。所有使用全局 request 的 Web API handler 全部失败。
- 修复建议：把上下文注入移进协程内部（在 loop 线程上 set/reset），同步 handler 仍走 gRPC 线程路径：

```python
    def _run_web_handler(self, handler, req, path_params: dict):
        ...
        fake = FakeQuartRequest(plugin_req, path_params)
        bound = _bind(handler, self.inst)
        kwargs = dict(path_params)

        async def _run_with_ctx():
            # 在常驻 loop 线程内注入 ContextVar，协程体内可见
            cv_tokens = []
            try:
                from quart.globals import _cv_app, _cv_request
                cv_tokens.append((_cv_app, _cv_app.set(FakeQuartAppCtx())))
                cv_tokens.append((_cv_request, _cv_request.set(fake)))
            except Exception as e:
                logger.debug(f"quart 全局上下文注入失败: {e}")
            try:
                with bind_request_context(plugin_req):
                    return await bound(**kwargs)
            finally:
                for var, token in reversed(cv_tokens):
                    try:
                        var.reset(token)
                    except Exception:
                        pass

        result = handler(**kwargs)  # 探测同步/异步
        if inspect.iscoroutine(result):
            result.close()
            return loop.run_coro(_run_with_ctx(), timeout=HANDLER_TIMEOUT)
        # 同步 handler：直接在 gRPC 线程注入执行（原路径）
        ...
```

### [astrbot/_bridge/server.py:141-153] `add_insecure_port` 绑定失败返回 0 而非抛异常，握手行会宣告端口 0 [严重程度：中]
- 严重程度：中
- 问题所在行号：141-156
- 问题描述：

```python
        bound_port = None
        min_port, max_port = go_handshake.port_range()
        if min_port and max_port:
            for port in range(min_port, max_port + 1):
                try:
                    bound_port = server.add_insecure_port(f"127.0.0.1:{port}")
                    break
                except Exception:
                    continue
            if bound_port is None:
                raise RuntimeError(
                    f"无法在 PLUGIN_MIN_PORT={min_port}..PLUGIN_MAX_PORT={max_port} 范围内绑定端口"
                )
```

grpc-python 的 add_insecure_port 绑定失败时不抛异常，返回 0。上述代码把 0 当成功值 break，随后握手行打出 `...|tcp|127.0.0.1:0|grpc|`，宿主连接 0 端口失败，整个启动卡在 go-plugin 的连接等待上，错误毫无指向性。触发条件：端口范围内全被占用。
- 修复建议：

```python
            for port in range(min_port, max_port + 1):
                try:
                    bound = server.add_insecure_port(f"127.0.0.1:{port}")
                except Exception:
                    continue
                if bound:  # 0 表示绑定失败
                    bound_port = bound
                    break
            if not bound_port:
                raise RuntimeError(
                    f"无法在 PLUGIN_MIN_PORT={min_port}..PLUGIN_MAX_PORT={max_port} 范围内绑定端口"
                )
```

### [astrbot/_bridge/dispatch.py:381-386, 730-737, 374-377] 插件异常原文与参数错误原文直接回传聊天 [严重程度：中]
- 严重程度：中
- 问题所在行号：382-386、734-736、374-376
- 问题描述：

```python
        bound = _bind(handler, self.inst)
        try:
            results = _call(bound, event, **params)
        except Exception as e:
            logger.error(f"命令 {request.name} 执行失败: {e}")
            resp.text = f"插件执行失败: {e}"
            _set_result(resp, event, handled=True)
            return resp
```

```python
        except Exception as e:
            logger.error(f"工具 {request.name} 执行失败: {e}")
            resp = plugin_pb2.HandleToolResponse(
                text=f"工具 {request.name} 执行失败: {e}", is_error=True
            )
```

异常文本常含本地文件路径、API 端点、连接串（如 sqlite:///...、openai api key invalid: sk-...）。项目自带 error_redaction.py 的 safe_error（provider/manager.py 已在用），此处未过。
- 修复建议：

```python
        except Exception as e:
            from astrbot.core.utils.error_redaction import safe_error
            logger.error(f"命令 {request.name} 执行失败: {e}")
            resp.text = safe_error("插件执行失败: ", e)
```

HandleTool 处同理：`text=safe_error(f"工具 {request.name} 执行失败: ", e), is_error=True`。

### [astrbot/_bridge/dispatch.py:414-463] HandleFilter 三条失败路径全部 fail-open [严重程度：中]
- 严重程度：中
- 问题所在行号：424-427、431-447、450-453、463
- 问题描述：

```python
                md = star_handlers_registry.get_handler_by_full_name(request.name)
                if md is not None:
                    ...
                        except Exception as e:
                            # 过滤器抛异常按"不拦截"放行，但记录日志便于排查。
                            logger.warning(f"过滤器 {name} 的 {f} 执行异常，放行: {e}")
                            return plugin_pb2.HandleFilterResponse(allow=True)
                ...
                except Exception as e:
                    logger.error(f"过滤器 {request.name} 执行失败: {e}")
                    return plugin_pb2.HandleFilterResponse(allow=True)
        return plugin_pb2.HandleFilterResponse(allow=True)
```

fail-open 共四条路径：(1) self.filter_handlers 里找不到 request.name（463 行，热重载后注册表快照过时）；(2) **get_handler_by_full_name 返回 None 时完全跳过过滤器直接执行 handler**（431-437 的 if 不成立则落到 448 行 bound = _bind(...)，权限过滤器形同虚设）——最危险；(3) 单个过滤器抛异常（444-447）；(4) handler 执行抛异常（450-453）。安全语义上，权限过滤器异常应 fail-closed。
- 修复建议：

```python
                md = star_handlers_registry.get_handler_by_full_name(request.name)
                if md is None:
                    # 注册表 miss：无法证明该事件被授权，拒绝而不是放行
                    logger.error(f"HandleFilter: 找不到 {request.name} 的注册元数据，拒绝")
                    return plugin_pb2.HandleFilterResponse(allow=False)
                ...
                    try:
                        if not f.filter(event, cfg):
                            return plugin_pb2.HandleFilterResponse(allow=True)
                    except Exception as e:
                        logger.warning(f"过滤器 {name} 的 {f} 执行异常: {e}")
                        return plugin_pb2.HandleFilterResponse(allow=False)
```

（handler 执行异常路径保留 allow=True 是合理的——事件确已处理；463 行 miss 分支同样改 allow=False。）

### [astrbot/core/message/components.py:747-766 + 115-125] `File.file` property 在事件循环内同步阻塞等待下载（每次新建线程池） [严重程度：中]
- 严重程度：中
- 问题所在行号：747-766（property）、115-125（_download_sync）
- 问题描述：

```python
    @property
    def file(self) -> str:
        if self._file_cache is not None:
            return self._file_cache
        ...
        if self.file_.startswith("http"):
            self._file_cache = _download_sync(self.file_)
            return self._file_cache
```

```python
def _download_sync(url: str, suffix: str = "") -> str:
    """同步下载封装：事件循环运行时切到线程池执行，避免
    opener.open(timeout=30) 冻结事件循环；无运行中循环时直接下载。"""
    try:
        asyncio.get_running_loop()
    except RuntimeError:
        return _download_to_temp(url, suffix)
    import concurrent.futures

    with concurrent.futures.ThreadPoolExecutor(max_workers=2) as ex:
        return ex.submit(_download_to_temp, url, suffix).result()
```

_download_sync 虽把下载移进线程，但随后 `.result()` 无超时阻塞调用线程——当插件在 async handler 中访问 f.file 时，调用线程就是常驻 loop 线程，loop 仍被冻结最长 30s，期间所有其他命令/钩子停摆。另外每次调用都新建并销毁 ThreadPoolExecutor(2)。get_file()（异步版）是正确做法，但 property 是插件最常用的访问点。
- 修复建议：

```python
_DOWNLOAD_EXECUTOR: concurrent.futures.ThreadPoolExecutor | None = None

def _download_sync(url: str, suffix: str = "") -> str:
    try:
        asyncio.get_running_loop()
    except RuntimeError:
        return _download_to_temp(url, suffix)
    logging.getLogger("astrbot").warning(
        "File.file 在事件循环内触发了同步下载（最长阻塞 30s），"
        "请改用 await file.get_file()"
    )
    global _DOWNLOAD_EXECUTOR
    if _DOWNLOAD_EXECUTOR is None:
        _DOWNLOAD_EXECUTOR = concurrent.futures.ThreadPoolExecutor(
            max_workers=4, name_prefix="file-download"
        )
    return _DOWNLOAD_EXECUTOR.submit(_download_to_temp, url, suffix).result()
```

### [astrbot/core/utils/message_history_manager.py:190-243] 全部 async 接口在事件循环内做同步磁盘 IO + threading.Lock；insert 持锁全量重写 [严重程度：中]
- 严重程度：中
- 问题所在行号：221-243（insert），265-273（get）等
- 问题描述：

```python
        key = self._key(str(platform_id), str(user_id))
        with self._lock:
            record = PlatformMessageHistory(
                id=self._next_id,
                ...
            )
            data = self._load()
            records = data.setdefault(key, [])
            records.append(record.to_dict())
            if max_messages and len(records) > max_messages:
                del records[: len(records) - max_messages]
            self._next_id += 1
            self._save()
        return record
```

insert/get/delete/update/clear 都是 async def，但内部全程同步：self._lock（threading.Lock）+ _save() 的 json.dump 全量写盘都在常驻 loop 线程上执行。历史文件接近上限（2000 条 × N 个会话）后每次 insert 都是全量序列化 + 全量写，几百 KB 到数 MB 的同步写盘直接冻结事件循环；_lock 还会与其他持锁慢操作形成跨模块隐性串行点。
- 修复建议：锁与磁盘 IO 移入 asyncio.to_thread，锁内逻辑抽成同步私有方法：

```python
    def _insert_sync(self, platform_id, user_id, content, sender_id,
                     sender_name, llm_checkpoint_id, max_messages):
        key = self._key(str(platform_id), str(user_id))
        with self._lock:
            record = PlatformMessageHistory(
                id=self._next_id, ...,
            )
            data = self._load()
            records = data.setdefault(key, [])
            records.append(record.to_dict())
            if max_messages and len(records) > max_messages:
                del records[: len(records) - max_messages]
            self._next_id += 1
            self._save()
            return record

    async def insert(self, ...) -> PlatformMessageHistory:
        ...
        return await asyncio.to_thread(
            self._insert_sync, platform_id, user_id, content, sender_id,
            sender_name, llm_checkpoint_id, max_messages,
        )
```

（get/delete/update/clear 同样包 asyncio.to_thread。）

### [astrbot/core/utils/shared_preferences.py:165-169, 181-189] `get_async`/`range_get_async` 在事件循环线程持锁做同步读盘与逐条 deepcopy [严重程度：中]
- 严重程度：中
- 问题所在行号：165-169、181-189、135-149（_ensure_cache）
- 问题描述：

```python
    async def get_async(self, scope: str, scope_id: str, key: str, default: _VT = None) -> _VT:
        with _lock:
            self._ensure_cache()
            value = self._cache.get((scope, scope_id, key), _MISSING)
            return default if value is _MISSING else deepcopy(value)
```

两个读接口虽名为 _async，但直接在事件循环线程执行：(1) 首次调用 _ensure_cache 同步读盘解析整个 shared_preferences.json；(2) _lock 是模块级 threading.RLock，与 put_async（已正确移入 to_thread）的持锁写盘互斥——任意一次并发 put_async 落盘期间，get_async 在 loop 线程上阻塞等锁直到写盘完成；(3) range_get_async 对全部命中条目逐个 deepcopy，大值（图片 base64 等）时开销可观。
- 修复建议：

```python
    def _get_sync(self, scope, scope_id, key, default):
        with _lock:
            self._ensure_cache()
            value = self._cache.get((scope, scope_id, key), _MISSING)
            return default if value is _MISSING else deepcopy(value)

    async def get_async(self, scope, scope_id, key, default=None):
        return await asyncio.to_thread(self._get_sync, scope, scope_id, key, default)
```

（range_get_async 同法。）

### [astrbot/_bridge/loader.py:274-278, 303-311] 同步 `initialize()`/`terminate()` 会在 run_coro 处崩溃且异常被吞 [严重程度：中]
- 严重程度：中
- 问题所在行号：274-278、303-311
- 问题描述：

```python
    # 生命周期 initialize()
    init = getattr(inst, "initialize", None)
    if init is not None:
        loop.run_coro(init(), timeout=INIT_TIMEOUT)
        logger.info(f"插件 {metadata.module.__name__} initialize() 完成")
```

```python
    for name in ("terminate", "shutdown"):
        fn = getattr(inst, name, None)
        if fn is None:
            continue
        try:
            loop.run_coro(fn(), timeout=TERM_TIMEOUT)
        except Exception as e:
            logger.warning(f"插件 {metadata.name} {name}() 失败: {e}")
        break
```

loop.run_coro 直接调 asyncio.run_coroutine_threadsafe(coro, loop)，传入非协程时抛 TypeError。插件生态中存在定义同步 `def initialize(self)` 或 `def terminate(self)` 的插件（本体对两者都兼容）。后果：同步 initialize() → 实例化阶段抛 TypeError，插件启动失败，错误消息对作者毫无指向性；同步 terminate() → TypeError 被 except 吞成一行 warning，**清理逻辑完全没执行**（文件句柄、后台线程、宿主侧等待不注销）。
- 修复建议：按返回值类型分发：

```python
    init = getattr(inst, "initialize", None)
    if init is not None:
        result = init()
        if inspect.iscoroutine(result):
            loop.run_coro(result, timeout=INIT_TIMEOUT)
        logger.info(f"插件 {metadata.module.__name__} initialize() 完成")
```

```python
    for name in ("terminate", "shutdown"):
        fn = getattr(inst, name, None)
        if fn is None:
            continue
        try:
            result = fn()
            if inspect.iscoroutine(result):
                loop.run_coro(result, timeout=TERM_TIMEOUT)
        except Exception as e:
            logger.warning(f"插件 {metadata.name} {name}() 失败: {e}")
        break
```

### [astrbot/core/utils/cron_manager.py:199-216] AsyncIOScheduler 懒启动只尝试一次，失败后任务登记但永不触发 [严重程度：中]
- 严重程度：中
- 问题所在行号：199-216、248-254
- 问题描述：

```python
    def _ensure_started(self) -> None:
        """懒启动：仅在首次 add_job 时尝试一次。"""
        if self._start_attempted:
            return
        self._start_attempted = True
        try:
            if not self._inner.running:
                self._inner.start()
                logger.info("cron_manager 调度器已懒启动")
        except Exception as e:
            logger.warning(
                f"cron_manager 调度器懒启动失败（任务已登记但不触发）: {e}"
            )
```

AsyncIOScheduler.start() 要求在事件循环线程内调用。插件的 schedule_jobs() 是同步代码，通常在 __init__（gRPC/主线程，无运行循环）里执行；此时 start() 抛 RuntimeError，_start_attempted 已置 True，**此后永远不会重试**。结果：apscheduler 可用时定时任务静默不跑，日志只有一条 warning——签到、日报类插件最难排查的"无声失效"。降级路径 _MinimalScheduler 反而能跑（线程调度），行为不一致。
- 修复建议：懒启动失败后允许在事件循环上下文中重试；更彻底的方案是让 CronJobManager 在 SDK 常驻 loop 就绪后由 loader 显式 run_coro(self.scheduler.start())：

```python
    def add_job(self, *args, **kwargs):
        self._ensure_started()
        if not self._inner.running:
            try:
                import asyncio as _asyncio
                try:
                    _asyncio.get_running_loop()
                    in_loop = True
                except RuntimeError:
                    in_loop = False
                if in_loop:
                    self._start_attempted = True
                    self._inner.start()
            except Exception as e:
                logger.debug(f"cron_manager 调度器启动重试失败: {e}")
        return self._inner.add_job(*args, **kwargs)
```

### [astrbot/core/utils/plugin_kv_store.py:41-59] kv_store 非原子写、无锁读改写竞争、async 方法内同步 IO [严重程度：中]
- 严重程度：中
- 问题所在行号：41-59
- 问题描述：

```python
    def _kv_save(self, data: dict) -> None:
        import json

        with open(self._kv_path(), "w", encoding="utf-8") as f:
            json.dump(data, f, ensure_ascii=False, indent=2)

    async def put_kv_data(self, key: str, value) -> None:
        data = self._kv_load()
        data[key] = value
        self._kv_save(data)
```

三个叠加缺陷：(1) **非原子写**——open("w") 直接截断目标文件再写，进程在写入中途被 SIGTERM 杀掉（宿主停插件正是发 SIGTERM）会留下半个 JSON，下次 _kv_load 静默返回 {}，插件全部 KV 数据丢失且无告警；(2) **无锁读-改-写**——两个并发 put_kv_data 各自 load 旧快照、各自覆盖保存，后写者覆盖前写者的 key；(3) 三个 async def 全在事件循环线程做同步磁盘读写。对比同项目 message_history_manager、shared_preferences 都已用 mkstemp+os.replace 原子写，唯独此处遗漏。
- 修复建议：

```python
_KV_LOCK = threading.Lock()

    def _kv_save(self, data: dict) -> None:
        path = self._kv_path()
        fd, tmp = tempfile.mkstemp(dir=os.path.dirname(path) or ".", suffix=".tmp")
        try:
            with os.fdopen(fd, "w", encoding="utf-8") as f:
                json.dump(data, f, ensure_ascii=False, indent=2)
            os.replace(tmp, path)
        except Exception:
            try:
                os.remove(tmp)
            except OSError:
                pass
            raise

    def _mutate_sync(self, fn) -> None:
        with _KV_LOCK:
            data = self._kv_load()
            fn(data)
            self._kv_save(data)

    async def put_kv_data(self, key: str, value) -> None:
        await asyncio.to_thread(self._mutate_sync, lambda d: d.__setitem__(key, value))

    async def delete_kv_data(self, key: str) -> None:
        def _del(d: dict) -> None:
            d.pop(key, None)
        await asyncio.to_thread(self._mutate_sync, _del)
```

### [astrbot/_bridge/host.py:156-166 等] `set_config` 恒返回 True、无异常处理；`react`/`recall_message`/`send_message` 同样裸调 RPC [严重程度：低]
- 严重程度：低
- 问题所在行号：156-166、196-208、236-253、320-327
- 问题描述：

```python
    def set_config(self, plugin_name: str, cfg: dict) -> bool:
        if not self.ensure_connected():
            return False
        self._stub.SetConfig(
            plugin_pb2.SetConfigRequest(
                plugin_name=plugin_name or self.plugin_name,
                config_json=json.dumps(cfg).encode(),
            ),
            timeout=30,
        )
        return True
```

set_config 只要连接成功就返回 True：宿主侧校验失败时 RpcError 直接向上抛而不是返回 False，调用方据此判断"配置已保存"。同文件 30+ 个方法都有 try/except grpc.RpcError → warning → 返回失败值，react/send_message/recall_message 这四个例外。更糟的是 event.send() 中异常抛出发在 `self._has_send_oper = True` 之后，管线会认为"已发送"而跳过 LLM 兜底，用户得不到任何回复。
- 修复建议：统一为与同文件一致的风格：

```python
    def set_config(self, plugin_name: str, cfg: dict) -> bool:
        if not self.ensure_connected():
            return False
        try:
            self._stub.SetConfig(
                plugin_pb2.SetConfigRequest(
                    plugin_name=plugin_name or self.plugin_name,
                    config_json=json.dumps(cfg).encode(),
                ),
                timeout=30,
            )
            return True
        except grpc.RpcError as e:
            logger.warning(f"SetConfig 失败: {e}")
            return False
```

（react/send_message/recall_message 同法；event.send() 侧把 _has_send_oper = True 移到发送成功之后再置位。）

### [astrbot/_bridge/host.py:97-110, 128-149] `get_config` 在 ensure_connected 与实际调用之间存在 `_stub` 置空竞态 [严重程度：低]
- 严重程度：低
- 问题所在行号：97-110、128-149
- 问题描述：

```python
    def ensure_connected(self, attempts: int = PRECONNECT_ATTEMPTS) -> bool:
        with self._lock:
            if self._stub is not None and self._probed:
                return True
        if self._stub is not None and self._probe_alive():
            return True
        self._teardown()
        ...
```

ensure_connected 返回 True 后，get_config 在锁外读取 self._stub。若此刻另一线程探测失败触发 _teardown()（把 _stub 置 None 并关闭 channel），本线程的 self._stub.GetConfig 抛 AttributeError——不在 except grpc.RpcError 捕获范围内，直接冲进插件 __init__（get_config 是启动热路径），重试逻辑失效。触发条件：宿主重启期间多线程并发调用宿主桥。
- 修复建议：调用点持锁取 stub 局部引用：

```python
        for attempt in range(PRECONNECT_ATTEMPTS):
            if not self.ensure_connected(attempts=1):
                last_err = RuntimeError("宿主桥未连接")
            else:
                with self._lock:
                    stub = self._stub
                if stub is None:
                    last_err = RuntimeError("宿主桥连接已被并发重建")
                    time.sleep(PRECONNECT_INTERVAL)
                    continue
                try:
                    resp = stub.GetConfig(...)
```

### [astrbot/_bridge/loader.py:240-256] `__init__` TypeError 回退基于异常文本匹配，且可能触发双重实例化副作用 [严重程度：低]
- 严重程度：低
- 问题所在行号：240-256
- 问题描述：

```python
    try:
        inst = star_cls(context, config)
    except TypeError as e:
        if (
            "config" not in str(e)
            and "positional argument" not in str(e)
            and "argument" not in str(e)
        ):
            raise
        logger.info(
            f"插件 {metadata.name or metadata.root_dir_name} 的 __init__ 不接受 "
            f"config 参数，回退为仅 context 实例化"
        )
        inst = star_cls(context)
```

(1) 匹配条件过宽——__init__ 内部任何 TypeError 消息含 "argument" 一词（如 `int() argument must be a string...`）都会被误判为签名不匹配，真实初始化 bug 被掩盖；(2) 第一次构造可能已执行部分副作用（建线程、发网络请求）后抛 TypeError，第二次构造再跑一遍，副作用翻倍且第一次的资源无人回收。
- 修复建议：用 inspect.signature 预检代替异常文本匹配：

```python
    def _accepts_config(cls) -> bool:
        try:
            sig = _inspect.signature(cls.__init__)
            params = list(sig.parameters.values())[1:]
            for p in params:
                if p.name == "config" and p.kind in (
                    _inspect.Parameter.POSITIONAL_OR_KEYWORD,
                    _inspect.Parameter.KEYWORD_ONLY,
                ):
                    return True
                if p.kind is _inspect.Parameter.VAR_KEYWORD:
                    return True
            return False
        except (TypeError, ValueError):
            return True

    try:
        inst = star_cls(context, config) if _accepts_config(star_cls) else star_cls(context)
    except TypeError:
        ...  # 保留原回退作兜底
```

### [astrbot/core/utils/session_waiter.py:226-237] `_cleanup` 在无运行循环的线程上经 get_event_loop().create_task 注销，任务可能永不执行 [严重程度：低]
- 严重程度：低
- 问题所在行号：226-237
- 问题描述：

```python
                    if loop is None:
                        try:
                            loop = asyncio.get_event_loop()
                        except RuntimeError:
                            loop = None
                    if loop is not None:
                        loop.create_task(bridge.unregister_session_wait_async(wait_id))
```

_cleanup 从两条路径进入：事件循环线程（正常超时）和 gRPC/其他线程（异常清理）。当处于后者且主线程无运行循环时，get_event_loop() 返回一个**新建的、从未 run 的 loop**，create_task 在未运行的循环上抛 RuntimeError（被吞成 debug 日志）或创建了永不执行的任务——宿主侧 session wait 登记都不会被注销。3.12 起 get_event_loop 已弃用。
- 修复建议：直接复用 SDK 常驻 loop：

```python
        if wait_id:
            try:
                from astrbot._bridge import loop as _sdk_loop
                from astrbot.core.star.context import get_host_bridge

                bridge = get_host_bridge()
                if bridge is not None:
                    _sdk_loop.get_loop().create_task(
                        bridge.unregister_session_wait_async(wait_id)
                    )
            except Exception as e:
                logger.debug(f"UnregisterSessionWait({wait_id}) 失败: {e}")
```

### [astrbot/core/utils/session_waiter.py:38-39, 314, 209-211] FILTERS 全局列表是死状态：从不参与匹配 [严重程度：低]
- 严重程度：低
- 问题所在行号：38-39、314、209-211
- 问题描述：

```python
USER_SESSIONS: dict[str, "SessionWaiter"] = {}
FILTERS: list["SessionFilter"] = []
```

```python
            session_id = session_filter.filter(event)
            FILTERS.append(session_filter)
```

```python
    def _cleanup(self, error: Exception | None = None) -> None:
        USER_SESSIONS.pop(self.session_id, None)
        try:
            FILTERS.remove(self.session_filter)
        except ValueError:
            pass
```

全文件检索：FILTERS 只有声明、append、remove 三处出现，**没有任何匹配逻辑读取它**（try_trigger 遍历的是 USER_SESSIONS，用各 waiter 自带的 filter 现算）。因此：(1) 模块 docstring 宣称的"插件可 FILTERS.append(selection_filter) 后自行管理"完全无效；(2) 同一 SessionFilter 实例被两个并发 waiter 复用时，第一个结束会把实例 remove 掉，状态表失真；(3) waiter 异常路径 _cleanup 被调用两次（except + finally），第二次靠 ValueError 兜底。对照本体：本体 star_manager 确实用 FILTERS 做事件预分发，SDK 移植漏掉了读取侧。
- 修复建议：收敛 _cleanup 为只调一次（finally 单点），并让 try_trigger 尊重手动登记的过滤器或明确删掉死状态。

### [astrbot/core/conversation_mgr.py:355-377] `get_human_readable_context` 丢弃末尾 user 消息组，且 insert(0,...) 为 O(n²) [严重程度：低]
- 严重程度：低
- 问题所在行号：355-377
- 问题描述：

```python
        contexts_groups: list[list[str]] = []
        temp_contexts: list[str] = []
        for record in history:
            ...
            if role == "user":
                temp_contexts.append(f"User: {record.get('content')}")
            elif role == "assistant":
                ...
                contexts_groups.insert(0, temp_contexts)
                temp_contexts = []
```

(1) 循环结束后 temp_contexts 里残留的末尾连续 user 消息（最新一条提问）从未被计入——用户最关心的"刚问了什么"被丢弃，/history 永远少最后一轮；(2) insert(0, ...) 头部插入 O(n)，长历史整体 O(n²)。原版 AstrBot-py 同函数同样如此，属"忠实移植了原版缺陷"。
- 修复建议：

```python
            elif role == "assistant":
                ...
                contexts_groups.append(temp_contexts)
                temp_contexts = []
        if temp_contexts:
            contexts_groups.append(temp_contexts)  # 末尾残留的 user 消息也要计入
        contexts_groups.reverse()  # 时间正序，O(n)
```

### [astrbot/core/utils/message_history_manager.py:307, 325] delete_by_id/update 对非数字 id 记录抛未捕获 ValueError [严重程度：低]
- 严重程度：低
- 问题所在行号：307、325
- 问题描述：

```python
                new_records = [r for r in records if int(r.get("id", -1) or -1) != message_id]
```

裸 int() 转换记录 id。文件中只要存在一条历史被外部工具写成非数字 id（"abc"、uuid 字符串），int("abc") 抛 ValueError：delete_by_id 在遍历第一个坏记录的 key 时即中断（后续 key 全部跳过），update 同样整条失败。对比同文件 _load（160-163）已对非数字 id 做了 try/except 兜底，这两处漏了。
- 修复建议：

```python
    @staticmethod
    def _record_id(r: dict) -> int:
        try:
            return int(r.get("id", -1) or -1)
        except (TypeError, ValueError):
            return -1
```

两处 int(...) 改为 self._record_id(r)。

### [astrbot/_bridge/dispatch.py:995-1002] FakeQuartRequest 的 url/query_string/host 是硬编码占位值 [严重程度：低]
- 严重程度：低
- 问题所在行号：995-1002
- 问题描述：

```python
        self.method = plugin_req.method
        self.path = plugin_req.path
        self.url = plugin_req.path
        self.full_path = plugin_req.path
        self.query_string = ""
        self.endpoint = plugin_req.path
        self.host = ""
        self.remote_addr = plugin_req.client_host or ""
```

query_string 恒为 ""（插件做签名校验、OAuth 回调、webhook 验证时校验必然失败）；url 缺 scheme 与 query；host 恒空串（url_for 外部地址、CORS、Host 校验全部异常）。而 req.query 与 plugin_req.headers 里明明有构造这些值所需的全部数据。
- 修复建议：

```python
        host = (
            plugin_req.headers.get("host")
            or plugin_req.headers.get("x-forwarded-host")
            or "localhost"
        )
        self.host = host
        pairs = plugin_req.query.multi_items()
        from urllib.parse import quote
        self.query_string = "&".join(
            f"{quote(str(k), safe='')}={quote(str(v), safe='')}" for k, v in pairs
        )
        self.url = f"http://{host}{plugin_req.path}" + (
            f"?{self.query_string}" if self.query_string else ""
        )
        self.full_path = self.url if self.query_string else plugin_req.path
```

### [astrbot/_bridge/dispatch.py:1086-1099] FakeMultiDict.get 取"最后一个"匹配值，与 werkzeug"第一个"语义相反 [严重程度：低]
- 严重程度：低
- 问题所在行号：1086-1099
- 问题描述：

```python
    def get(self, key, default=None, type=None):
        val = None
        for k, v in reversed(self._pairs):
            if k == key:
                val = v
                break
        ...
```

reversed 使 get 返回最后一次出现的值；werkzeug/quart 的 MultiDict.get 返回第一次出现的值。重复 query 参数（?id=1&id=2）时插件拿到的 id 与本体运行时相反。同项目 api/web.py 的 PluginMultiDict.get（25-36 行）用正向遍历、语义正确，两个 MultiDict 实现互相矛盾。
- 修复建议：

```python
    def get(self, key, default=None, type=None):
        # werkzeug MultiDict 语义：返回第一次出现的值
        for k, v in self._pairs:
            if k != key:
                continue
            if type is not None:
                try:
                    return type(v)
                except (TypeError, ValueError):
                    return default
            return v
        return default
```

### [astrbot/core/config/astrbot_config.py:133-188] load_config/put_config/update_config 的 namespace 未做路径清洗（路径穿越） [严重程度：低]
- 严重程度：低
- 问题所在行号：133-148、150-173、176-188
- 问题描述：

```python
    @staticmethod
    def load_config(namespace: str) -> dict | bool:
        path = os.path.join(
            get_astrbot_data_path(), "config", f"{namespace}.json"
        )
```

namespace 直接拼进文件路径，无任何清洗。`namespace="../../somewhere/evil"` 时 load_config 可读任意 .json（信息泄露），put_config/update_config 可在任意可写路径创建/改写 .json。该 SDK 的威胁模型里插件是半受信代码、宿主 config/ 下有 WebUI 密码配置，穿越读写是真实的越权面。
- 修复建议：

```python
    @staticmethod
    def _safe_namespace(namespace: str) -> str:
        if not namespace or not re.fullmatch(r"[A-Za-z0-9_\-]+", namespace):
            raise ValueError(f"非法的配置 namespace: {namespace!r}")
        return namespace
```

三个入口开头均加 `namespace = AstrBotConfig._safe_namespace(namespace)`。

### [astrbot/core/utils/active_event_registry.py:34-40] unregister 对未登记的 umo 触发 defaultdict 空 set 泄漏 [严重程度：低]
- 严重程度：低
- 问题所在行号：34-40
- 问题描述：

```python
    def unregister(self, event: "AstrMessageEvent") -> None:
        umo = getattr(event, "unified_msg_origin", "")
        self._agent_stop_callbacks.pop(event, None)
        self._events[umo].discard(event)
        if umo and not self._events[umo]:
            del self._events[umo]
```

self._events 是 defaultdict(set)。unregister 一个未登记过的事件（或 umo 为 ""）时，self._events[umo] 会创建一个空 set；清理条件对 umo == "" 不成立，空 set 永久留在字典里，长期运行缓慢累积。
- 修复建议：

```python
        bucket = self._events.get(umo)
        if bucket is not None:
            bucket.discard(event)
            if not bucket:
                del self._events[umo]
```

### [astrbot/_bridge/dispatch.py:336-341 + core/star/context.py:586-591] 运行期 `register_web_api` 不会上报宿主，路由静默 404 [严重程度：低]
- 严重程度：低
- 问题所在行号：dispatch.py 336-341、context.py 586-591
- 问题描述：

```python
        # Web APIs（context.register_web_api）
        for route, _, methods, desc in self.web_apis:
            resp.web_apis.append(
                plugin_pb2.WebApiDesc(route=route, methods=list(methods), description=desc)
            )
```

```python
        self._web_apis.append((route, handler, methods, desc))
        logger.info(f"register_web_api: {methods} {route} — {desc}")
```

resp.web_apis 只在 Register RPC（启动一次性）时快照上报。插件在 initialize()（晚于 Register，tools 有 ListTools 实时拉取的补救，web_apis 没有）或运行期调用 context.register_web_api 时，本地更新、日志提示成功，但宿主网关路由表里没有这条 route——所有请求 404，且无任何错误指向原因。HandleWebRequest 用的也是启动快照。
- 修复建议：短期——文档化 + 运行期注册时告警；正式修复需宿主补一个 ListWebApis 实时拉取 RPC（对齐 ListTools 模式）。

### [astrbot/_bridge/dispatch.py:685-694] HandleLLMRequest 只回传 system_prompt，插件对 prompt/图片等修改静默丢弃 [严重程度：低]
- 严重程度：低
- 问题所在行号：675-694
- 问题描述：

```python
        req = ProviderRequest(
            prompt=request.user_prompt,
            system_prompt=request.system_prompt,
        )
        ...
        for r in results:
            if r is None:
                continue
            if isinstance(r, ProviderRequest):
                req = r
        resp.system_prompt = req.system_prompt or ""
        resp.stop = bool(getattr(req, "stop", False))
```

钩子签名与本体一致地传入完整 ProviderRequest（含 prompt、contexts、image_urls），插件在 on_llm_request 里改写 req.prompt / 追加图片是本体生态的常见用法。但响应只回填 system_prompt——req.prompt 的一切修改被静默丢弃，插件侧看似生效、实际 LLM 收到的是原始 user_prompt，且无日志提示。
- 修复建议：宿主 proto 增加 user_prompt 字段后回传；过渡期至少 logger.debug 记录被丢弃的修改。

### [astrbot/_bridge/dispatch.py:929-936] `_serialize_web_result` 对 `(body, status, headers)` 三元组丢弃 headers [严重程度：低]
- 严重程度：低
- 问题所在行号：929-936（FakeQuartApp.make_response 1184-1189 同样只处理二元组）
- 问题描述：

```python
        if isinstance(result, tuple) and len(result) >= 2 and isinstance(result[1], int):
            result, status = result
```

quart/flask 允许 handler 返回三元组。这里解包只取前两项，第三项 headers 被静默丢弃——插件设置 Content-Disposition、Set-Cookie、CORS 头的响应全部失效，状态码却生效，行为半对半错最难排查。
- 修复建议：

```python
        if isinstance(result, tuple) and len(result) >= 2 and isinstance(result[1], int):
            status = result[1]
            if len(result) >= 3:
                raw_headers = result[2]
                if isinstance(raw_headers, dict):
                    headers.update(raw_headers)
                elif isinstance(raw_headers, (list, tuple)):
                    headers.update(dict(raw_headers))
            result = result[0]
```

### [astrbot/core/message/components.py:136-159] MediaResolver 对非 base64 的 data: URI 与不存在的本地路径返回原串，后续 open() 崩溃 [严重程度：低]
- 严重程度：低
- 问题所在行号：136-159
- 问题描述：

```python
        if source.startswith("data:"):
            m = re.match(r"data:[^;]+;base64,(.*)", source, re.S)
            if m:
                ...
                return self._write_base64(m.group(1), suffix)
        ...
        return source
```

(1) `data:text/plain,abc`（非 base64）不匹配正则，落到末尾 return source，调用方拿到 URI 字符串当文件路径用；(2) 任意不存在的本地路径原样返回。下游 to_base64 的 open(path, "rb") 抛 FileNotFoundError，错误在离根因很远的地方爆出。
- 修复建议：两个穿透分支改为显式 raise ValueError（"不支持的 data: URI"/"媒体源既非 URL/URI 也不是存在的本地文件"）；调用点已有 except Exception 兜底，不会破坏降级路径。

### [astrbot/_bridge/serialize.py:85-91, 174-185] Node/Nodes 递归反序列化无深度限制，深嵌 chain 触发 RecursionError [严重程度：低]
- 严重程度：低
- 问题所在行号：85-91（component_from_json）、174-185（component_to_json）
- 问题描述：

```python
    if ctype == "Node":
        # 还原转发子链（component_to_json 的对称结构：data.content）
        content = [
            component_from_json(c)
            for c in (data.get("data") or {}).get("content") or []
        ]
        return Node(content=content, uin=data.get("uin", "0"), name=data.get("name", ""))
```

component_from_json 对 Node/Nodes 无限递归。宿主推来的 event_json/chain_json 属于跨进程序列化边界，一条自嵌套转发消息（数千层 Node）会使 HandleHook/HandleCommand 抛 RecursionError——不是 json.JSONDecodeError，不在 360-363 行等处的捕获范围内，gRPC handler 以 UNKNOWN 失败，宿主重试还可能反复触发。Python 默认递归上限 1000，构造并不困难。
- 修复建议：

```python
_MAX_NODE_DEPTH = 50

def component_from_json(data: dict, _depth: int = 0) -> BaseMessageComponent:
    ...
    if ctype == "Node":
        if _depth >= _MAX_NODE_DEPTH:
            return Unknown(text="")
        content = [
            component_from_json(c, _depth + 1)
            for c in (data.get("data") or {}).get("content") or []
        ]
```

（component_to_json 对称加 _depth。）

### [astrbot/api/web.py:361-369] file_response 在事件循环内同步 read_bytes 最多 64MB [严重程度：低]
- 严重程度：低
- 问题所在行号：361-369
- 问题描述：

```python
    path = Path(path)
    # 先做存在性与大小检查再读文件，避免超大文件整读进内存
    if not path.is_file():
        raise FileNotFoundError("请求的文件不存在或不可读")
    if path.stat().st_size > 64 * 1024 * 1024:
        raise ValueError("文件大小超过 64MB 上限，拒绝以 file_response 发送")
    body = path.read_bytes()
```

file_response 是同步函数，通常在 async Web handler（常驻 loop 线程）里直接调用。64MB 上限内的 read_bytes 在 loop 线程同步执行：机械盘/网络盘上一次读可耗时数百 ms 到秒级，期间所有 async handler 停摆。同文件 PluginUploadFile.save（78-81 行）已正确用 asyncio.to_thread。
- 修复建议：提供异步版 afile_response，检查与读盘全部 asyncio.to_thread；将 file_response 的头部组装抽成共用函数。

### [astrbot/core/platform/astr_message_event.py:543-547 + core/star/filter/permission.py:62-71] 宿主事件只映射 admin/member，OWNER/GROUP_ADMIN 权限位永远不匹配 [严重程度：低]
- 严重程度：低（fail-closed，功能失效）
- 问题所在行号：astr_message_event.py 543-547、permission.py 62-71
- 问题描述：

```python
        event.role = "admin" if is_admin else "member"
```

```python
        if flag == PermissionType.OWNER:
            return role == "owner"
        if flag == PermissionType.ADMIN:
            return role == "admin"
        if flag == PermissionType.GROUP_ADMIN:
            return is_group and role in ("admin", "owner")
```

from_event_json 把 role 二值化为 admin/member，宿主侧事件 JSON 没有 owner/群管理员维度字段。插件用 @filter.permission_type(PermissionType.GROUP_ADMIN) 或 OWNER 注册的命令，即使真正的群主/群管调用也恒被拒；用户表现为"命令不存在/无响应"。
- 修复建议：事件 JSON 有扩展字段时优先读取：

```python
        raw_role = (metadata or {}).get("role") or (data.get("sender_role"))
        if raw_role in ("admin", "owner", "member"):
            event.role = raw_role
        else:
            event.role = "admin" if is_admin else "member"
```